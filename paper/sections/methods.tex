% =============================================================
% Section 3: Methods
% =============================================================
\section{Methods}\label{sec:methods}

We state the FD reference (Sec.~\ref{sec:fwd-problem}), the
forward (Sec.~\ref{sec:fwd-pinn}) and inverse
(Sec.~\ref{sec:inv-pinn}) PINNs, the five extraction methods
M1--M5 (Sec.~\ref{sec:qnm-methods}), and the reporting convention
(Sec.~\ref{sec:reporting}). M4 and M5 are the new estimators.

% --------------------------------------------------------------
\subsection{Schwarzschild Zerilli problem}
\label{sec:fwd-problem}

The forward problem solved by the PINN of Sec.~\ref{sec:fwd-pinn}
is the Zerilli equation Eq.~\eqref{eq:zerilli} of
Sec.~\ref{sec:bg-zerilli}, with the polar potential
$V_{\ell}^{\mathrm{Z}}$ of Eq.~\eqref{eq:V-zerilli} at $\ell = 2$
and $M = 1$. The numerical domain is $x \in [-50,\,150]\,M$ and
$t \in [0,\,50]\,M$. The initial data are an outgoing Gaussian
pulse,
\begin{equation}
  \Phi(x,0) = A_0\,\exp\!\left[-\frac{(x-x_0)^2}{2\sigma_0^2}\right],
  \qquad
  \partial_t \Phi(x,0) = -\partial_x \Phi(x,0),
  \label{eq:ic}
\end{equation}
with $A_0 = 1$, $x_0 = 4\,M$ and $\sigma_0 = 5\,M$. The right-hand side of the velocity
condition imposes a purely outgoing Gaussian, in contrast to the
quadratic $\partial_t \Phi \propto (x-x_0)^2 \Phi$ used by
\citet{Patel2024pinn}. We adopt the standard outgoing form, which is the conventional
initial condition for the Zerilli equation \citep{Berti2009review}. The asymptotic outgoing QNM
boundary conditions Eq.~\eqref{eq:qnm-bc} are not
imposed exactly at the truncated boundaries; instead we apply
first-order Sommerfeld absorbing conditions,
\begin{equation}
  (\partial_t - \partial_x)\Phi\bigl|_{x=x_{\min}} = 0, \qquad
  (\partial_t + \partial_x)\Phi\bigl|_{x=x_{\max}} = 0,
  \label{eq:bc-sommerfeld}
\end{equation}
identical to those used by \citet{Patel2024pinn}. The spatial
domain $[-50,150]\,M$ is sized so that the wavefront, propagating
at unit speed from $x_0 = 4\,M$, never reaches either boundary
within the simulated interval $t \in [0,50]\,M$. The right-going
and left-going arrival times are
\begin{equation}
  t_{R} = x_{\max} - x_0 = 146\,M, \qquad
  t_{L} = x_0 - x_{\min} = 54\,M,
  \label{eq:bc-arrival}
\end{equation}
both larger than the QNM extraction window $t \in [10,50]\,M$ at
the observer position $x_{\mathrm{obs}} = 10\,M$, so the residuals
of Eq.~\eqref{eq:bc-sommerfeld} act only as soft regularisers in a
region that is causally disconnected from the initial pulse on the
simulated interval, and therefore does not affect the extracted
modes.

The reference solution is obtained by a fourth-order Runge--Kutta
method-of-lines finite-difference (FD) integration of
Eq.~\eqref{eq:zerilli} with $\Delta x = 0.2\,M$ and
$\Delta t = 0.1\,M$. This FD waveform is used throughout as the ground
truth for benchmarking PINN field accuracy and for cross-checking the
QNM extraction methods on a non-PINN signal. The Leaver reference
values $M\omega_0 = 0.3737$, $\tau_0/M = 11.241$ (fundamental) and
$M\omega_1 = 0.3467$, $\tau_1/M = 3.651$ (first overtone) of
Eq.~\eqref{eq:qnm-ref} are used for percentage-error reporting.
Figure~\ref{fig:waveform-3d} shows the full FD reference field
$\Phi_{\mathrm{FD}}(x, t)$ as a 3D surface, with the outgoing
Gaussian initial condition producing a right-moving free pulse and
a back-scattered left-moving component, the photon-sphere
barrier at $r \simeq 3M$ trapping a fraction of the energy that
then leaks out as the QNM ringdown along $x \simeq 0$--$15\,M$,
and the ringdown decay visible as the amplitude decrease in the
$t$ direction at the observer column $\xq = 10\,M$.

\begin{figure}[!htbp]
\centering
\begin{minipage}[t]{0.49\textwidth}
  \centering
  \includegraphics[width=\linewidth]{../outputs/pinn/waveform_3d_full.png}
  \subcaption{Full domain $x \in [-50, 150]\,M$, $t \in [0, 50]\,M$.
  The right-moving free pulse from the outgoing initial condition
  is visible as the diagonal ridge propagating to the right (towards
  infinity); the left-moving ridge towards the horizon is the
  back-scattered component generated by the photon-sphere barrier.}
  \label{fig:waveform-3d-full}
\end{minipage}\hfill
\begin{minipage}[t]{0.49\textwidth}
  \centering
  \includegraphics[width=\linewidth]{../outputs/pinn/waveform_3d_zoom.png}
  \subcaption{Zoom on the QNM region $x \in [-20, 80]\,M$. The
  ringdown is visible as the small-amplitude decaying oscillation
  near $x \simeq 10\,M$ for $t \gtrsim 10\,M$, sandwiched between
  the right-moving free pulse and the back-scattered left-mover.}
  \label{fig:waveform-3d-zoom}
\end{minipage}
\caption{FD reference field $\Phi_{\mathrm{FD}}(x, t)$ for
Schwarzschild $\ell = 2$, generated by the fourth-order
Runge--Kutta method-of-lines integration of
Eq.~\eqref{eq:zerilli} with the initial data of
Eq.~\eqref{eq:ic} and the Sommerfeld boundaries of
Eq.~\eqref{eq:bc-sommerfeld}). All PINN field-accuracy
metrics in Sec.~\ref{sec:results} are computed against this
reference.}
\label{fig:waveform-3d}
\end{figure}

% --------------------------------------------------------------
\subsection{Forward PINN}
\label{sec:fwd-pinn}

We solve the forward problem of Sec.~\ref{sec:fwd-problem} with a
fully-connected PINN
\citep{Raissi2017} implemented in DeepXDE \citep{Lu2021deepxde} with
the PyTorch backend.

The network architecture, output transform and weight initialiser
are chosen identically to \citet{Patel2024pinn} so that any
difference in extracted QNMs between the two works can be attributed
to the loss formulation, training schedule, sampling strategy or
extraction methodology rather than to the network itself.
The network has hidden layers $[80,40,20,10]$ with $\tanh$
activations and a single scalar output $\Phi(x,t)$ from input
$(x,t) \in \mathbb{R}^{2}$, giving $4{,}521$ trainable
parameters.\footnote{$2{\cdot}80 + 80 + 80{\cdot}40 + 40 +
40{\cdot}20 + 20 + 20{\cdot}10 + 10 + 10{\cdot}1 + 1 = 4521$.} The
raw scalar output $\hat y(x,t)$ is wrapped in a tanh envelope,
$\Phi_\theta(x,t) = A\tanh\bigl(\hat y(x,t)\bigr)$ with
$A = A_0 = 1$ taken as the dynamic range of the initial data,
to keep activations within
the dynamic range of $\Phi$ over the whole domain.
The architecture is summarised in Fig.~\ref{fig:fwd-pinn-arch},
together with the automatic-differentiation
pipeline that produces the residual losses defined below.

\begin{figure}[!htbp]
\centering
\begin{tikzpicture}[
  node distance=10mm and 12mm,
  every node/.style={font=\small},
  inp/.style={circle, draw, fill=blue!10, minimum size=7mm, inner sep=0pt},
  hid/.style={circle, draw, fill=gray!12, minimum size=4mm, inner sep=0pt},
  outn/.style={circle, draw, fill=orange!20, minimum size=7mm, inner sep=0pt},
  loss/.style={rectangle, draw, fill=green!10, rounded corners=2pt,
               minimum height=6mm, minimum width=18mm, inner sep=2pt,
               align=center, font=\footnotesize},
  arr/.style={->, >=stealth, thin, gray!70},
]
  % Inputs
  \node[inp] (x) at (0, 0) {$x$};
  \node[inp] (t) at (0, -1.2) {$t$};

  % Layer geometry: visually decreasing column heights to reflect 80, 40, 20, 10.
  % Layer 1: 8 nodes, Layer 2: 4 nodes, Layer 3: 2 nodes, Layer 4: 1 node.
  % Use vertical spacing 0.45 cm between nodes; centre each column at y=-0.6.
  \def\xL{2.0}
  \def\xLii{4.0}
  \def\xLiii{6.0}
  \def\xLiv{8.0}

  % Layer 1: 8 nodes, ys from +1.6 to -2.8 (step 0.55)
  \foreach \k/\y in {1/1.6, 2/1.05, 3/0.5, 4/-0.05, 5/-0.6, 6/-1.15, 7/-1.7, 8/-2.25}{
    \node[hid] (a\k) at (\xL, \y) {};
  }
  % Layer 2: 4 nodes, centred at y=-0.6, spacing 0.65
  \foreach \k/\y in {1/0.4, 2/-0.25, 3/-0.9, 4/-1.55}{
    \node[hid] (b\k) at (\xLii, \y) {};
  }
  % Layer 3: 2 nodes
  \foreach \k/\y in {1/-0.2, 2/-1.0}{
    \node[hid] (c\k) at (\xLiii, \y) {};
  }
  % Layer 4: 1 node
  \node[hid] (d1) at (\xLiv, -0.6) {};

  % Layer width labels
  \node[font=\scriptsize] at (\xL, 2.15) {$80$};
  \node[font=\scriptsize] at (\xLii, 0.95) {$40$};
  \node[font=\scriptsize] at (\xLiii, 0.35) {$20$};
  \node[font=\scriptsize] at (\xLiv, -0.05) {$10$};

  % Output
  \node[outn] (phi) at (10.0, -0.6) {$\Phi_\theta$};

  % Connections (sparse, only a sample to suggest dense connectivity)
  \foreach \src in {x, t}
    \foreach \dst in {a1, a3, a5, a7}
      \draw[arr] (\src) -- (\dst);
  \foreach \a in {1,3,5,7}
    \foreach \b in {1,2,3,4}
      \draw[arr, opacity=0.30] (a\a) -- (b\b);
  \foreach \a in {1,2,3,4}
    \foreach \b in {1,2}
      \draw[arr, opacity=0.30] (b\a) -- (c\b);
  \foreach \a in {1,2}
      \draw[arr, opacity=0.45] (c\a) -- (d1);
  \draw[arr] (d1) -- (phi);

  % Loss boxes on the right
  \node[loss, right=14mm of phi, yshift=14mm] (lpde)
    {gPINN: $\mathcal{L}_{\mathrm{r}},\,\mathcal{L}_{\partial_x r},\,\mathcal{L}_{\partial_t r}$};
  \node[loss, right=14mm of phi] (lic)
    {IC: $\mathcal{L}_{\Phi}^{\mathrm{IC}},\,\mathcal{L}_{\partial_t\Phi}^{\mathrm{IC}}$};
  \node[loss, right=14mm of phi, yshift=-14mm] (lbc)
    {BC: $\mathcal{L}_{\mathrm{BC}}^{\mathrm{left}},\,\mathcal{L}_{\mathrm{BC}}^{\mathrm{right}}$};
  \draw[arr, thick, black] (phi) -- (lpde) node[midway, above, font=\scriptsize, sloped] {AD};
  \draw[arr, thick, black] (phi) -- (lic);
  \draw[arr, thick, black] (phi) -- (lbc);

  % Sum to total loss
  \node[loss, fill=green!25, right=8mm of lic] (ltot)
    {$\mathcal{L}_{\mathrm{fwd}}$\\Eq.~\eqref{eq:loss-fwd}};
  \draw[arr, thick, black] (lpde) -| (ltot);
  \draw[arr, thick, black] (lic) -- (ltot);
  \draw[arr, thick, black] (lbc) -| (ltot);

  % Section labels (placed above the layer columns).
  \node[font=\scriptsize, gray!70!black] at (0.0, 2.7) {inputs};
  \node[font=\scriptsize, gray!70!black] at (5.0, 2.7) {$[80,40,20,10]$ tanh MLP};
  \node[font=\scriptsize, gray!70!black] at (10.0, 2.7) {output transform};
  \node[font=\scriptsize, gray!70!black] at (13.5, 2.7) {weighted loss terms};
\end{tikzpicture}
\caption{Forward PINN architecture used in this work. Inputs
$(x,t)$ pass through a fully-connected $[80, 40, 20, 10]$ $\tanh$
network with $4{,}521$ trainable parameters, whose scalar output
is wrapped in the tanh envelope
$\Phi_\theta(x,t) = A\tanh(\hat y(x,t))$ with $A = 1$. Spatial and
temporal derivatives required by the Zerilli PDE residual
$\mathcal{L}_{\mathrm{r}}$ of Eq.~\eqref{eq:loss-fwd} and its
gradient augmentations $\mathcal{L}_{\partial_x r},
\mathcal{L}_{\partial_t r}$ (gPINN, \citealt{Yu2022gpinn}) are
computed by automatic differentiation; the IC residuals
$\mathcal{L}_{\Phi}^{\mathrm{IC}}, \mathcal{L}_{\partial_t\Phi}^{\mathrm{IC}}$ and the left/right BC residuals
$\mathcal{L}_{\mathrm{BC}}^{\mathrm{left}}, \mathcal{L}_{\mathrm{BC}}^{\mathrm{right}}$ enter as additional weighted terms with
$\boldsymbol{\lambda} = (100,100,100,1,100,1,1)$
(Eq.~\eqref{eq:weights}). The decreasing layer widths are
reflected in the visually shrinking node columns.}
\label{fig:fwd-pinn-arch}
\end{figure}

The training loss combines PDE, initial-condition and boundary
residuals. Let $r_\theta(x,t) \equiv \partial_t^2\Phi_\theta -
\partial_x^2\Phi_\theta + V_\ell\,\Phi_\theta$ be the pointwise
residual of Eq.~\eqref{eq:zerilli}. The training loss is the
standard composite of PDE, initial-condition and boundary-condition
residuals, augmented with the gradient terms of the
gradient-enhanced PINN \citep{Yu2022gpinn},
\begin{equation}
  \mathcal{L}_{\mathrm{fwd}}
  = \lambda_{r}     \Lpde
  + \lambda_{r_x}   \mathcal{L}_{\partial_x r}
  + \lambda_{r_t}   \mathcal{L}_{\partial_t r}
  + \lambda_{\mathrm{IC}} \mathcal{L}_{\Phi}^{\mathrm{IC}}
  + \lambda_{\mathrm{IV}} \mathcal{L}_{\partial_t\Phi}^{\mathrm{IC}}
  + \lambda_{\mathrm{BL}} \mathcal{L}_{\mathrm{BC}}^{\mathrm{left}}
  + \lambda_{\mathrm{BR}} \mathcal{L}_{\mathrm{BC}}^{\mathrm{right}}.
  \label{eq:loss-fwd}
\end{equation}
The seven mean-squared component losses are
\begin{align}
  \Lpde &= \frac{1}{N_r}\sum_{(x,t)\in\mathcal{X}_r}
            r_\theta(x,t)^{2},
  & \mathcal{L}_{\partial_x r} &= \frac{1}{N_r}\sum_{(x,t)\in\mathcal{X}_r}
            \bigl(\partial_x r_\theta(x,t)\bigr)^{2},
  \nonumber\\
  \mathcal{L}_{\partial_t r} &= \frac{1}{N_r}\sum_{(x,t)\in\mathcal{X}_r}
            \bigl(\partial_t r_\theta(x,t)\bigr)^{2},
  & \mathcal{L}_{\Phi}^{\mathrm{IC}} &= \frac{1}{N_i}\sum_{x\in\mathcal{X}_i}
            \bigl(\Phi_\theta(x,0)-\Phi(x,0)\bigr)^{2},
  \nonumber\\
  \mathcal{L}_{\partial_t\Phi}^{\mathrm{IC}}
     &= \frac{1}{N_i}\sum_{x\in\mathcal{X}_i}
        \bigl(\partial_t\Phi_\theta(x,0)-\partial_t\Phi(x,0)\bigr)^{2},
  & \mathcal{L}_{\mathrm{BC}}^{\mathrm{left}}
     &= \frac{1}{N_b}\sum_{t\in\mathcal{X}_b^{L}}
        \bigl((\partial_t-\partial_x)\Phi_\theta(x_{\min},t)\bigr)^{2},
  \nonumber\\
  \mathcal{L}_{\mathrm{BC}}^{\mathrm{right}}
     &= \frac{1}{N_b}\sum_{t\in\mathcal{X}_b^{R}}
        \bigl((\partial_t+\partial_x)\Phi_\theta(x_{\max},t)\bigr)^{2},
  & &
  \label{eq:loss-components}
\end{align}
mirroring the formulation of \citet[Eqs.~(4)--(10)]{Patel2024pinn}.
The two gradient terms $\mathcal{L}_{\partial_x r},
\mathcal{L}_{\partial_t r}$ encourage the network to produce a
residual field that vanishes \emph{smoothly} rather than oscillating
around zero \citep{Yu2022gpinn}. The collocation set has $N_r = 32{,}000$ PDE points,
$N_i = 800$ initial-condition points and $N_b = 400$ boundary points distributed across both spatial
boundaries (so each boundary contributes $\sim 200$ points), for
$33{,}200$ space-time points in
total. \citet{Patel2024pinn} use the same $N_r$ and $N_i$ but quote
$N_b = 400$ \emph{per boundary}, giving their total of $33{,}600$;
this is the only collocation-count difference between the two
baselines. The weight vector applied throughout this work is
\begin{equation}
  \boldsymbol{\lambda}
  = (\lambda_{r},\lambda_{r_x},\lambda_{r_t},\lambda_{\mathrm{IC}},
     \lambda_{\mathrm{IV}},\lambda_{\mathrm{BL}},\lambda_{\mathrm{BR}})
  = (100,\,100,\,100,\,1,\,100,\,1,\,1).
  \label{eq:weights}
\end{equation}
The equal weight $100$ on the PDE primary residual, its two
gradient augmentations, and the velocity initial condition reflects
that the first three terms control how accurately the network
satisfies Eq.~\eqref{eq:zerilli} throughout the bulk of the domain,
while the velocity initial condition fixes the propagation
direction of the pulse at $t = 0$ and was found by
\citet{Patel2024pinn} to be unstable to under-weighting (see
below). The unit weights on the field initial condition and on the
two Sommerfeld boundaries reflect that, by
Eq.~\eqref{eq:bc-arrival}, the wavefront does not reach either
boundary within the simulated interval, so the boundary residuals
act only as soft regularisers on a region the dynamics of
Eq.~\eqref{eq:zerilli} barely populate. The choice
$\lambda_{\mathrm{IV}} = 100$ follows \citet{Patel2024pinn}, who
report empirically that down-weighting the velocity initial
condition produces a ``phase problem'' (a slow drift of the learned
waveform's phase relative to the FD reference), which the larger
weight suppresses. We observe the same behaviour and adopt the same
remedy.

Optimisation proceeds in two stages, namely $10{,}000$ iterations
of Adam \citep{Kingma2015adam} with learning rate $10^{-3}$,
followed by $30{,}000$ iterations of L-BFGS \citep{Liu1989lbfgs}.
The number of L-BFGS iterations is doubled relative to the
$15{,}000$ used by \citet{Patel2024pinn}. Empirically the field-residual loss
continues to decrease through iteration $\sim 25{,}000$ in our runs,
and the
plateau-window analysis of Sec.~\ref{sec:plateau} requires the
resulting $\Phi(\xq,t)$ to be sufficiently converged that the
plateau standard deviation is dominated by the window choice rather
than by the noise floor of the converged training loss.
We use the residual-based adaptive sampling scheme of
\citet{Lu2021deepxde} (the RAR-G variant in the taxonomy of
\citet{Wu2023adaptive}): every $1{,}000$ Adam
iterations, $10^{5}$ candidate residual points are drawn uniformly
from the spacetime domain, the PDE residual is evaluated on them in
minibatches of $5{,}000$, and the entire $N_r = 32{,}000$
collocation set is replaced by $0.3\,N_r = 9{,}600$ candidates of
largest $|r_\theta|$ together with $0.7\,N_r = 22{,}400$ candidates
drawn uniformly at random from the remainder. The greedy fraction
$0.3$ was tuned for this problem; the
uniform component prevents the collocation set from collapsing
entirely onto the wavefront at late training iterations.

All computations use double precision; we found
this necessary to avoid floating-point-induced stalls in L-BFGS for
this problem.

% --------------------------------------------------------------
\subsection{Inverse PINN}
\label{sec:inv-pinn}

The forward problem of Sec.~\ref{sec:fwd-pinn} computes
$\Phi(x,t)$ from a known PDE; the inverse problem reverses the
direction of inference and recovers unknown parameters of the
PDE, and of the asymptotic solution that the PDE supports, from
noisy observations of the field. Here the unknown parameters are
the black hole mass $M$, which enters the Zerilli potential
Eq.~\eqref{eq:V-zerilli} through every $M$-dependent term, and
the fundamental QNM template parameters
$(\omega,\tau,A_{\mathrm{ring}},\phi)$ that label the late-time
damped sinusoid of Eq.~\eqref{eq:qnm-omega} at the observer
position. The inverse PINN trains the same network of
Sec.~\ref{sec:fwd-pinn} jointly with these five scalars, treated
as learnable variables. Each scalar is appended to the optimiser's
trainable-parameter list alongside the network weights, so Adam
and L-BFGS update the network weights $\theta$ and the five
scalars $(M,\omega,\tau,A_{\mathrm{ring}},\phi)$ in the same
gradient step, with a single shared loss
\citep{Lu2021deepxde,Cornell2022pinnqnm}. The PDE residual
depends on $M$ and pushes the optimiser to values of $M$ for
which $\Phi_\theta$ satisfies the Zerilli equation; the data
residual pushes $\Phi_\theta$ to match the noisy observations;
the template residual pushes $(\omega,\tau,A_{\mathrm{ring}},\phi)$ to label the late-time behaviour of the same
$\Phi_\theta$. The shared gradient step couples all five
unknowns and the network weights, and the joint minimum is the
inverse-problem solution. Training data are noisy samples of
the true Zerilli waveform, generated by adding Gaussian noise to
the FD reference,
\begin{equation}
  \Phiobs(x_i, t_i) = \Phi_{\mathrm{FD}}(x_i, t_i) + \eta_i,
  \qquad
  \eta_i \sim \mathcal{N}(0,\sigma_{\mathrm{obs}}^2),
  \label{eq:noisy-obs}
\end{equation}
with
$\sigma_{\mathrm{obs}} = 0.01\times\max_{x,t}|\Phi_{\mathrm{FD}}(x,t)|$
and $\Nobs = 500$ points $(x_i, t_i)$ drawn uniformly from the full
two-dimensional domain $[-50,150]\,M\times[0,50]\,M$, distinct
from the one-dimensional template grid at $x = \xq$ introduced in
Eq.~\eqref{eq:loss-ring} below. The fixed observer position
$\xq = 10\,M$ is used both for the ringdown template constraint
and for all post-hoc QNM extraction.

The composite inverse loss has three blocks,
\begin{equation}
  \mathcal{L}_{\mathrm{inv}}
  = \mathcal{L}_{\mathrm{PINN}}(M)
  + \lambda_{\mathrm{data}}\,\Ldata
  + \lambda_{\mathrm{ring}}\,\Lring(\omega,\tau,A_{\mathrm{ring}},\phi),
  \label{eq:loss-inv}
\end{equation}
where $\mathcal{L}_{\mathrm{PINN}}(M)$ is the seven-term sum of
Eq.~\eqref{eq:loss-fwd} with weights as in Eq.~\eqref{eq:weights}, the
PDE residual now depending on the learnable $M$ through the Zerilli
potential. The data term is the mean squared mismatch between the
network output and the observations,
\begin{equation*}
  \Ldata = \frac{1}{\Nobs}\sum_{i=1}^{\Nobs}
  \bigl(\Phitheta(x_i,t_i) - \Phiobs(x_i,t_i)\bigr)^{2},
\end{equation*}
with weight $\lambda_{\mathrm{data}} = 10$, set empirically so that
$\lambda_{\mathrm{data}}\Ldata$ is comparable in magnitude to the
PDE residual term at the start of training; we did not tune this
value further. The ringdown term
enforces a damped-sinusoid template at the observer position,
\begin{equation}
  \Lring = \frac{1}{\Nring}\sum_{j=1}^{\Nring}
  \Bigl(\Phitheta(\xq,t_j) - A_{\mathrm{ring}}\,e^{-t_j/\tau}\cos(\omega t_j + \phi)\Bigr)^{2},
  \label{eq:loss-ring}
\end{equation}
with the times $t_j$ on a uniform grid of $\Nring$ points on
$[\tring,\,t_{\max}]$, where $t_{\max} = 50\,M$ is the end of the
simulation domain. Eq.~\eqref{eq:loss-ring} is implemented as a soft constraint that
evaluates the template residual on a one-dimensional grid of
$\Nring$ time samples at $x = \xq$; the residual
$\Phitheta(\xq,t_j) - \mathrm{template}(t_j)$ back-propagates into
both the network parameters $\theta$ and the
template parameters $(\omega,\tau,A_{\mathrm{ring}},\phi)$ on every
training step. Embedding a damped-sinusoid template as a soft
constraint inside the PINN loss is new. The template encodes the
prior knowledge that, in the QNM regime, $\Phi(\xq,t)$ takes the
damped-sinusoid form of Eq.~\eqref{eq:qnm-omega}; without it the
inverse PINN would have to discover that functional form from
noisy observations alone, and the learnable scalars
$(\omega,\tau,A_{\mathrm{ring}},\phi)$ would have no operational
definition inside the loss. Prior PINN-based black-hole
perturbation studies, including \citet{Patel2024pinn},
fit the template only after training. We therefore treat
Eq.~\eqref{eq:loss-ring} as a methodological component of this
paper and study its sensitivity to $\tring$,
$\lambda_{\mathrm{ring}}$ and $\Nring$ in
Sec.~\ref{sec:variants} below.

The architecture and the (Adam, L-BFGS) optimiser sequence are the
same as in the forward PINN; here we use $10{,}000$ Adam
iterations followed by $15{,}000$ L-BFGS iterations.
The full architecture, including the five learnable scalars
appended to the network parameters, is shown in
Fig.~\ref{fig:inv-pinn-arch}.

\begin{figure}[!htbp]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
  node distance=10mm and 12mm,
  every node/.style={font=\small},
  inp/.style={circle, draw, fill=blue!10, minimum size=7mm, inner sep=0pt},
  hid/.style={circle, draw, fill=gray!12, minimum size=4mm, inner sep=0pt},
  outn/.style={circle, draw, fill=orange!20, minimum size=7mm, inner sep=0pt},
  var/.style={rectangle, draw, fill=yellow!25, rounded corners=2pt,
              minimum height=5mm, minimum width=9mm, inner sep=2pt,
              font=\scriptsize, align=center},
  loss/.style={rectangle, draw, fill=red!10, rounded corners=2pt,
               minimum height=6mm, minimum width=22mm, inner sep=2pt,
               align=center, font=\footnotesize},
  arr/.style={->, >=stealth, thin, gray!70},
]
  % Inputs
  \node[inp] (x) at (0, 0) {$x$};
  \node[inp] (t) at (0, -1.2) {$t$};

  \def\xL{2.0}
  \def\xLii{4.0}
  \def\xLiii{6.0}
  \def\xLiv{8.0}

  % Layer 1: 8 nodes
  \foreach \k/\y in {1/1.6, 2/1.05, 3/0.5, 4/-0.05, 5/-0.6, 6/-1.15, 7/-1.7, 8/-2.25}{
    \node[hid] (a\k) at (\xL, \y) {};
  }
  % Layer 2: 4 nodes
  \foreach \k/\y in {1/0.4, 2/-0.25, 3/-0.9, 4/-1.55}{
    \node[hid] (b\k) at (\xLii, \y) {};
  }
  % Layer 3: 2 nodes
  \foreach \k/\y in {1/-0.2, 2/-1.0}{
    \node[hid] (c\k) at (\xLiii, \y) {};
  }
  % Layer 4: 1 node
  \node[hid] (d1) at (\xLiv, -0.6) {};

  % Layer width labels
  \node[font=\scriptsize] at (\xL, 2.15) {$80$};
  \node[font=\scriptsize] at (\xLii, 0.95) {$40$};
  \node[font=\scriptsize] at (\xLiii, 0.35) {$20$};
  \node[font=\scriptsize] at (\xLiv, -0.05) {$10$};

  % Output
  \node[outn] (phi) at (10.0, -0.6) {$\Phi_\theta$};

  % Connections (sparse)
  \foreach \src in {x, t}
    \foreach \dst in {a1, a3, a5, a7}
      \draw[arr] (\src) -- (\dst);
  \foreach \a in {1,3,5,7}
    \foreach \b in {1,2,3,4}
      \draw[arr, opacity=0.30] (a\a) -- (b\b);
  \foreach \a in {1,2,3,4}
    \foreach \b in {1,2}
      \draw[arr, opacity=0.30] (b\a) -- (c\b);
  \foreach \a in {1,2}
      \draw[arr, opacity=0.45] (c\a) -- (d1);
  \draw[arr] (d1) -- (phi);

  % Loss boxes on the right.
  \node[loss, right=14mm of phi, yshift=22mm] (lpinn)
    {$\mathcal{L}_{\mathrm{PINN}}(M)$\\Eq.~\eqref{eq:loss-fwd}};
  \node[loss, right=14mm of phi] (ldata)
    {$\lambda_{\mathrm{data}}\,\Ldata$\\$\Phiobs$ at $\Nobs=500$ pts};
  \node[loss, right=14mm of phi, yshift=-22mm] (lring)
    {$\lambda_{\mathrm{ring}}\,\Lring$\\template at $\xq$, Eq.~\eqref{eq:loss-ring}};

  % Edges from network output to losses
  \draw[arr, thick, black] (phi) -- (lpinn) node[midway, above, font=\scriptsize, sloped] {AD};
  \draw[arr, thick, black] (phi) -- (ldata);
  \draw[arr, thick, black] (phi) -- (lring);

  % Trainable scalar M sits above L_PINN at the top right.
  \node[var, above=4mm of lpinn] (vM) {$M$};
  \node[font=\scriptsize, gray!70!black, above=0.5mm of vM]
    {trainable scalar};
  \draw[arr, thick, black!70] (vM) -- (lpinn);

  % Trainable template scalars sit below L_ring at the bottom right.
  % The four boxes are placed first; the label sits below them so it
  % cannot collide with the L_ring loss box above.
  \node[var, below=6mm of lring.south west, anchor=north west] (vom) {$\omega$};
  \node[var, right=1mm of vom]   (vtau)  {$\tau$};
  \node[var, right=1mm of vtau]  (vA)    {$A_{\mathrm{ring}}$};
  \node[var, right=1mm of vA]    (vphi0) {$\phi$};
  \node[font=\scriptsize, gray!70!black, below=0.5mm of vom.south west, anchor=north west]
    {trainable scalars};
  \draw[arr, thick, black!70] (vom.north)   -- (vom.north   |- lring.south);
  \draw[arr, thick, black!70] (vtau.north)  -- (vtau.north  |- lring.south);
  \draw[arr, thick, black!70] (vA.north)    -- (vA.north    |- lring.south);
  \draw[arr, thick, black!70] (vphi0.north) -- (vphi0.north |- lring.south);

  % Sum to total loss
  \node[loss, fill=red!25, right=8mm of ldata] (ltot)
    {$\mathcal{L}_{\mathrm{inv}}$\\Eq.~\eqref{eq:loss-inv}};
  \draw[arr, thick, black] (lpinn) -| (ltot);
  \draw[arr, thick, black] (ldata) -- (ltot);
  \draw[arr, thick, black] (lring) -| (ltot);

  % Section labels (placed above the network).
  \node[font=\scriptsize, gray!70!black] at (0.0, 2.7) {inputs};
  \node[font=\scriptsize, gray!70!black] at (5.0, 2.7) {$[80,40,20,10]$ tanh MLP};
  \node[font=\scriptsize, gray!70!black] at (10.0, 2.7) {output};
\end{tikzpicture}%
}
\caption{Inverse PINN architecture. The same $[80,40,20,10]$
tanh MLP as the forward PINN of Fig.~\ref{fig:fwd-pinn-arch}
produces $\Phi_\theta(x,t)$. Five additional scalars
$(M,\omega,\tau,A_{\mathrm{ring}},\phi)$ (yellow boxes) are
appended to the optimiser's parameter list alongside
the network weights $\theta$.
The mass $M$ enters the Zerilli potential of the PINN PDE
residual $\mathcal{L}_{\mathrm{PINN}}(M)$ at the top, the noisy
observations $\Phiobs$ enter the data residual $\Ldata$ in the
middle, and the four template parameters
$(\omega,\tau,A_{\mathrm{ring}},\phi)$ enter the ringdown
template residual $\Lring$ at the observer column $\xq=10\,M$ at
the bottom. The total loss $\mathcal{L}_{\mathrm{inv}}$ of
Eq.~\eqref{eq:loss-inv} is back-propagated through both the
network weights and the five learnable scalars in every gradient
step; the embedded template residual is the key structural
difference from the forward PINN.}
\label{fig:inv-pinn-arch}
\end{figure}

We initialise $M_{\mathrm{init}} = 0.8$ ($20\%$ below the true mass),
$\omega_{\mathrm{init}} = 0.30$ ($\approx 20\%$ below the
$M\omega_0 = 0.3737$ value of \citet{Leaver1985}) and
$\tau_{\mathrm{init}} = 8.0$ ($\approx 29\%$ below
$\tau_0/M = 11.241$). These offsets are large enough that the
inverse pipeline cannot succeed by sitting near its
initialisation.

A schematic of the full inverse training pipeline (data generation,
loss decomposition, parameter update and post-hoc QNM extraction)
is given in Fig.~\ref{fig:pipeline}.

\begin{figure}[t]
\centering
\begin{tikzpicture}[
  node distance=0.55cm and 0.7cm,
  block/.style={draw, rounded corners, minimum width=2.7cm,
                minimum height=0.9cm, align=center, font=\footnotesize,
                fill=blue!8},
  data/.style={draw, rounded corners, minimum width=2.7cm,
               minimum height=0.9cm, align=center, font=\footnotesize,
               fill=green!10},
  loss/.style={draw, rounded corners, minimum width=2.6cm,
               minimum height=0.7cm, align=center, font=\scriptsize,
               fill=red!8},
  outbox/.style={draw, rounded corners, minimum width=2.7cm,
                 minimum height=0.9cm, align=center, font=\footnotesize,
                 fill=orange!15},
  arr/.style={->,>=stealth,thick}
]
  % Data row
  \node[data] (fd) {FD reference\\$\Phi_{\mathrm{FD}}(x,t)$};
  \node[data, right=of fd] (noise)
       {Add noise\\$\eta\sim\mathcal{N}(0,\sigma_{\mathrm{obs}}^2)$};
  \node[data, right=of noise] (obs)
       {$\Phiobs$\\$\Nobs=500$};

  % Network
  \node[block, below=1.1cm of noise] (net)
       {PINN $\Phi_\theta(x,t)$\\$+$ learnables\\$M,\omega,\tau,A_{\mathrm{ring}},\phi$};

  % Losses
  \node[loss, below left=1.0cm and 0.0cm of net] (lpinn)
       {$\mathcal{L}_{\mathrm{PINN}}(M)$\\
        $\boldsymbol{\lambda}=(100,100,100,1,100,1,1)$};
  \node[loss, below=1.0cm of net] (ldata)
       {$\lambda_{\mathrm{data}}\,\Ldata$\\$\lambda_{\mathrm{data}}=10$};
  \node[loss, below right=1.0cm and 0.0cm of net] (lring)
       {$\lambda_{\mathrm{ring}}\,\Lring$\\(template at $\xq=10M$)};

  % Optimiser
  \node[block, below=1.0cm of ldata] (opt)
       {Adam ($10^{4}$) $\to$ L-BFGS ($1.5{\times}10^{4}$)};

  % Post-hoc
  \node[outbox, below=of opt] (qnm)
       {Post-hoc QNM extraction\\(M1, M2, M3, M4, M5)};

  % Edges
  \draw[arr] (fd) -- (noise);
  \draw[arr] (noise) -- (obs);
  \draw[arr] (obs) |- (net.east);
  \draw[arr] (net) -- (lpinn);
  \draw[arr] (net) -- (ldata);
  \draw[arr] (net) -- (lring);
  \draw[arr] (lpinn) |- (opt);
  \draw[arr] (ldata) -- (opt);
  \draw[arr] (lring) |- (opt);
  \draw[arr] (opt) -- node[right,font=\scriptsize]{trained $\Phi_\theta(\xq,t)$} (qnm);
\end{tikzpicture}
\caption{Inverse PINN training and extraction pipeline. The FD
reference waveform is corrupted by $1\%$ Gaussian noise to produce
$\Nobs = 500$ observations. The PINN $\Phi_\theta$ is trained jointly
with the learnable mass $M$ and template parameters
$(\omega,\tau,A_{\mathrm{ring}},\phi)$ against the composite loss of
Eq.~\eqref{eq:loss-inv}. After training, the time series
$\Phi_\theta(\xq, t)$ at the observer is passed to the five post-hoc
QNM extraction methods of Sec.~\ref{sec:qnm-methods}.}
\label{fig:pipeline}
\end{figure}

\subsubsection{Variants of the ringdown-template loss}
\label{sec:variants}

The hyperparameters $\tring$, $\lambda_{\mathrm{ring}}$ and $\Nring$
control, respectively, the start of the ringdown window, the strength
of the template constraint relative to the PDE constraint, and the
number of template evaluation points. We study the six configurations
of Table~\ref{tab:variants}, designed as a one-factor-at-a-time
ablation followed by a combined run and a structural extension.
Variants A, B, C each modify exactly one hyperparameter relative to
the baseline; variant D combines all three; variant E adds the first
overtone to the ringdown template as a second damped sinusoid with
its own learnable $(\omega_1,\tau_1,A_1,\phi_1)$, and the original
template parameters of Eq.~\eqref{eq:loss-ring} are relabelled
$(\omega_0,\tau_0,A_0^{\mathrm{ring}},\phi_0)$ in this variant.
The overtone is initialised at $\omega_1^{\mathrm{init}} = 0.28$
($\approx 19\%$ below the Leaver value $M\omega_1 = 0.3467$) and
$\tau_1^{\mathrm{init}} = 4.7$ ($\approx 29\%$ above
$\tau_1/M = 3.651$), matching the offset philosophy adopted for the
fundamental in Sec.~\ref{sec:inv-pinn}. The subscripts
$\omega_n,\tau_n$ used here for the two learnable template modes are
distinct from the Leaver overtone labels of Eq.~\eqref{eq:qnm-ref}
and from the two-mode NLS post-hoc fit of Eq.~\eqref{eq:two-mode}
below; the three notations agree numerically only at the truth.

\begin{table}[t]
\centering
\caption{Inverse PINN variants. All variants share the architecture
of Sec.~\ref{sec:fwd-pinn} (Eq.~\eqref{eq:weights} weights), the
optimiser schedule of Sec.~\ref{sec:inv-pinn}, the noisy data of
Eq.~\eqref{eq:noisy-obs} with $\Nobs = 500$ and
$\sigma_{\mathrm{obs}} = 0.01\,\Phi_{\max}$, and the observer
position $\xq = 10\,M$. Variant E adds an overtone to the template
with learnable $(\omega_1,\tau_1,A_1,\phi_1)$, initialised at
$\omega_1 = 0.28$, $\tau_1 = 4.7$, $A_1 = 0.25$, $\phi_1 = 0$.
``Single mode'' refers to the template of
Eq.~\eqref{eq:loss-ring}; ``two mode'' replaces the template by a sum
of two damped sinusoids. Bold values mark the hyperparameter changed
relative to the baseline.}
\label{tab:variants}
\small
\begin{tabular}{@{}llcccl@{}}
\toprule
Tag & Config name & $\tring/M$ & $\lambda_{\mathrm{ring}}$ & $\Nring$ & Template \\
\midrule
baseline   & \texttt{inverse\_qnm}            & $10$ & $1$   & $200$  & single mode \\
A          & \texttt{inverse\_qnm\_tring18}   & $\mathbf{18}$ & $1$   & $200$  & single mode \\
B          & \texttt{inverse\_qnm\_lring100}  & $10$ & $\mathbf{100}$ & $200$  & single mode \\
C          & \texttt{inverse\_qnm\_nring1000} & $10$ & $1$   & $\mathbf{1000}$ & single mode \\
\textbf{D} & \texttt{inverse\_qnm\_combo}     & $\mathbf{18}$ & $\mathbf{100}$ & $\mathbf{1000}$ & single mode \\
E          & \texttt{inverse\_qnm\_2mode}     & $18$ & $100$ & $1000$ & two modes \\
\bottomrule
\end{tabular}
\end{table}

The choice $\tring = 18\,M$ in variants A, D, E is motivated
\emph{a~posteriori} by the M5 plateau scan of
Sec.~\ref{sec:qnm-methods} on the forward waveform, which identifies
$t_0 \in [10,\,16.7]\,M$ as the lowest-bias plateau for the
fundamental. The value $18\,M$ sits at the upper edge, where overtone
contamination is negligible
($e^{-18/\tau_1} = e^{-18/3.651} \approx 7\times 10^{-3}$) while
the fundamental still retains
$e^{-18/\tau_0} = e^{-18/11.241} \approx 0.20$ of its initial
amplitude. The choice $\lambda_{\mathrm{ring}} = 100$ in variants B,
D, E places the template constraint at the same weight as the PDE
primary residual $\lambda_r = 100$ in
Eq.~\eqref{eq:weights}. With the default $\lambda_{\mathrm{ring}} = 1$
the template is two orders of magnitude weaker than $\lambda_r$, and
the QNM parameters drift to whatever configuration minimises the
residual under the much weaker template penalty.

The ringdown loss term Eq.~\eqref{eq:loss-ring} contains an
$A_{\mathrm{ring}}$--$\tau$ degeneracy that follows directly from
the shape of the template inside the fit window. Expand the
envelope at the early edge of the window,
$A_{\mathrm{ring}} e^{-t/\tau} = A_{\mathrm{ring}}\bigl(1 - t/\tau + \mathcal{O}(t^2/\tau^2)\bigr)$,
so that to leading order the envelope enters the residual only
through the two combinations $A_{\mathrm{ring}}$ and
$A_{\mathrm{ring}}/\tau$. Pairs $(A_{\mathrm{ring}},\tau)$ that
share the same $A_{\mathrm{ring}}$ at the start of the window and
the same ratio $A_{\mathrm{ring}}/\tau$ are therefore
indistinguishable to first order in $t/\tau$; equivalently, a
smaller $\tau$ paired with a larger $A_{\mathrm{ring}}$ produces
a fit of comparable residual at the early edge of the template
window. At late times the envelope is exponentially suppressed
and contributes negligibly to $\Lring$, so the second-order
curvature term that would lift the degeneracy is too weak to
constrain the optimiser. The optimiser drifts down this
degenerate direction during L-BFGS,
so the \emph{learned} $\tau_{\mathrm{learned}}$ recorded in the
parameter history can differ substantially from the decay rate
actually present in the converged $\Phi_\theta(\xq,t)$. A direct
empirical diagnostic of this effect is that M2 evaluated on the
variant's own training-template window $[\tring,\,50]\,M$
recovers the (biased) learned $\tau_{\mathrm{learned}}$, whereas
M2 evaluated on the wider window $[10,\,50]\,M$ recovers a value
much closer to the Leaver value. The reporting convention of
Sec.~\ref{sec:reporting} therefore quotes M2 over the wide window
as the primary $\tau$ across all variants. The narrower-window
value is reported separately as a diagnostic in
Sec.~\ref{sec:results} to characterise the degeneracy.

% --------------------------------------------------------------
\subsection{Curriculum PINN}
\label{sec:cur-pinn}

The forward PINN of Sec.~\ref{sec:fwd-pinn} trains a single
network on the full temporal domain $t \in [0,50]\,M$. The
pointwise error of the converged baseline grows monotonically
with $t$: the late-time band $t \in [37.5,\,50]\,M$ contributes
$\sim 41\%$ of the total RMSD against the FD reference, despite
spanning only a quarter of the domain. This motivates a
curriculum-learning strategy
\citep{Bengio2009curriculum,Krishnapriyan2021failure} in which
the temporal domain is split into $K$ contiguous windows and a
separate PINN is trained sequentially on each window, with the
trained $k$-th PINN providing the initial conditions for the
$(k{+}1)$-th. Smaller temporal sub-domains carry less of the
late-time decay range and therefore an easier optimisation
landscape; the price is that the initial conditions of every
window after the first are themselves PINN outputs, and so
inherit the residual error of the previous window.

\paragraph{Relation to causal training.} The hard-window
curriculum used here is the discrete limit of the continuous
causal-training scheme of \citet{Wang2022causal}, in which the
PDE-residual loss is reweighted by a soft causal envelope
$w(t) = \exp\!\bigl(-\varepsilon \int_{t_{\min}}^{t}
\mathcal{R}(\tau)\,d\tau\bigr)$ that suppresses gradient flow
from time $t$ until the residual $\mathcal{R}$ at all earlier
times has been driven down. Both strategies diagnose the same
pathology---a vanilla PINN can minimise its total residual by
fitting the late-time decay before the early-time pulse, in
violation of the underlying causal structure---and both impose
an earliest-first ordering on the optimisation. The hard
partition adopted here trades the continuous reweighting of
\citet{Wang2022causal} for a discrete handoff between $K$
independent sub-networks: this couples cleanly to the
three-window soft-overlap variant of
Sec.~\ref{sec:res-cur} ($\Delta_{\mathrm{ov}}=10\,M$,
$\lambda_{\mathrm{cont}}=10$), which sits between the hard
partition and the fully soft causal envelope.

\paragraph{Hard partition.} The general $K$-window curriculum
splits the temporal domain at $K-1$ split points
$t_{\min} < t_{1} < \dots < t_{K-1} < t_{\max}$ into $K$ disjoint
windows
\begin{equation}
  W_{k} = [t_{k-1},\,t_{k}],
  \qquad
  t_{0} = t_{\min},\;\; t_{K} = t_{\max}.
  \label{eq:cur-split}
\end{equation}
On each window we train an independent PINN
$\Phi_{\theta_{k}}$ with the same architecture as
Sec.~\ref{sec:fwd-pinn}, the loss Eq.~\eqref{eq:loss-fwd},
weights Eq.~\eqref{eq:weights}, and optimiser schedule (Adam
$10{,}000$ + L-BFGS $30{,}000$). Window $W_{1}$ uses the analytic
Gaussian-pulse initial conditions of Eq.~\eqref{eq:ic};
for $k \ge 2$ the initial conditions at $t = t_{k-1}$ are
extracted from the trained $\Phi_{\theta_{k-1}}$ at the matching
time slice,
\begin{equation}
  \Phi^{\mathrm{IC}}_{k}(x) = \Phi_{\theta_{k-1}}(x,\,t_{k-1}),
  \qquad
  \partial_{t}\Phi^{\mathrm{IC}}_{k}(x)
   = \partial_{t}\Phi_{\theta_{k-1}}(x,\,t_{k-1}),
  \label{eq:cur-ic}
\end{equation}
with the velocity field $\partial_{t}\Phi_{\theta_{k-1}}$
computed by automatic differentiation on the trained network and
both fields then evaluated by cubic-spline interpolation at the
collocation points of $W_{k}$. The IC residuals
$\mathcal{L}_{\Phi}^{\mathrm{IC}},
\mathcal{L}_{\partial_{t}\Phi}^{\mathrm{IC}}$ in
Eq.~\eqref{eq:loss-components} are then evaluated against
Eq.~\eqref{eq:cur-ic} rather than against the analytic IC. The
greedy adaptive resampler of Sec.~\ref{sec:fwd-pinn} is restarted
fresh on each window so that $0.3 N_{r}$ collocation points are
concentrated on the window's own residual field. The final
stitched field is
\begin{equation}
  \Phi_{\mathrm{cur}}(x,t)
  = \Phi_{\theta_{k}}(x,t),
  \qquad t \in W_{k},
  \label{eq:cur-stitch}
\end{equation}
with the splits $\{t_{k}\}_{k=1}^{K-1}$ being the only points at
which two networks could disagree; we adopt the convention that
$\Phi_{\theta_{k+1}}$ owns the splice time exactly.

\paragraph{Soft-overlap partition.} The hard partition of
Eq.~\eqref{eq:cur-stitch} produces a discontinuity in the stitched
field whenever
$\Phi_{\theta_{k}}(x,t_{k}) \ne \Phi_{\theta_{k+1}}(x,t_{k})$,
which occurs generically because $\Phi_{\theta_{k+1}}$ is trained
against an interpolated rather than exact initial condition. We
also implement a soft-overlap variant in which two consecutive
windows share an overlap band of width $\Delta_{\mathrm{ov}}$
centred on $t_{\mathrm{split}}$,
\begin{equation}
  W_{1}^{\mathrm{ov}}
   = [t_{\min},\,t_{\mathrm{split}} + \Delta_{\mathrm{ov}}/2],
  \qquad
  W_{2}^{\mathrm{ov}}
   = [t_{\mathrm{split}} - \Delta_{\mathrm{ov}}/2,\,t_{\max}],
  \label{eq:cur-soft}
\end{equation}
with two additional ingredients on top of Eq.~\eqref{eq:cur-ic}.
First, $\Phi_{\theta_{2}}$ receives a fixed
\emph{continuity-anchor} loss term on a uniform grid of
$n_{x} \times n_{t}$ points across the overlap rectangle
$[x_{\min},x_{\max}] \times
[t_{\mathrm{split}} - \Delta_{\mathrm{ov}}/2,\,
 t_{\mathrm{split}} + \Delta_{\mathrm{ov}}/2]$,
\begin{equation}
  \mathcal{L}_{\mathrm{cont}}
  = \frac{\lambda_{\mathrm{cont}}}{n_{x} n_{t}}
    \sum_{i=1}^{n_{x}}\sum_{j=1}^{n_{t}}
    \bigl(\Phi_{\theta_{2}}(x_{i},t_{j})
        - \Phi_{\theta_{1}}(x_{i},t_{j})\bigr)^{2},
  \label{eq:cur-anchor}
\end{equation}
that pulls $\Phi_{\theta_{2}}$ towards the (already-trained)
$\Phi_{\theta_{1}}$ across the entire overlap rather than at the
splice time alone. Second, the final field on the overlap is a
smoothstep-blended average of the two networks,
\begin{equation}
  \Phi_{\mathrm{cur}}^{\mathrm{ov}}(x,t)
  = \bigl(1 - s(\xi)\bigr)\,\Phi_{\theta_{1}}(x,t)
   + s(\xi)\,\Phi_{\theta_{2}}(x,t),
  \quad
  s(\xi) = \xi^{2}(3 - 2\xi),
  \quad
  \xi = \frac{t - t_{\mathrm{ov}}^{-}}{\Delta_{\mathrm{ov}}},
  \label{eq:cur-blend}
\end{equation}
with $t_{\mathrm{ov}}^{-} = t_{\mathrm{split}} -
\Delta_{\mathrm{ov}}/2$, on the band
$t \in W_{1}^{\mathrm{ov}} \cap W_{2}^{\mathrm{ov}}$. The
smoothstep $s$ is $C^{1}$ and satisfies $s(0) = 0$, $s(1) = 1$,
$s'(0) = s'(1) = 0$, so the blended field is continuously
differentiable across the overlap edges. Outside the overlap the
stitching reduces to Eq.~\eqref{eq:cur-stitch}.

We study three configurations of the curriculum PINN, summarised
in Table~\ref{tab:cur-variants}: a 2-window hard partition at
$t_{\mathrm{split}} = 25\,M$, a 3-window hard partition at
$\{17,34\}\,M$, and a 2-window soft-overlap partition with
$\Delta_{\mathrm{ov}} = 10\,M$, $n_{x} \times n_{t} = 201\times 11$
continuity anchors and $\lambda_{\mathrm{cont}} = 10$. All three
variants share the per-window architecture of
Sec.~\ref{sec:fwd-pinn}, the loss Eq.~\eqref{eq:loss-fwd}, the
weights Eq.~\eqref{eq:weights}, and the (Adam + L-BFGS) optimiser
schedule applied independently per window (so the total training
budget scales linearly with $K$).

\begin{table}[!htbp]
\centering
\caption{Curriculum PINN variants. ``Windows'' lists the temporal
sub-domains in units of $M$. The hard-partition variants stitch
through Eq.~\eqref{eq:cur-stitch}; the soft-overlap variant
additionally applies the continuity anchors of
Eq.~\eqref{eq:cur-anchor} and the smoothstep blend of
Eq.~\eqref{eq:cur-blend} on the overlap band.}
\label{tab:cur-variants}
\small
\setlength{\tabcolsep}{6pt}
\begin{tabular}{@{}llll@{}}
\toprule
Tag      & Config name                          & Windows                          & Stitching \\
\midrule
Cur-2w   & \texttt{curriculum}                  & $[0,25]\cup[25,50]$              & hard \\
Cur-3w   & \texttt{curriculum\_3w}              & $[0,17]\cup[17,34]\cup[34,50]$   & hard \\
Cur-soft & \texttt{soft\_overlap\_curriculum}   & $[0,30]\cup[20,50]$              & soft, $\Delta_{\mathrm{ov}}{=}10$, $\lambda_{\mathrm{cont}}{=}10$ \\
\bottomrule
\end{tabular}
\end{table}

The schematic of the three partitions is shown in
Fig.~\ref{fig:cur-partition}, alongside the IC-transfer arrows
that distinguish each variant.

\begin{figure}[!htbp]
\centering
\begin{tikzpicture}[
  every node/.style={font=\small},
  win/.style={draw, rounded corners=2pt, minimum height=9mm,
              inner sep=2pt, font=\small},
  ovl/.style={draw, rounded corners=2pt, minimum height=9mm,
              inner sep=2pt, fill=purple!25, font=\small},
  arr/.style={->, >=stealth, thick, gray!50!black},
  tick/.style={font=\scriptsize, gray!50!black},
]
  % Horizontal axis scale.
  \def\sc{0.28}    % cm per M
  \def\xo{-0.5*50*\sc} % so t=0 sits at this x

  % --- Cur-2w: [0,25] U [25,50] ---
  \node[font=\small\bfseries, anchor=east] at ({\xo - 0.4}, 2.6) {Cur-2w:};
  \node[win, fill=orange!20, minimum width={25*\sc cm}, anchor=west] (a1)
       at ({0*\sc + \xo}, 2.6) {$W_{1}=[0,25]M$};
  \node[win, fill=blue!15, minimum width={25*\sc cm}, anchor=west] (a2)
       at ({25*\sc + \xo}, 2.6) {$W_{2}=[25,50]M$};
  % IC transfer arrow lifted above the boundary
  \draw[arr] ($(a1.north)!0.85!(a1.north east)$)
          .. controls +(0.2,0.7) and +(-0.2,0.7) ..
             ($(a2.north)!0.15!(a2.north west)$);
  \node[font=\scriptsize, anchor=south]
       at ({25*\sc + \xo}, 3.85) {IC at $t{=}25M$};

  % --- Cur-3w: [0,17] U [17,34] U [34,50] ---
  \node[font=\small\bfseries, anchor=east] at ({\xo - 0.4}, 0.7) {Cur-3w:};
  \node[win, fill=orange!20, minimum width={17*\sc cm}, anchor=west] (b1)
       at ({0*\sc + \xo}, 0.7) {$[0,17]M$};
  \node[win, fill=blue!15,   minimum width={17*\sc cm}, anchor=west] (b2)
       at ({17*\sc + \xo}, 0.7) {$[17,34]M$};
  \node[win, fill=green!15,  minimum width={16*\sc cm}, anchor=west] (b3)
       at ({34*\sc + \xo}, 0.7) {$[34,50]M$};
  \draw[arr] ($(b1.north)!0.85!(b1.north east)$)
          .. controls +(0.15,0.55) and +(-0.15,0.55) ..
             ($(b2.north)!0.15!(b2.north west)$);
  \draw[arr] ($(b2.north)!0.85!(b2.north east)$)
          .. controls +(0.15,0.55) and +(-0.15,0.55) ..
             ($(b3.north)!0.15!(b3.north west)$);
  \node[font=\scriptsize, anchor=south]
       at ({17*\sc + \xo}, 1.85) {IC};
  \node[font=\scriptsize, anchor=south]
       at ({34*\sc + \xo}, 1.85) {IC};

  % --- Cur-soft: W1 [0,30] (top) and W2 [20,50] (bottom), overlap [20,30] ---
  \node[font=\small\bfseries, anchor=east] at ({\xo - 0.4}, -1.0) {Cur-soft:};
  \node[win, fill=orange!20, minimum width={30*\sc cm}, anchor=west] (c1)
       at ({0*\sc + \xo}, -1.0) {$W_{1}^{\mathrm{ov}}=[0,30]M$};
  \node[win, fill=blue!15, minimum width={30*\sc cm}, anchor=west] (c2)
       at ({20*\sc + \xo}, -2.0) {$W_{2}^{\mathrm{ov}}=[20,50]M$};
  % Highlight overlap band as vertical purple shading spanning both rows
  \fill[purple!25, opacity=0.55]
       ({20*\sc + \xo}, -2.5) rectangle ({30*\sc + \xo}, -0.5);
  \draw[purple!60!black, dashed] ({20*\sc + \xo}, -2.5) -- ({20*\sc + \xo}, -0.5);
  \draw[purple!60!black, dashed] ({30*\sc + \xo}, -2.5) -- ({30*\sc + \xo}, -0.5);
  \node[font=\scriptsize, anchor=south, purple!60!black]
       at ({25*\sc + \xo}, -0.5)
       {overlap $[20,30]M$: anchors\,+\,blend};

  % Time axis (bottom)
  \draw[->, thick] ({0*\sc + \xo}, -3.0)
                -- ({50*\sc + \xo + 0.5}, -3.0)
                node[right]{$t/M$};
  \foreach \t in {0,10,17,20,25,30,34,40,50}{
    \draw ({\t*\sc + \xo}, -3.0) -- ({\t*\sc + \xo}, -3.15);
    \node[tick, below] at ({\t*\sc + \xo}, -3.15) {\t};
  }
\end{tikzpicture}
\caption{Curriculum PINN partitions. Cur-2w (top) splits the
temporal domain at $t = 25\,M$ into two disjoint hard-stitched
windows; the trained network on $W_{1}$ supplies the numerical
initial condition Eq.~\eqref{eq:cur-ic} for $W_{2}$. Cur-3w
(middle) splits at $\{17,34\}\,M$ into three windows with the
same hard-stitch IC transfer between consecutive windows.
Cur-soft (bottom) uses overlapping windows
$W_{1}^{\mathrm{ov}} = [0,30]\,M$ and
$W_{2}^{\mathrm{ov}} = [20,50]\,M$ with a $10\,M$ overlap band
(purple) on which the continuity anchors of
Eq.~\eqref{eq:cur-anchor} are imposed and the smoothstep blend of
Eq.~\eqref{eq:cur-blend} is applied to the final stitched field.}
\label{fig:cur-partition}
\end{figure}

% --------------------------------------------------------------
\subsection{Fourier neural operator surrogate}
\label{sec:fno-method}

The forward, inverse and curriculum PINNs of
Secs.~\ref{sec:fwd-pinn}--\ref{sec:cur-pinn} each train a network
that solves a \emph{single} instance of the Zerilli problem,
parametrised by one mass $M$ and one Gaussian initial pulse
$(x_{0},\sigma)$. A new initial pulse, or a different mass,
requires a new $\mathcal{O}(10^{4})$-iteration L-BFGS run from a
fresh random initialisation. The Fourier neural operator
\citep{Li2021fno,Kovachki2023neuralop}, henceforth FNO, instead
trains a single network once on a population of $1{,}000$
$(M,x_{0},\sigma)$ draws and then predicts $\Phi(x,t)$ on the
full spacetime grid for an unseen draw with one forward pass.
This subsection describes the surrogate as a fourth modality
that produces the same field $\Phi(\xq,t)$ on which the
QNM extractors of Secs.~\ref{sec:qnm-methods}--\ref{sec:plateau}
operate, and the reporting convention of
Sec.~\ref{sec:reporting} applies unchanged. The architecture and
loss pipeline are summarised in Fig.~\ref{fig:fno-arch}.

\begin{figure}[!htbp]
\centering
\resizebox{0.68\textwidth}{!}{%
\begin{tikzpicture}[
  every node/.style={font=\small},
  inp/.style={rectangle, draw, fill=blue!10, rounded corners=2pt,
              minimum height=7mm, minimum width=88mm, inner sep=3pt,
              align=center, font=\small},
  stack/.style={rectangle, draw=none, fill=none,
                inner sep=2pt, align=center, font=\small},
  stackbg/.style={rectangle, draw, fill=blue!5, rounded corners=3pt,
                  inner sep=4pt},
  sconv/.style={rectangle, draw, fill=gray!16, rounded corners=2pt,
                minimum height=9mm, minimum width=96mm, inner sep=3pt,
                align=center, font=\small},
  sconvhdr/.style={rectangle, draw=none, fill=none,
                   inner sep=2pt, align=center},
  sconvbg/.style={rectangle, draw, fill=gray!18, rounded corners=3pt,
                  inner sep=5pt},
  outn/.style={rectangle, draw, fill=orange!22, rounded corners=2pt,
               minimum height=8mm, minimum width=68mm, inner sep=3pt,
               align=center, font=\small},
  loss/.style={rectangle, draw, fill=green!12, rounded corners=2pt,
               minimum height=6mm, minimum width=88mm, inner sep=2pt,
               align=center, font=\footnotesize},
  lossbg/.style={rectangle, draw, fill=green!5, rounded corners=3pt,
                 inner sep=4pt},
  ltot/.style={rectangle, draw, fill=green!28, rounded corners=2pt,
               minimum height=9mm, minimum width=68mm, inner sep=3pt,
               align=center, font=\small},
  arr/.style={->, >=stealth, thick, black!75},
  arrm/.style={->, >=stealth, thin, gray!75},
  arrt/.style={->, >=stealth, thick, gray!55!black, dashed},
]
  % ============ four-channel input stack ============
  \node[stack] (stackhdr) {%
    \textbf{four-channel input on fine grid}\;
    $X\in\mathbb{R}^{4\times N_{t}\times N_{x}}$};
  \node[inp, below=2mm of stackhdr]
        (c0) {ch~0: $\Phi(x,0)$ broadcast in $t$};
  \node[inp, below=1mm of c0]
        (c1) {ch~1: $\partial_{t}\Phi(x,0)$ broadcast in $t$};
  \node[inp, below=1mm of c1]
        (c2) {ch~2: $V_{\ell}(x;M)$ broadcast in $t$};
  \node[inp, below=1mm of c2]
        (c3) {ch~3: $M$ broadcast over $(x,t)$};
  \begin{scope}[on background layer]
    \node[stackbg, fit=(stackhdr)(c0)(c3)] (stackbg) {};
  \end{scope}

  % ============ FNO operator ============
  \node[sconvhdr, below=8mm of stackbg.south] (sconvhdrn) {%
    \begin{minipage}{96mm}
      \centering
      \textbf{neural solution operator $\mathcal{G}_{\theta}$ — four-layer 2-D FNO}\\[1mm]
      hidden channels $h=96$,\;
      Fourier modes $(K_{t},K_{x})=(64,64)$
    \end{minipage}};
  \node[sconv, below=3mm of sconvhdrn]
        (lift)  {lift $1{\times}1$ conv: $4{\to}96$ channels};
  \node[sconv, below=2mm of lift]
        (sb)    {$L=4 \times$ spectral block\\
                 $v^{(\ell+1)}=\mathrm{GELU}\bigl(
                  \mathcal{F}^{-1}[R^{(\ell)}{\odot}\mathcal{F}v^{(\ell)}]
                  + W^{(\ell)}v^{(\ell)}\bigr)$};
  \node[sconv, below=2mm of sb]
        (proj)  {projection $1{\times}1$ conv: $96{\to}1$ channel};
  \draw[arr] (lift) -- (sb);
  \draw[arr] (sb) -- (proj);
  \begin{scope}[on background layer]
    \node[sconvbg, fit=(sconvhdrn)(lift)(proj)] (fnoall) {};
  \end{scope}

  \draw[arr] (stackbg.south) -- (fnoall.north);

  % ============ output field ============
  \node[outn, below=8mm of fnoall.south, fill=orange!45]
        (phi) {$\Phi_{\theta}(x,t)$ — predicted field on $(N_{t}{\times}N_{x})$ fine grid};
  \draw[arr] (fnoall.south) -- (phi.north);

    % ============ six loss terms (training only) ============
    \node[stack, below=8mm of phi] (losshdr) {%
      \textbf{six weighted loss terms}\\[-0.5mm]
      training only};
    \node[loss, below=2mm of losshdr] (lf)
      {$\mathcal{L}_{\mathrm{field}}$ — relative $L^{2}$ on $\Phi$};
    \node[loss, below=1mm of lf] (lh1)
      {$\mathcal{L}_{H^{1}}$ — gradients $(\partial_{t}\Phi,\partial_{x}\Phi)$};
    \node[loss, below=1mm of lh1] (lring)
      {$\mathcal{L}_{\mathrm{ring}}$ — ringdown window $t\ge\tring$};
    \node[loss, below=1mm of lring] (lobs)
      {$\mathcal{L}_{\mathrm{obs}}$ — observer column $x=\xq$};
    \node[loss, below=1mm of lobs] (ltw)
      {$\mathcal{L}_{\mathrm{tw}}$ — time-window ramp $w(t)$};
    \node[loss, below=1mm of ltw] (lpde)
      {$\mathcal{L}_{\mathrm{pde}}$ — FD residual of the wave eq.};
    \begin{scope}[on background layer]
      \node[lossbg, fit=(losshdr)(lf)(lpde)] (lossall) {};
    \end{scope}

    \draw[arrt] (phi.south) -- (lossall.north);

    % aggregator below the loss stack
    \node[ltot, below=8mm of lossall.south]
        (lt) {$\mathcal{L}_{\mathrm{FNO}}
              =\sum_{k} \lambda_{k}\,\mathcal{L}_{k}$
              \quad (Eq.~\eqref{eq:fno-loss})};
    \draw[arrt] (lossall.south) -- (lt.north);
\end{tikzpicture}%
}
\caption{FNO surrogate architecture used in this work. The
four-channel input stack
$(\Phi(x,0), \partial_{t}\Phi(x,0), V_{\ell}(x;M), M)$ is lifted
to $96$ channels by a $1{\times}1$ convolution and passed through
four spectral-convolution blocks, each retaining
$64{\times}64$ Fourier modes along $(x,t)$ and combined with a
$1{\times}1$ skip convolution and a GELU nonlinearity, before a
$1{\times}1$ projection back to the single output channel
$\Phi_{\theta}(x,t)$. The output field feeds six weighted loss
terms summed into $\mathcal{L}_{\mathrm{FNO}}$
(Eq.~\eqref{eq:fno-loss}), of which $\mathcal{L}_{\mathrm{obs}}$
and $\mathcal{L}_{\mathrm{tw}}$ are the QNM-targeted terms
introduced in the v4 configuration of
Sec.~\ref{sec:res-fno}. The total parameter count is
$\sim\!7.8\times 10^{7}$.}
\label{fig:fno-arch}
\end{figure}

\paragraph{Operator and discretisation.} We learn the parametric
solution operator
\begin{equation}
  \mathcal{G}_{\theta}\colon\;
  \bigl(\Phi(\cdot,0),\,\partial_{t}\Phi(\cdot,0),\,
        V_{\ell}(\cdot\,;M),\,M\bigr)
  \;\longmapsto\;
  \Phi(x,t),
  \label{eq:fno-operator}
\end{equation}
discretised on a uniform $N_{x}\times N_{t} = 401\times 401$
grid with $\Delta x = 0.5\,M$ and $\Delta t = 0.25\,M$, covering
$x \in [-50,150]\,M$ and $t \in [0,100]\,M$. The input is a
four-channel rank-3 tensor stacked along the channel axis: the
field initial condition $\Phi(x,0)$, the outgoing initial
velocity $\partial_{t}\Phi(x,0) = -\partial_{x}\Phi(x,0)$, the
Zerilli potential $V_{\ell}(x;M)$ of Eq.~\eqref{eq:V-zerilli}
broadcast in $t$, and the scalar mass $M$ broadcast across the
full $(x,t)$ grid. The output is the single-channel field
$\Phi(x,t)$ on the same grid.

\paragraph{Architecture.} The configuration adopted for the
primary numbers (denoted \texttt{v4} in
Sec.~\ref{sec:res-fno}) is a four-layer 2-D FNO with
$96$ hidden channels, $64$ retained Fourier modes along $t$,
$64$ retained Fourier modes along $x$, GELU activations, a
domain-padding fraction of $0.10$, and grid positional
embeddings, for $\sim\!7.8\times 10^{7}$ trainable parameters.
The discretisation-invariance of the FNO
\citep{Li2021fno,Kovachki2023neuralop} means that the trained
operator can in principle be evaluated on a finer grid than the
training resolution; we exercise this property in
Sec.~\ref{sec:res-fno} on grids ranging from $201^{2}$ to
$801^{2}$ over the same physical box.

\paragraph{Why FNO and not DeepONet?} The other widely used
neural-operator architecture is DeepONet
\citep{Lu2021deeponet}, which factorises the operator into a
branch network acting on the input function sampled at a fixed
set of sensor points and a trunk network acting on the output
query coordinates. For Zerilli ringdown the relevant input is
the full initial-data pair $(\Phi(\cdot,0),\partial_{t}\Phi(\cdot,0))$
plus the potential $V_{\ell}(\cdot;M)$, all of which are
naturally functions of $x$ on a regular grid: a 2-D spectral
operator exploits this structure directly through FFTs and
benefits from the discretisation invariance demonstrated in
Sec.~\ref{sec:res-fno}, whereas a DeepONet would commit at
training time to a fixed sensor grid and would have to learn
the ringdown decay through pointwise trunk evaluations rather
than through global spectral truncation. The two architectures
are comparable in expressive power on benchmark PDE problems
\citep{Kovachki2023neuralop,Lu2021deeponet}; we therefore make
the practical choice of FNO and leave a DeepONet comparison
on the same Zerilli population to future work.

\paragraph{Training distribution.} The training distribution
covers $M \sim \mathrm{Unif}[0.8,\,1.2]$,
$x_{0} \sim \mathrm{Unif}[-10,\,30]\,M$, and
$\sigma \sim \mathrm{Unif}[3,\,8]\,M$. We draw $1{,}000$ training,
$100$ validation and $100$ test triples with seed $1234$; for
each draw the target field is the same RK4 method-of-lines
solver of Appendix~\ref{app:fd} that supplies the FD reference
to the PINNs, run on the same $401\times 401$ grid as the FNO
input--output. The canonical evaluation BH used for the
single-instance diagnostics below is $(M, x_{0}, \sigma) = (1,4,5)$.

\paragraph{Loss.} The training loss combines five terms,
\begin{equation}
  \mathcal{L}_{\mathrm{FNO}}
  = \lambda_{f}\,\mathcal{L}_{\mathrm{field}}
  + \lambda_{H^{1}}\,\mathcal{L}_{H^{1}}
  + \lambda_{\mathrm{ring}}\,\mathcal{L}_{\mathrm{ring}}
  + \lambda_{\mathrm{obs}}\,\mathcal{L}_{\mathrm{obs}}
  + \lambda_{\mathrm{tw}}\,\mathcal{L}_{\mathrm{tw}}
  + \lambda_{\mathrm{pde}}\,\mathcal{L}_{\mathrm{pde}},
  \label{eq:fno-loss}
\end{equation}
with $\mathcal{L}_{\mathrm{field}}$ the relative $L^{2}$ field
error, $\mathcal{L}_{H^{1}}$ the analogous $H^{1}$ seminorm
penalising mis-resolved spatial and temporal derivatives,
$\mathcal{L}_{\mathrm{ring}}$ the field $L^{2}$ restricted to
the ringdown window $t \in [\tring,\,t_{\max}]$ with
$\tring = 18\,M$, $\mathcal{L}_{\mathrm{obs}}$ the 1-D
$L^{2}$ along the observer slice $x = \xq = 2\,M$ that the QNM
extractors actually read, $\mathcal{L}_{\mathrm{tw}}$ the
field $L^{2}$ weighted by a smooth ramp
$w(t) = 1 + \beta\,\mathrm{ramp}(t;\,t_{a},t_{b})$ that grows
from $1$ at $t \le t_{a} = 10\,M$ to $1 + \beta$ at
$t \ge t_{b} = 15\,M$ with $\beta = 50$, and
$\mathcal{L}_{\mathrm{pde}}$ a soft Zerilli residual evaluated
by central finite differences on the output grid. The weights
$(\lambda_{f},\lambda_{H^{1}},\lambda_{\mathrm{ring}},
  \lambda_{\mathrm{obs}},\lambda_{\mathrm{tw}},
  \lambda_{\mathrm{pde}})
  = (1,\,0.20,\,10,\,5,\,1,\,0.05)$
are the v4 values; the prior configurations differ from this
choice by one or two terms as set out in
Table~\ref{tab:fno-versions} of Sec.~\ref{sec:res-fno}. The
observer slice loss $\mathcal{L}_{\mathrm{obs}}$ and the
time-weighted loss $\mathcal{L}_{\mathrm{tw}}$ are the two
terms introduced in v4 specifically to target QNM extraction
fidelity; the empirical effect of their addition is reported in
the ablation of Sec.~\ref{sec:res-fno}.

\paragraph{Optimiser.} Training uses Adam with learning rate
$10^{-3}$, weight decay $10^{-6}$, batch size $4$,
cosine-annealed for $300$ epochs, with gradient clipping at $1.0$
and \texttt{float32} precision. The Adam-only schedule is
standard for FNO training
\citep{Li2021fno,Kovachki2023neuralop}; we do not follow with
an L-BFGS phase because the memory and wall-clock cost of
L-BFGS on $\sim\!7.8\times 10^{7}$ parameters is prohibitive
on a single A100 (per-iteration activation requirement scales
as the chunk size times the full spatio-temporal grid).

\paragraph{Observer location.} All FNO numbers in
Sec.~\ref{sec:res-fno} are reported at $\xq = 2\,M$, in
contrast to the $\xq = 10\,M$ adopted throughout the PINN
results of Secs.~\ref{sec:res-fwd}--\ref{sec:res-inv}. The
choice is forced by the joint window $t \in [10,100]\,M$ used
for FNO QNM extraction. At $\xq = 10\,M$ the ringdown amplitude
$\propto e^{-(t-t_{\mathrm{peak}})/\tau_{0}}$ is below the
field-error floor of both the FNO surrogate and the FD
reference itself for most of the window, so the M4 plateau scan
of Sec.~\ref{sec:plateau} returns no plateau for the FD ground
truth as well as for the FNO. We demonstrate this shared
failure mode in Sec.~\ref{sec:res-fno} as part of the
$\xq$-sweep diagnostic. At $\xq = 2\,M$ the same M4 scan
returns a clean plateau for the FD reference and an
adjacent plateau for the FNO, and the two damping-time
estimates lie within the FD reference's own fit-window
systematic.

% --------------------------------------------------------------
\subsection{Hybrid coarse-FD + neural-residual surrogate}
\label{sec:hyb-method}

The FNO surrogate of Sec.~\ref{sec:fno-method} predicts
$\Phi(x,t)$ end-to-end from initial data and so commits the
network to learning the entire wave-propagation operator. An
alternative is to keep a cheap classical solver as the workhorse
and ask the network only for the part the classical solver
cannot resolve. The hybrid surrogate described here runs a
single inexpensive coarse-grid FD solve, upsamples it to the
fine grid, and trains a 2-D FNO to predict the pointwise
discretisation residual that a label-free Richardson
extrapolation of two coarse solves estimates against that
upsample. The network is never shown the fine-grid FD field in
its loss: its training target is built entirely from coarse
solves, so the surrogate is self-supervised in the sense that it
requires no fine-grid ground truth to train. A parameter-free
gradient gate confines the learned correction to the
under-resolved wavefronts, where the coarse prior carries its
dispersion error, and hands the smooth interior and the causal
exterior back to the prior unchanged. The neural component
therefore sees only the truncation error of the classical
solver, which is smaller and structurally simpler than the field
itself; inference cost is dominated by the single coarse-grid FD
solve plus one forward pass on the fine grid. The same post-hoc
QNM extractors and reporting convention of
Secs.~\ref{sec:qnm-methods}--\ref{sec:reporting} apply unchanged
to the hybrid prediction. The full pipeline is summarised in
Fig.~\ref{fig:hyb-arch}.

\begin{figure}[!htbp]
\centering
\resizebox{0.78\textwidth}{!}{%
\begin{tikzpicture}[
  every node/.style={font=\small},
  ic/.style={rectangle, draw, fill=yellow!18, rounded corners=2pt,
             minimum height=8mm, minimum width=42mm, inner sep=3pt,
             align=center},
  fd/.style={rectangle, draw, fill=red!12, rounded corners=2pt,
             minimum height=11mm, minimum width=58mm, inner sep=3pt,
             align=center},
  up/.style={rectangle, draw, fill=red!6, rounded corners=2pt,
             minimum height=10mm, minimum width=58mm, inner sep=3pt,
             align=center},
  inp/.style={rectangle, draw, fill=blue!10, rounded corners=2pt,
              minimum height=7mm, minimum width=88mm, inner sep=3pt,
              align=center, font=\small},
  stack/.style={rectangle, draw=none, fill=none,
                inner sep=2pt, align=center, font=\small},
  stackbg/.style={rectangle, draw, fill=blue!5, rounded corners=3pt,
                  inner sep=4pt},
  fnobox/.style={rectangle, draw, fill=gray!12, rounded corners=2pt,
                 minimum height=14mm, minimum width=58mm, inner sep=3pt,
                 align=center},
  fnoblock/.style={rectangle, draw=none, fill=none,
                   inner sep=2pt, align=center},
  fnobg/.style={rectangle, draw, fill=gray!18, rounded corners=3pt,
                inner sep=5pt},
  outn/.style={rectangle, draw, fill=orange!25, rounded corners=2pt,
               minimum height=10mm, minimum width=58mm, inner sep=3pt,
               align=center},
  sum/.style={circle, draw, fill=green!20, inner sep=0pt,
              minimum size=9mm, font=\large},
  loss/.style={rectangle, draw, fill=green!12, rounded corners=2pt,
               minimum height=10mm, minimum width=68mm, inner sep=3pt,
               align=center},
  arr/.style={->, >=stealth, thick, black},
  arrs/.style={->, >=stealth, thick, blue!55!black, dashed},
  arrt/.style={->, >=stealth, thick, gray!55!black, dashed},
  sec/.style={font=\footnotesize\itshape, gray!55!black, anchor=west},
]
  % ============ classical solver path ============
  \node[ic]   (icbox) {initial data $(M,\,x_{0},\,\sigma)$};
  \node[fd, below=4mm of icbox]
        (cfd)   {coarse RK4 method-of-lines, 2nd-order stencil\\
                 $(\Delta x,\Delta t)_{\mathrm{c}}=(0.8,0.4)\,M$ \\
                 grid $251\times 126$ ($k=4$)};
  \node[up, below=4mm of cfd]
        (upbox) {quintic-spline upsample $\mathcal{U}$ in $(t,x)$\\
                 (Eq.~\eqref{eq:hyb-upsample}, fine grid $1001\times 501$)};

  \draw[arr] (icbox) -- (cfd);
  \draw[arr] (cfd) -- (upbox);

  % ============ five-channel input stack ============
  \node[stack, below=6mm of upbox] (stackhdr) {%
    \textbf{five-channel input on fine grid}\;
    $X\in\mathbb{R}^{5\times N_{t}\times N_{x}}$};
  \node[inp, below=2mm of stackhdr]
        (c0) {ch~0: $\mathcal{U}\Phi^{\mathrm{c}}(x,t)$ — quintic-upsampled $k{=}4$ prior};
  \node[inp, below=1mm of c0]
        (c1) {ch~1: $\Phi^{\mathrm{c}}(x,0)$ broadcast in $t$};
  \node[inp, below=1mm of c1]
        (c2) {ch~2: $V_{\ell}(x;M)$ broadcast in $t$};
  \node[inp, below=1mm of c2]
        (c3) {ch~3: $M$ broadcast over $(x,t)$};
  \node[inp, below=1mm of c3]
        (c4) {ch~4: $\Pi^{\mathrm{c}}_{0}(x)$ broadcast in $t$
              (Eq.~\eqref{eq:hyb-pi0})};
  \begin{scope}[on background layer]
    \node[stackbg, fit=(stackhdr)(c0)(c4)] (stackbg) {};
  \end{scope}

  \draw[arr] (upbox) -- (stackbg.north);

  % ============ FNO residual operator ============
  \node[fnoblock, below=8mm of stackbg.south] (fnohdr) {%
    \begin{minipage}{96mm}
      \centering
      \textbf{neural residual operator $\mathcal{H}_{\theta}$ — four-layer 2-D FNO}\\[1mm]
      hidden channels $h=32$,\;
      Fourier modes $(K_{t},K_{x})=(16,32)$,\;
      domain padding $0.10$
    \end{minipage}};

  \node[fnobox, below=3mm of fnohdr]
        (lift)  {lift $1{\times}1$ conv: $5{\to}32$ channels};
  \node[fnobox, below=2mm of lift]
        (sb)    {$L=4 \times$ spectral block\\
                 $v^{(\ell+1)}=\mathrm{GELU}\bigl(
                  \mathcal{F}^{-1}[R^{(\ell)}{\odot}\mathcal{F}v^{(\ell)}]
                  + W^{(\ell)}v^{(\ell)}\bigr)$};
  \node[fnobox, below=2mm of sb]
        (proj)  {projection $1{\times}1$ conv: $32{\to}1$ channel};

  \draw[arr] (lift) -- (sb);
  \draw[arr] (sb) -- (proj);
  \begin{scope}[on background layer]
    \node[fnobg, fit=(fnohdr)(lift)(proj)] (fnoall) {};
  \end{scope}

  \draw[arr] (stackbg.south) -- (fnoall.north);

  % ============ output residual + reconstruction ============
  \node[outn, below=8mm of fnoall.south]
        (delta) {$g\,\delta\Phi_{\theta}(x,t)$ — gated residual\\
                 $g=|\nabla\mathcal{U}\Phi^{\mathrm{c}}|/
                 (|\nabla\mathcal{U}\Phi^{\mathrm{c}}|+s),\;s{=}10^{-3}$\\
                 (Eq.~\eqref{eq:hyb-gate})};
  \draw[arr] (fnoall.south) -- (delta.north);

  % sum node and Phi_hybrid
  \node[sum, below=8mm of delta] (sumn) {$+$};
  \node[outn, fill=orange!45, below=4mm of sumn]
        (phih)  {$\Phi^{\mathrm{hyb}}(x,t)
                 =\mathcal{U}\Phi^{\mathrm{c}}+g\,\delta\Phi_{\theta}$\\
                 (Eq.~\eqref{eq:hyb-reconstruct})};
  \draw[arr] (delta) -- (sumn);
  \draw[arr] (sumn) -- (phih);

  % skip connection from upsample (long curve down the left)
  \draw[arrs] (upbox.west) -- ++(-3.6,0) |-
              node[pos=0.10, anchor=south, rotate=90, font=\scriptsize, blue!55!black, yshift=2pt]
                   {skip: $\mathcal{U}\Phi^{\mathrm{c}}$ (physics prior)}
              (sumn.west);

  % training-only target + loss (right side, dashed)
  \node[outn, fill=red!18, right=22mm of delta]
        (target){label-free Richardson target\\
                 $\delta\Phi_{\mathrm{R}}=\Phi^{\mathrm{R}}-\mathcal{U}\Phi^{\mathrm{c}}$\\
                 (Eqs.~\eqref{eq:hyb-richardson},\eqref{eq:hyb-residual})};
  \node[loss, below=4mm of target]
        (lhyb)  {$\mathcal{L}_{\mathrm{hyb}}
                 =\mathrm{MSE}\bigl(g\,\delta\Phi_{\theta},\,\delta\Phi_{\mathrm{R}}\bigr)$\\
                 (Eq.~\eqref{eq:hyb-loss})};
  \draw[arrt] (delta.east) --
              node[pos=0.35, above, font=\scriptsize, gray!55!black] {training only}
              (target.west);
  \draw[arr]  (target) -- (lhyb);

  % gradient backflow from loss to FNO (training only)
  \draw[arrt, gray!65!black] (lhyb.east) -- ++(0.5,0) |-
              node[pos=0.25, anchor=south, rotate=90, font=\scriptsize, gray!55!black, yshift=2pt]
                   {gradient $\nabla_{\theta}\mathcal{L}_{\mathrm{hyb}}$}
              (fnoall.east);
\end{tikzpicture}
}
\caption{Hybrid surrogate pipeline of Sec.~\ref{sec:hyb-method},
shown as a top-to-bottom flow. The classical RK4
method-of-lines solver of Appendix~\ref{app:fd} produces the
coarse field $\Phi^{\mathrm{c}}$ on
$(\Delta x,\Delta t)_{\mathrm{c}} = (0.8,0.4)\,M$ ($k=4$), which
is quintic-spline upsampled in $(t,x)$ onto the fine grid by the
operator $\mathcal{U}$ of Eq.~\eqref{eq:hyb-upsample}. The
five-channel input stack combines this prior
$\mathcal{U}\Phi^{\mathrm{c}}$ with the initial displacement
$\Phi^{\mathrm{c}}(x,0)$, initial velocity
$\Pi^{\mathrm{c}}_{0}(x)$ (Eq.~\eqref{eq:hyb-pi0}), Zerilli
potential $V_{\ell}(x;M)$ and broadcast mass $M$. A four-layer
2-D FNO $\mathcal{H}_{\theta}$ with hidden channel count
$h = 32$ and retained Fourier modes $(K_{t},K_{x}) = (16,32)$
predicts an additive correction, which is multiplied by the
parameter-free gradient gate
$g=|\nabla\mathcal{U}\Phi^{\mathrm{c}}|/
(|\nabla\mathcal{U}\Phi^{\mathrm{c}}|+s)$
(Eq.~\eqref{eq:hyb-gate}, $s = 10^{-3}$, a deterministic
function of the prior) before being added back; the hybrid
prediction Eq.~\eqref{eq:hyb-reconstruct} adds the
quintic-upsampled coarse field as a hard skip (blue dashed),
and the gate confines the correction to the under-resolved
wavefronts. The dashed grey path on the right depicts the
training-only, label-free Richardson target
$\delta\Phi_{\mathrm{R}} = \Phi^{\mathrm{R}} -
\mathcal{U}\Phi^{\mathrm{c}}$
(Eqs.~\eqref{eq:hyb-richardson},\eqref{eq:hyb-residual}), built
from $k = 4$ and $k = 2$ coarse solves alone, and the MSE loss
Eq.~\eqref{eq:hyb-loss} against which $\mathcal{H}_{\theta}$ is
fitted; at inference time the right-hand path is detached and
only the single $k = 4$ coarse solve and the left-hand
reconstruction path are evaluated. Total trainable parameters:
$1.13\times 10^{6}$.}
\label{fig:hyb-arch}
\end{figure}

\paragraph{Discretisation and grids.} The fine grid is
$(\Delta x, \Delta t)_{\mathrm{f}} = (0.2, 0.1)\,M$ on
$x \in [-50, 150]\,M$ and $t \in [0, 50]\,M$, giving
$(N_{x}, N_{t})_{\mathrm{f}} = (1001, 501)$. The coarse grid
adopts a single coarsening factor $k$ along both axes,
$(\Delta x, \Delta t)_{\mathrm{c}} = (k\Delta x, k\Delta
t)_{\mathrm{f}}$, on the same physical box. The deployment prior
uses $k = 4$, so $(\Delta x, \Delta t)_{\mathrm{c}} = (0.8,
0.4)\,M$ and $(N_{x}, N_{t})_{\mathrm{c}} = (251, 126)$; the
label-construction solve described below additionally uses
$k = 2$ ($(N_{x}, N_{t}) = (501, 251)$). All grids advance in
time with the RK4 method-of-lines scheme of
Appendix~\ref{app:fd} and use the same default second-order
centred difference for $\partial_x^2$ on both the coarse and the
fine grids; the coarse prior is deliberately under-resolved
rather than carrying a higher-order stencil, and the
leading-order error it incurs is removed instead by the
Richardson extrapolation of the next paragraph. Quadrupling
$\Delta x$ and $\Delta t$ together reduces FD work per draw by a
factor of $k^{3} = 64$ (volume of $(x, t)$ samples times the
$1/\Delta t$ step count), so the cost of the classical component
of one deployed hybrid prediction is $\tfrac{1}{64}$ of the fine
FD solve; the FNO forward pass adds a fixed
$\mathcal{O}(N_{x} N_{t} \log N_{x} N_{t})$ cost that is small
compared to RK4-MOL on the fine grid and is quantified
empirically in Sec.~\ref{sec:res-hyb}.

\paragraph{Label-free Richardson target.} The coarsening factor
$k$ sets how many fine-grid points the cheap prior discards; at
$k = 4$ the coarse solve is deliberately under-resolved, and its
default second-order centred stencil leaves a large
$\mathcal{O}(\Delta x_{\mathrm{c}}^{2})$ dispersion error --- the
upsampled coarse field misses the fine reference by a relative
$L^{2}$ error of $6.1\%$ on the canonical draw
(Sec.~\ref{sec:res-hyb}). Rather than invest in a more accurate
stencil, we keep the cheap second-order prior and construct the
training target by Richardson extrapolation, which removes the
leading discretisation error using only coarse solves and so
needs no fine-grid ground truth. We run the same problem at two
coarsenings, $k = 4$ and $k = 2$, upsample both to the fine grid
with the operator $\mathcal{U}$ of Eq.~\eqref{eq:hyb-upsample},
and form
\begin{equation}
  \Phi^{\mathrm{R}}
  \;=\; \frac{4\,\mathcal{U}\Phi^{\mathrm{c}}_{2}
              - \mathcal{U}\Phi^{\mathrm{c}}}{3},
  \label{eq:hyb-richardson}
\end{equation}
the standard $p = 2$ Richardson combination, in which the
$\mathcal{O}(\Delta x_{\mathrm{c}}^{2})$ terms of the two solves
cancel and $\Phi^{\mathrm{R}}$ is accurate to
$\mathcal{O}(\Delta x_{\mathrm{c}}^{4})$. Here
$\mathcal{U}\Phi^{\mathrm{c}}$ is the quintic upsample of the
$k = 4$ prior --- the deployment prior and channel~0 of the
input stack --- and $\mathcal{U}\Phi^{\mathrm{c}}_{2}$ is the
quintic upsample of the $k = 2$ solve, used only to build the
label. The network is trained to predict the
Richardson-estimated residual
$\delta\Phi_{\mathrm{R}} = \Phi^{\mathrm{R}} -
\mathcal{U}\Phi^{\mathrm{c}}$, so its loss never sees the
fine-grid FD field: the entire supervision signal is built from
coarse solves. At inference only the single $k = 4$ solve is
required --- the $k = 2$ solve and $\Phi^{\mathrm{R}}$ are
label-construction artefacts of the training set, not inputs to
the deployed surrogate --- so the classical component of one
hybrid prediction costs $k^{-3} = 1/64$ of a fine FD solve. This
weak-prior-plus-Richardson design is the opposite of investing
in a strong classical stencil: it keeps the cheapest possible
solver and asks the network to do correspondingly more, which is
the regime in which the $k^{d+1}$ work saving of a coarse prior
is largest and which is the operative lever for scaling the
construction to higher-dimensional problems.

\paragraph{Discretisation-error decomposition.} For a smooth
solution $\Phi(x, t)$ of the Zerilli equation, the fine-grid
RK4-MOL solution satisfies
$\Phi^{\mathrm{f}} = \Phi + \mathcal{O}(\Delta x_{\mathrm{f}}^{2},
\Delta t_{\mathrm{f}}^{4})$ and is taken as the ground truth at
this resolution, used only to \emph{evaluate} the surrogate. The
$k = 4$ coarse solve carries a second-order error,
$\Phi^{\mathrm{c}} = \Phi + \mathcal{O}(\Delta x_{\mathrm{c}}^{2})$,
whose leading term the Richardson combination
Eq.~\eqref{eq:hyb-richardson} cancels, leaving
$\Phi^{\mathrm{R}} = \Phi + \mathcal{O}(\Delta x_{\mathrm{c}}^{4})$
up to the quintic-interpolation error of the upsample. The
quantity the network learns,
\begin{equation}
  \delta\Phi_{\mathrm{R}}(x, t)
  \;=\; \Phi^{\mathrm{R}}(x, t) - \mathcal{U}\Phi^{\mathrm{c}}(x, t),
  \label{eq:hyb-residual}
\end{equation}
is therefore a label-free estimate of the coarse-prior
discretisation error, agreeing with the true error
$\Phi^{\mathrm{f}} - \mathcal{U}\Phi^{\mathrm{c}}$ to
$\mathcal{O}(\Delta x_{\mathrm{c}}^{4})$; both vanish as
$k \to 1$. The target is dominated by the coarse spatial
truncation: it is largest on the under-resolved wavefronts,
where the second-order stencil dephases the high-wavenumber
content of the prompt burst, and is near zero in the smooth
interior and the causal exterior where the coarse solve is
already accurate. This strong spatial localisation of the target
is exactly what the gradient gate of Eq.~\eqref{eq:hyb-gate}
exploits. Against a typical $|\Phi^{\mathrm{f}}|_{\max} \approx 1$
the upsampled $k = 4$ prior already carries a $6.1\%$ relative
$L^{2}$ error (Sec.~\ref{sec:res-hyb}), so unlike the end-to-end
FNO of Sec.~\ref{sec:fno-method} --- which must synthesise the
full field --- the network here only repairs this bounded,
spatially-localised residual.

\paragraph{Upsample operator.} The operator $\mathcal{U}$ used
in Eqs.~\eqref{eq:hyb-richardson}--\eqref{eq:hyb-residual} and as
the channel-0 prior is a separable degree-five (quintic)
tensor-product spline on the rectangular grid:
\begin{equation}
  (\mathcal{U}\Phi^{\mathrm{c}})(x, t)
  \;=\; \sum_{i, j} c_{ij}\,
        B_{5}\!\Bigl(\tfrac{t - t_{i}}{\Delta t_{\mathrm{c}}}\Bigr)\,
        B_{5}\!\Bigl(\tfrac{x - x_{j}}{\Delta x_{\mathrm{c}}}\Bigr),
  \label{eq:hyb-upsample}
\end{equation}
with $B_{5}$ the quintic B-spline basis and coefficients
$\{c_{ij}\}$ fixed by collocation at the coarse-grid nodes
$\{(t_{i}, x_{j})\}$. The quintic spline is preferred to a cubic
because its $\mathcal{O}(\Delta x_{\mathrm{c}}^{6})$
interpolation error stays comfortably below the
$\mathcal{O}(\Delta x_{\mathrm{c}}^{4})$ Richardson signal, so
the upsample does not contaminate the extrapolated target. The
same operator is applied to both the $k = 4$ and $k = 2$ coarse
solves in Eq.~\eqref{eq:hyb-richardson}. Out-of-domain queries
are clamped to zero amplitude. The upsample is performed once
per sample during dataset assembly at $\mathcal{O}(N_{x} N_{t})$
cost.

\paragraph{Input stack.} The network input is a tensor
$X \in \mathbb{R}^{5\times N_{t}\times N_{x}}$ whose five
channels are listed in Fig.~\ref{fig:hyb-arch} and described
here in full. Channel~0 is the quintic-upsampled $k = 4$ coarse
field $\mathcal{U}\Phi^{\mathrm{c}}(x, t)$ --- the deployment
prior --- which carries the bulk of the dynamical content the
network is asked to repair.
Channel~1 is the initial-displacement profile
$\Phi^{\mathrm{c}}(x, 0)$ broadcast across $t$, giving the
network unambiguous access to the unperturbed initial datum
without having to extract it from the noisy upsampled prior at
$t = 0$. Channel~2 is the per-sample Zerilli potential
$V_{\ell}(x; M)$ of Eq.~\eqref{eq:V-zerilli} broadcast across
$t$, which encodes the BH mass dependence in a form the
spectral layers can exploit through their $x$-convolution.
Channel~3 is the scalar mass $M$ broadcast across the full
$(x, t)$ grid, redundant with the potential channel but
inexpensive and empirically helpful for the
spline-upsample-conditioned residual at the prompt-burst edge.
Channel~4 is the initial velocity
\begin{equation}
  \Pi^{\mathrm{c}}_{0}(x)
  \;=\; \frac{\Phi^{\mathrm{c}}(x, \Delta t_{\mathrm{c}}) -
              \Phi^{\mathrm{c}}(x, 0)}{\Delta t_{\mathrm{c}}}
  \label{eq:hyb-pi0}
\end{equation}
computed by a forward Euler differencing of the coarse field at
$t = 0$ and then linearly resampled from the coarse $x$-grid
onto the fine $x$-grid, broadcast across $t$. The first-order
truncation of Eq.~\eqref{eq:hyb-pi0} is acceptable here because
the channel is auxiliary: it informs the network of the prompt
velocity sign and amplitude in a form that does not require
recovery from the upsampled prior.

\paragraph{Residual operator, gradient gate, and reconstruction.}
The neural residual operator
\begin{equation}
  \mathcal{H}_{\theta}\colon\;
  X = \bigl[\mathcal{U}\Phi^{\mathrm{c}},\,\Phi^{\mathrm{c}}(\cdot, 0),\,
            V_{\ell}(\cdot; M),\,M,\,\Pi^{\mathrm{c}}_{0}\bigr]
  \;\longmapsto\;
  \delta\Phi_{\theta}(x, t)
  \label{eq:hyb-operator}
\end{equation}
is modulated by a parameter-free gradient gate before it is
added to the prior. The gate
\begin{equation}
  g(x, t)
  \;=\; \frac{|\nabla\mathcal{U}\Phi^{\mathrm{c}}(x, t)|}
             {|\nabla\mathcal{U}\Phi^{\mathrm{c}}(x, t)| + s},
  \qquad
  |\nabla\mathcal{U}\Phi^{\mathrm{c}}|
  = \sqrt{(\partial_{t}\mathcal{U}\Phi^{\mathrm{c}})^{2}
          + (\partial_{x}\mathcal{U}\Phi^{\mathrm{c}})^{2}},
  \label{eq:hyb-gate}
\end{equation}
is a deterministic function of the prior alone --- it adds no
trainable parameters and needs no extra data at inference ---
with a single scale hyperparameter $s = 10^{-3}$, whose
principled, label-free selection from the prior gradient
distribution is derived in App.~\ref{app:gate-scale}. It rises
to $g \to 1$ on the
high-gradient wavefronts, where the coarse prior carries its
dispersion error, and falls to $g \to 0$ in the smooth interior
and the causal exterior, where the prior is already accurate and
$|\nabla\mathcal{U}\Phi^{\mathrm{c}}| \ll s$. The hybrid
prediction is the gated additive skip
\begin{equation}
  \Phi^{\mathrm{hyb}}(x, t)
  \;=\; \mathcal{U}\Phi^{\mathrm{c}}(x, t)
         + g(x, t)\,\mathcal{H}_{\theta}(X)(x, t),
  \label{eq:hyb-reconstruct}
\end{equation}
so the network correction is confined by construction to the
under-resolved regions and cannot inject error into the parts of
the domain the coarse solver already resolves. The gate is also
the prior$\leftrightarrow$network stitch: $g$ is a smooth convex
blend between the pure coarse prior ($g = 0$) and the
fully-corrected field ($g = 1$). With $\mathcal{H}_{\theta}
\equiv 0$, or wherever $g = 0$, the hybrid reduces to the
coarse-up baseline. This is the key structural difference from
the end-to-end FNO of Sec.~\ref{sec:fno-method}: the network is
in series with a classical solver that already produces a good
guess, and so inherits the stability and
discretisation-invariance of the underlying numerical scheme as
a hard prior.

\paragraph{FNO architecture.} The residual operator
$\mathcal{H}_{\theta}$ is a 2-D Fourier neural operator
\citep{Li2021fno,Kovachki2023neuralop} on the fine
$(t, x)$ grid. A $1\times 1$ \emph{lift} convolution maps the
$5$-channel input to $h = 32$ hidden channels, and a $1\times 1$
\emph{projection} maps back to the single output channel. In
between sit $L = 4$ Fourier layers, each of the form
\begin{equation}
  v^{(\ell+1)}(x, t)
  \;=\; \mathrm{GELU}\!\Bigl(
         \mathcal{F}^{-1}\bigl[R^{(\ell)} \odot
         \mathcal{F}\,v^{(\ell)}\bigr](x, t)
         \;+\; W^{(\ell)}\,v^{(\ell)}(x, t)
       \Bigr),
  \label{eq:hyb-fno-layer}
\end{equation}
where $\mathcal{F}$ is the 2-D discrete Fourier transform along
$(t, x)$, $R^{(\ell)} \in \mathbb{C}^{h\times h\times K_{t}\times
K_{x}}$ is a complex spectral-weight tensor truncated to the
lowest $K_{t}$ frequencies along $t$ and the lowest $K_{x}$
along $x$, $\odot$ is mode-by-mode matrix multiplication
across the channel axis, and $W^{(\ell)}$ is a pointwise
$1\times 1$ convolution acting as a local residual path. We use
$(K_{t}, K_{x}) = (16, 32)$, which matches the natural mode
ratio of the discrete grid ($N_{x} = 2 N_{t}$) and represents
all spatial frequencies below $k_{x,\max} = 2\pi \cdot 32 / 200
\approx 1.0\,M^{-1}$ on the fine grid. We pre-pad each axis by
the fraction $p = 0.10$ before $\mathcal{F}$ and crop the
output back after $\mathcal{F}^{-1}$, which damps the periodic
wraparound that would otherwise pollute the spectral
convolution near the domain boundaries. Positional grid
encodings $(x, t)$ are concatenated to the lifted features
before the first spectral block to break the translation
equivariance that would otherwise prevent the network from
distinguishing the source region $x \approx x_{0}$ from the
wave zones. The total trainable parameter count is dominated
by the spectral weights, $L \cdot h^{2} \cdot 2 K_{t} \cdot
2 K_{x} = 4\cdot 32^{2}\cdot 32\cdot 64 \approx 8.4\times
10^{6}$ real degrees of freedom before sparsification, with
the actual count of $1.13\times 10^{6}$ reduced by the
\texttt{neuraloperator} library's factorised-tensor
parametrisation of $R^{(\ell)}$
\citep{Kovachki2023neuralop}. The smaller hidden width and total
mode count relative to the end-to-end FNO of
Sec.~\ref{sec:fno-method}
($K_{t} = K_{x} = 64$, $h = 96$, $7.8\times 10^{7}$ parameters)
is afforded by the simpler target $\delta\Phi_{\mathrm{R}}$: the
residual has narrower spectral support than the full field
$\Phi^{\mathrm{f}}$ because the wave-propagation operator
already lives in the upsampled prior $\mathcal{U}\Phi^{\mathrm{c}}$,
so the network needs only to add a low-rank dispersion correction
rather than synthesise an outgoing wave from scratch.

\paragraph{Loss.} The training loss is the gridwise mean
squared error between the gated network output and the
label-free Richardson residual,
\begin{equation}
  \mathcal{L}_{\mathrm{hyb}}(\theta)
  \;=\; \frac{1}{B}\sum_{b=1}^{B}
         \frac{1}{N_{t} N_{x}}
         \sum_{i=1}^{N_{t}}\sum_{j=1}^{N_{x}}
         \bigl[
           g^{(b)}_{ij}\,\mathcal{H}_{\theta}(X^{(b)})_{ij}
           \,-\, \delta\Phi^{(b)}_{\mathrm{R},\,ij}
         \bigr]^{2},
  \label{eq:hyb-loss}
\end{equation}
averaged across the training batch of size $B$. We do not weight
by a physical-units factor, do not include a PDE-residual term,
and do not introduce auxiliary observer-slice or time-weighted
losses of the kind that the end-to-end FNO of
Sec.~\ref{sec:fno-method} requires: the target
$\delta\Phi_{\mathrm{R}}$ is already small, smooth, and
spatially localised, and the gate $g$ of Eq.~\eqref{eq:hyb-gate}
has already confined the correction to the wavefronts, so a
uniform MSE on the gated output is adequate. The empirical
effect of the loss simplicity and of the gate scale $s$ on the
field-error, speckle, and QNM-extraction floors is quantified in
Sec.~\ref{sec:res-hyb}.

\paragraph{Optimiser.} Training is a single phase of Adam with
learning rate $10^{-3}$, weight decay $10^{-6}$, batch size
$B = 8$, gradient clipping at unit norm, and \texttt{float32}
precision, for $N_{\mathrm{adam}} = 100$
epochs over the $n_{\mathrm{train}} = 1{,}000$ training BHs.
The network is small ($1.13\times 10^{6}$ parameters) and the
gated residual target is smooth and spatially localised, so
$100$ Adam epochs reach the validation floor; we found no
measurable improvement from a subsequent L-BFGS phase on this
target and report the Adam-only optimum. The per-epoch
validation history is shown in Sec.~\ref{sec:res-hyb}.

\paragraph{Training distribution.} A Sobol sequence of $1{,}200$
$(M, x_{0}, \sigma)$ triples is drawn from
$M \sim \mathrm{Unif}[0.8, 1.2]$,
$x_{0} \sim \mathrm{Unif}[2, 6]\,M$, and
$\sigma \sim \mathrm{Unif}[3, 7]\,M$, then split
$(n_{\mathrm{train}}, n_{\mathrm{val}}, n_{\mathrm{test}}) =
(1000, 100, 100)$ with seed $1234$. For each triple the
fine-grid FD solution and the $k = 4$ and $k = 2$ coarse-grid FD
solutions are precomputed offline; the quintic upsample
$\mathcal{U}\Phi^{\mathrm{c}}$ of the $k = 4$ prior and the four
auxiliary channels of Fig.~\ref{fig:hyb-arch} are assembled
once and cached to disk, and the label-free Richardson target
$\delta\Phi_{\mathrm{R}} = \Phi^{\mathrm{R}} -
\mathcal{U}\Phi^{\mathrm{c}}$ of
Eqs.~\eqref{eq:hyb-richardson}--\eqref{eq:hyb-residual} is
stored alongside. The fine-grid FD solution is retained only as
an evaluation reference and is never used in the training loss.
The canonical evaluation BH used for the
single-instance diagnostics in Sec.~\ref{sec:res-hyb} is
$(M, x_{0}, \sigma) = (1, 4, 5)$. The training distribution
overlaps but is not identical to the FNO training distribution
of Sec.~\ref{sec:fno-method}: we contract the $(x_{0},
\sigma)$ box to keep the prompt-burst region inside the
$t \in [0, 50]\,M$ window of the hybrid simulation, which is
shorter than the $t \in [0, 100]\,M$ window used by the
end-to-end FNO. This is the only intentional difference in
training distribution between the two surrogates.

\paragraph{Observer location.} Hybrid numbers in
Sec.~\ref{sec:res-hyb} are reported at $\xq = 2\,M$. The choice
matches the FNO surrogate of Sec.~\ref{sec:fno-method} for
direct comparability; the empirical finding from
Sec.~\ref{sec:res-hyb} that the QNM observable is already at
the plateau-extractor precision floor on the coarse-up baseline
at this observer (and not at the hybrid-vs-baseline field-error
crossover) motivates reporting the hybrid's value through the
global field-error reduction discussed there rather than
through the median QNM at $\xq = 2\,M$.

% --------------------------------------------------------------
\subsection{QNM extraction methods}
\label{sec:qnm-methods}

Once a forward or inverse PINN has been trained, the QNM parameters
$(\omega,\tau)$ are extracted post-hoc from the time series
$\Phi(\xq,t)$ at $\xq = 10\,M$. We apply five extraction methods, the
first three of which mirror standard numerical-relativity practice
and the last two of which are the plateau-based methods that
constitute our methodological contribution.

The two plateau-based methods rely on the following operational
definition, formalised and motivated in
Sec.~\ref{sec:plateau} below.

\begin{definition}[fit-window plateau]
\label{def:plateau}
A fit window $[t_0,\,\tend]$ lies inside the QNM regime if and only
if shifting either edge by a small amount produces no significant
change in the recovered $(\omega,\tau)$. Operationally, in M4 and
M5 the plateau is the contiguous block of fixed size
$w = \lfloor\tfrac12 N\rfloor$ along each scan axis that minimises
the combined relative population standard deviation
$\mathrm{std}(\omega)/|\bar\omega|+\mathrm{std}(\tau)/|\bar\tau|$ of
the per-window fits inside the block (population standard deviation,
divisor $N$).
\end{definition}

Method 1 (M1) is FFT and log-line through local maxima.
The frequency $\omega$ is estimated from the location of the largest
peak of a zero-padded, Hann-windowed FFT of $\Phi(\xq,t)$ over the
fit window, with parabolic interpolation around the peak bin. The
damping time $\tau$ is estimated from the slope of
Eq.~\eqref{eq:logline} applied to the local maxima
$\{(t_k, |\Phi(\xq, t_k)|)\}$ of $\Phi$ in the same window; in
practice the slope $-1/\tau$ is obtained by an ordinary
least-squares straight-line fit to $\log|\Phi(\xq, t_k)|$ versus
$t_k$. This is the procedure adopted by
\citet{Patel2024pinn}.

Method 2 (M2) is direct nonlinear least squares.
A single damped sinusoid is fitted to $\Phi(\xq,t)$ on the chosen
window,
\[
  \Phi_{\mathrm{model}}(t) = A\,e^{-t/\tau}\cos(\omega t + \phi),
\]
using a bound-constrained Levenberg--Marquardt solver. M2 is a
four-parameter fit and is the most sensitive of the five methods to
overtone contamination at the early edge of the window.

Method 3 (M3) is ESPRIT subspace decomposition.
ESPRIT \citep{Roy1989esprit} applied with model order $K = 6$ to
the Hilbert-analytic signal is ill-conditioned at the SNR of the
PINN-derived $\Phi(\xq,t)$ in our setup; M3 is reported in
Appendix~\ref{app:esprit} but is not used in the quoted numbers.

Method 4 (M4) is two-mode NLS with start-time plateau scan,
following the Schwarzschild $\ell=m=2$ overtone-aware ringdown
recipe of \citet{Giesler2019overtones}: simultaneously fitting the
fundamental and the first overtone absorbs the
residual $n=1$ power that would otherwise bias a single-mode
template at early times, and we shall see below that this is the
reason M4 attains a fundamental-mode accuracy roughly an order of
magnitude better than the single-mode M2 estimator on the same
data. A two-mode template,
\begin{equation}
  \Phi_{\mathrm{model}}(t) =
  A_0\,e^{-t/\tau_0}\cos(\omega_0 t + \phi_0)
  + A_1\,e^{-t/\tau_1}\cos(\omega_1 t + \phi_1),
  \label{eq:two-mode}
\end{equation}
is fitted by nonlinear least squares over a window
$[t_0,\,t_{\max}]$, with the mode of longer $\tau$ identified as the
fundamental. The fit is repeated for $t_0$ on a uniform grid of $16$
values in $[10,\,25]\,M$ (step $1\,M$), at fixed $\tend = 50\,M$.
We then identify the contiguous block of
eight consecutive start times of lowest combined relative scatter in
$(\omega_0,\tau_0)$ and report the plateau mean and the plateau
population standard deviation
as the M4 estimate of $(\omega,\tau)$ and its
fit-window systematic uncertainty $(\sigma_\omega,\sigma_\tau)$. The
block width $w = 8$ is fixed by the convention
$w = \lfloor\tfrac12 N_{t_0}\rfloor$,
which trades scan
resolution against block statistics.
The procedure is summarised in Algorithm~\ref{alg:m4}.

\begin{algorithm}[t]
\caption{M4 --- 1-D plateau scan for $(\omega_0,\tau_0)$.}
\label{alg:m4}
\begin{algorithmic}[1]
\STATE \textbf{Input:} time series $\Phi(\xq,t)$,
       grid $\{t_0^{(k)}\}_{k=1}^{16} \subset [10,25]\,M$,
       fixed $\tend = 50\,M$, plateau width $w = 8$.
\FOR{$k = 1,\dots,16$}
  \STATE Fit Eq.~\eqref{eq:two-mode} to $\Phi(\xq,t)$ on $[t_0^{(k)},\tend]$
         by NLS.
  \STATE Identify the longer-$\tau$ component as the fundamental,
         giving $(\omega_0^{(k)},\tau_0^{(k)})$.
\ENDFOR
\STATE Find the contiguous index range $k^* : k^*{+}w{-}1$ minimising
       the combined relative standard deviation
       $\mathrm{std}(\omega_0)/\bar\omega_0 + \mathrm{std}(\tau_0)/\bar\tau_0$.
\STATE \textbf{Return} plateau mean
       $(\bar\omega_0,\bar\tau_0)$ and population standard deviation
       $(\sigma_{\omega_0},\sigma_{\tau_0})$ over the plateau block.
\end{algorithmic}
\end{algorithm}

Method 5 (M5) is two-mode NLS with two-dimensional plateau scan.
M5 generalises M4 by sweeping both edges of the fit window
$[t_0,\,\tend]$. We use a $10\times 6$ rectangular grid with
$t_0 \in [10,\,25]\,M$ in steps of $5/3\,M$ and
$\tend \in [30,\,50]\,M$ in steps of $4\,M$.
The plateau is the contiguous $5\times 3$ rectangle of smallest
combined relative scatter, fixed by the same
$\lfloor\tfrac12 N\rfloor$ rule applied independently along the two
axes ($\lfloor10/2\rfloor=5$ in $t_0$, $\lfloor 6/2\rfloor=3$ in
$\tend$). The plateau mean and plateau standard deviation are
reported as for M4. To our knowledge M5 is the first
systematic two-dimensional plateau analysis applied to PINN-derived
QNM extraction. A schematic of the two scans, overlaid on a
representative ringdown waveform, is given in
Fig.~\ref{fig:windows}.

\begin{figure}[t]
\centering
%-------------------------------------------------------------
% (a) M4 1-D start-time scan
%-------------------------------------------------------------
\begin{subfigure}[t]{\textwidth}
\centering
\begin{tikzpicture}[>=stealth, x=0.20cm, y=0.45cm,
                    every node/.style={font=\scriptsize}]
  % --- decorative ringdown waveform (top band) ---
  \begin{scope}[yshift=2.6cm]
    \draw[domain=0:50, samples=240, smooth, thick, blue!55!black]
      plot (\x, {0.9*exp(-0.09*\x)*cos(72*\x)});
    \node[blue!55!black, anchor=west] at (51,0)
         {$\Phi(\xq,t)$};
    % light dashed connector to indicate same time axis
    \foreach \x in {10,18,25,50}
      \draw[gray!40, dashed, very thin] (\x,-1.0) -- (\x,-2.5);
  \end{scope}

  % --- M4 windows: vary t_0 in [10,25], fixed t_end = 50 ---
  % plateau block (t_0 = 10..16) drawn solid
  \foreach \t/\y in {10/0, 12/0.55, 14/1.10, 16/1.65}
    \draw[orange!85!black, very thick] (\t,\y) -- (50,\y);
  % post-plateau windows drawn dotted
  \foreach \t/\y in {18/2.20, 22/2.75}
    \draw[orange!85!black, very thick, dotted] (\t,\y) -- (50,\y);
  \node[orange!85!black, anchor=west] at (51,1.4)
       {M4: scan $t_0$};

  % time axis
  \draw[->, thick] (-1,-0.6) -- (54,-0.6) node[right]{$t/M$};
  \foreach \x in {0,10,18,25,50}
    \draw (\x,-0.5) -- (\x,-0.7) node[below]{$\x$};
\end{tikzpicture}
\caption{M4 schematic: 1-D scan of the start time $t_0$ on
$\{10,12,14,16,18,22\}\,M$ with the end time fixed at $t=50\,M$.
Solid bars belong to the contiguous plateau block; dotted bars sit
outside it. The decorative curve at the top depicts the ringdown
waveform $\Phi(\xq,t)$ on the same time axis.}
\label{fig:m4}
\end{subfigure}

\vspace{1.0em}

%-------------------------------------------------------------
% (b) M5 2-D plateau in (t_0, t_end) plane
%-------------------------------------------------------------
\begin{subfigure}[t]{\textwidth}
\centering
\begin{tikzpicture}[>=stealth, x=0.55cm, y=0.55cm,
                    every node/.style={font=\scriptsize}]
  % axes
  \draw[->, thick] (-0.2,0) -- (10.5,0) node[right]{$t_0/M$};
  \draw[->, thick] (0,-0.2) -- (0,7.5) node[above]{$\tend/M$};
  \foreach \x/\lab in {0/10, 3/16.7, 6/22, 9/25}
    \draw (\x,0.08) -- (\x,-0.08) node[below]{$\lab$};
  \foreach \y/\lab in {0/30, 2.5/35, 5/42, 7/50}
    \draw (0.08,\y) -- (-0.08,\y) node[left]{$\lab$};

  % grid of (t_0,t_end) candidate windows (10x6 dots)
  \foreach \i in {0,...,9}
    \foreach \j in {0,...,5}
      \fill[gray!50] ({\i*0.95+0.1},{\j*1.25+0.2}) circle (1.6pt);

  % plateau rectangle: t_0 in [10,16.7], t_end in [42,50]
  \draw[red!80!black, very thick, fill=red!10, fill opacity=0.5]
        (-0.05,4.7) rectangle (3.1,7.1);
  \node[red!80!black, anchor=south west, font=\small] at (3.2,5.6)
       {plateau};

  % marker for inverse-variant choice t_0=18 (placed below the x-axis
  % to avoid colliding with the t_end/M axis label at the top)
  \draw[->, thick, blue!70!black] (4.85,-1.05) -- (4.85,-0.15);
  \node[blue!70!black, anchor=north, font=\small] at (4.85,-1.1)
       {$t_0{=}18\,M$ (variants A, D, E)};
\end{tikzpicture}
\caption{M5 schematic: 2-D scan in the $(t_0,\tend)$ plane. Grey
dots are candidate fit windows on a $10\times 6$ grid with
$t_0\in[10,25]\,M$, $\tend\in[30,50]\,M$. The shaded red rectangle
(plateau) is the contiguous $5\times 3$ block of lowest combined
relative scatter measured on the forward waveform,
$t_0\in[10,16.7]\,M$ and $\tend\in[42,50]\,M$. The arrow marks the
value $t_0=18\,M$ adopted for inverse variants A, D, E, which sits
just above the plateau's upper $t_0$ edge.}
\label{fig:m5}
\end{subfigure}
\caption{Fit-window scan schematics for the two plateau-based QNM
extraction methods. (a) M4 sweeps the start time only; (b) M5
sweeps both edges, with the plateau identified as a contiguous
rectangle in the $(t_0,\tend)$ plane.}
\label{fig:windows}
\end{figure}

% --------------------------------------------------------------
\subsection{Plateau-based fit-window selection}
\label{sec:plateau}

The damped-sinusoid template
$A\,e^{-t/\tau}\cos(\omega t + \phi)$ is the correct model for
$\Phi(\xq,t)$ only inside the QNM regime, that is, after the prompt
response of the initial pulse has passed and before the late-time
power-law tail becomes comparable to the exponential. Fitting the template to
data outside this regime is \emph{model misspecification}, not a
noise problem, and the resulting parameter biases do not vanish with
more data. The fit window is therefore a hyperparameter of the
extraction, and a robust $(\omega,\tau)$ measurement requires a
reproducible criterion for the window. Definition~\ref{def:plateau}
captures the operational criterion adopted in M4 and M5; inside the
plateau the recovered $(\omega,\tau)$ are stationary in the window
edges, and their variation across the plateau provides an estimate
of the fit-window systematic uncertainty.

For the Schwarzschild $\ell = 2$ fundamental at our parameters the
four relevant timescales are summarised by
\begin{equation}
  t_{\mathrm{burst}} \sim 5\,M, \qquad
  \tau_1 = 3.651\,M, \qquad
  \tau_0 = 11.241\,M, \qquad
  t_{\mathrm{tail}} \gg 50\,M,
  \label{eq:timescales}
\end{equation}
where $t_{\mathrm{burst}}$ is the prompt-burst duration set by the
initial condition Eq.~\eqref{eq:ic}, $\tau_1$ and $\tau_0$ are the
overtone and fundamental decay times of
Eq.~\eqref{eq:qnm-ref}, and $t_{\mathrm{tail}}$ is the onset of the
power-law Price tail $\propto t^{-(2\ell+3)}$
\citep{Berti2009review}. The Price-tail amplitude depends on the
initial data and observer position, but for our setup the QNM
remains the dominant component of $\Phi(\xq,t)$ on the entire
simulation interval $[0,50]\,M$, as confirmed by direct inspection
of the FD reference. The right edge of any fit window inside this
interval is therefore well within the QNM regime, which is borne
out empirically by the M5 plateau rectangles extending in $\tend$
all the way to the domain end (Sec.~\ref{sec:results}).
The plateau procedure differs from the standard practice of quoting a
single number from a single fit window, including that of
\citet{Patel2024pinn}, who do not specify a numerical $t_0$ or
$\tend$ and report no fit-window sensitivity. The closest precedents
in the gravitational-wave ringdown literature are the start-time
stability scans of \citet{Buonanno2007ringdown} and the
$f_{220}$-vs-start-time consistency check used in the
GW150914 ringdown analysis \citep{LIGO2016ringdown}, both of which
sweep $t_0$ at fixed $\tend$, and the Bayesian start-time
marginalisation of \citet{Isi2021priors}, which integrates over the
posterior of $t_0$ instead of selecting a window. The present
approach extends these along two axes. First, we sweep both edges
of the window simultaneously rather than $t_0$ alone (M5,
Sec.~\ref{sec:qnm-methods}). Second, we quote the spread of the
recovered $(\omega,\tau)$ across the empirically identified
plateau as a fit-window systematic uncertainty for $\omega$, in lieu
of the nonlinear-fit covariance which our M2 implementation
discards (see Sec.~\ref{sec:reporting}).
% --------------------------------------------------------------
\subsection{Reporting convention}
\label{sec:reporting}

We quote one $(\omega,\tau)$ per experiment, with $\omega$ from
the M4 plateau mean and $\tau$ from M2 on the wide window
$[10,\,50]\,M$. The
$\sigma_\omega$ quoted alongside $\omega$ is the M4 plateau standard
deviation (Algorithm~\ref{alg:m4}, line~7), an estimate of the
fit-window systematic uncertainty in the sense of
Def.~\ref{def:plateau}. M2 over a single fixed window does not
produce an uncertainty, so the dispersion in $\tau$ across variants
is reported only through the per-window per-method tables of
Appendix~\ref{app:per-window} rather than as a single $\sigma_\tau$
on the primary value.

The lower edge $t = 10\,M$ matches the lower edge $\tring$ of
the baseline ringdown-template window in Eq.~\eqref{eq:loss-ring}
and is used unchanged across all extraction variants.
Empirically the
dominant peak of $|\Phi(\xq,t)|$ on the FD reference falls at
$t \approx 10.8\,M$ (with subsequent local maxima at
$t \approx 18.3, 26.7, 35.2, 43.6\,M$),
so the lower edge $t = 10\,M$ is placed just before the first
ringdown peak. The choice $t = 10\,M$ rather than the empirical peak
location $t \approx 10.8\,M$ is conventional and is preserved here
for exact comparability with the inverse training window.

This convention follows established practice in numerical-relativity
QNM extraction. Quoting different estimators for $\omega$ and
$\tau$ is also done by \citet{Berti2007ringdown}, who use FFT
for $\omega$ and an amplitude-envelope fit for $\tau$, and by
\citet{Patel2024pinn}, who use FFT for $\omega$ and a log-line
through local maxima for $\tau$. The motivation in all cases is
that $\omega$ is best estimated from phase information aggregated
across many cycles, and the M4 plateau is the lowest-variance such
estimator in our setup with $\sigma_\omega \sim 10^{-5}$ for the
converged variants, whereas $\tau$ is best estimated from amplitude
information over the longest available decay range, motivating a
wider M2 window.

The primary $\tau$ is taken from the post-hoc M2 wide-window fit
on $\Phi_\theta(\xq,t)$ rather than from the learned scalar
$\tau_{\mathrm{learned}}$, because of the
$A_{\mathrm{ring}}$--$\tau$ degeneracy of
Eq.~\eqref{eq:loss-ring} introduced in Sec.~\ref{sec:inv-pinn}
and quantified in Sec.~\ref{sec:res-inv}.

For each method and each variant the full per-window per-method
results are tabulated in Appendix~\ref{app:per-window} and the
corresponding plateau plots and 2-D heatmaps are shown in
Appendix~\ref{app:plots}. The summary table in
Sec.~\ref{sec:results} collects only the values selected by the
convention above.
