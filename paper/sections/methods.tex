% =============================================================
% Section 3: Methods
% =============================================================
\section{Methods}\label{sec:methods}

We state the FD reference (Sec.~\ref{sec:fwd-problem}), the
forward (Sec.~\ref{sec:fwd-pinn}) and inverse
(Sec.~\ref{sec:inv-pinn}) PINNs, the two amortised surrogates
(Secs.~\ref{sec:fno-method} and~\ref{sec:hyb-method}), the five
extraction methods M1--M5 (Sec.~\ref{sec:qnm-methods}), the
plateau criterion (Sec.~\ref{sec:plateau}), and the reporting
convention (Sec.~\ref{sec:reporting}). M4 and M5 are the new
estimators. Table~\ref{tab:train-config} collects the training
configuration of the four networks.

\begin{table}[!htbp]
\centering
\caption{Training configuration of the four networks. The two
surrogates train with weight decay $10^{-6}$ and gradient
clipping at unit norm; all learning rates are $10^{-3}$.}
\label{tab:train-config}
\small
\setlength{\tabcolsep}{5pt}
\begin{tabular}{@{}lcccc@{}}
\toprule
 & Forward PINN & Inverse PINN & FNO & Hybrid FNO \\
\midrule
Parameters & $4{,}521$ & $4{,}521 + 5$ & $7.8\times 10^{7}$ & $1.13\times 10^{6}$ \\
Optimiser  & Adam${\to}$L-BFGS & Adam${\to}$L-BFGS & Adam & Adam \\
Budget     & $10^{4} + 3{\times}10^{4}$ it. & $10^{4} + 1.5{\times}10^{4}$ it. & $300$ ep. & $100$ ep. \\
Batch size & full & full & $4$ & $8$ \\
Precision  & \texttt{float64} & \texttt{float64} & \texttt{float32} & \texttt{float32} \\
\bottomrule
\end{tabular}
\end{table}

% --------------------------------------------------------------
\subsection{Schwarzschild Zerilli problem}
\label{sec:fwd-problem}

The forward problem is the Zerilli equation
Eq.~\eqref{eq:zerilli} with the polar potential
$V_{\ell}^{\mathrm{Z}}$ of Eq.~\eqref{eq:V-zerilli} at
$\ell = 2$, $M = 1$, on the numerical domain
$x \in [-50,\,150]\,M$, $t \in [0,\,50]\,M$, with
outgoing-Gaussian initial data
\begin{equation}
  \Phi(x,0) = A_0\,\exp\!\left[-\frac{(x-x_0)^2}{2\sigma_0^2}\right],
  \qquad
  \partial_t \Phi(x,0) = -\partial_x \Phi(x,0),
  \label{eq:ic}
\end{equation}
with $A_0 = 1$, $x_0 = 4\,M$ and $\sigma_0 = 5\,M$. The velocity
condition imposes the conventional purely outgoing pulse
\citep{Berti2009review}, in contrast to the quadratic
$\partial_t \Phi \propto (x-x_0)^2 \Phi$ of
\citet{Patel2024pinn}. At the truncated boundaries we apply
first-order Sommerfeld absorbing conditions in place of the
asymptotic QNM conditions Eq.~\eqref{eq:qnm-bc},
\begin{equation}
  (\partial_t - \partial_x)\Phi\bigl|_{x=x_{\min}} = 0, \qquad
  (\partial_t + \partial_x)\Phi\bigl|_{x=x_{\max}} = 0,
  \label{eq:bc-sommerfeld}
\end{equation}
identical to those used by \citet{Patel2024pinn}. The domain is
sized so that the wavefront, propagating at unit speed from
$x_0 = 4\,M$, never reaches either boundary within
$t \in [0,50]\,M$: the arrival times
\begin{equation}
  t_{R} = x_{\max} - x_0 = 146\,M, \qquad
  t_{L} = x_0 - x_{\min} = 54\,M,
  \label{eq:bc-arrival}
\end{equation}
both exceed the extraction window, so the boundary residuals
act only as soft regularisers in a causally disconnected
region.

The reference solution (Fig.~\ref{fig:waveform-3d}) is a
fourth-order Runge--Kutta method-of-lines finite-difference (FD)
integration of Eq.~\eqref{eq:zerilli} with $\Delta x = 0.2\,M$
and $\Delta t = 0.1\,M$, used throughout as the ground truth for
field accuracy and for cross-checking the extractors on a
non-PINN signal. The Leaver values $M\omega_0 = 0.3737$,
$\tau_0/M = 11.241$ (fundamental) and $M\omega_1 = 0.3467$,
$\tau_1/M = 3.651$ (first overtone) of Eq.~\eqref{eq:qnm-ref}
are used for percentage-error reporting.

\begin{figure}[!htbp]
\centering
\begin{minipage}[t]{0.49\textwidth}
  \centering
  \includegraphics[width=\linewidth]{../outputs/pinn/waveform_3d_full.png}
  \subcaption{Full domain $x \in [-50, 150]\,M$, $t \in [0, 50]\,M$.
  The right-moving free pulse from the outgoing initial condition
  is visible as the diagonal ridge propagating to the right (towards
  infinity); the left-moving ridge towards the horizon is the
  back-scattered component generated by the photon-sphere barrier.}
  \label{fig:waveform-3d-full}
\end{minipage}\hfill
\begin{minipage}[t]{0.49\textwidth}
  \centering
  \includegraphics[width=\linewidth]{../outputs/pinn/waveform_3d_zoom.png}
  \subcaption{Zoom on the QNM region $x \in [-20, 80]\,M$. The
  ringdown is visible as the small-amplitude decaying oscillation
  near $x \simeq 10\,M$ for $t \gtrsim 10\,M$, sandwiched between
  the right-moving free pulse and the back-scattered left-mover.}
  \label{fig:waveform-3d-zoom}
\end{minipage}
\caption{FD reference field $\Phi_{\mathrm{FD}}(x, t)$ for
Schwarzschild $\ell = 2$, generated by the fourth-order
Runge--Kutta method-of-lines integration of
Eq.~\eqref{eq:zerilli} with the initial data of
Eq.~\eqref{eq:ic} and the Sommerfeld boundaries of
Eq.~\eqref{eq:bc-sommerfeld}. All PINN field-accuracy
metrics in Sec.~\ref{sec:results} are computed against this
reference.}
\label{fig:waveform-3d}
\end{figure}

% --------------------------------------------------------------
\subsection{Forward PINN}
\label{sec:fwd-pinn}

We solve the forward problem of Sec.~\ref{sec:fwd-problem} with a
fully-connected PINN
\citep{Raissi2017} implemented in DeepXDE \citep{Lu2021deepxde} with
the PyTorch backend \citep{Paszke2019pytorch}.

Architecture, output transform and weight initialiser are chosen
identically to \citet{Patel2024pinn}, so that differences in
extracted QNMs are attributable to training and extraction
rather than the network itself: hidden layers $[80,40,20,10]$
with $\tanh$ activations, input $(x,t) \in \mathbb{R}^{2}$, and
a single scalar output wrapped in the tanh envelope
$\Phi_\theta(x,t) = A\tanh\bigl(\hat y(x,t)\bigr)$ with
$A = A_0 = 1$, giving $4{,}521$ trainable
parameters.\footnote{$2{\cdot}80 + 80 + 80{\cdot}40 + 40 +
40{\cdot}20 + 20 + 20{\cdot}10 + 10 + 10{\cdot}1 + 1 = 4521$.}
The architecture is summarised in Fig.~\ref{fig:fwd-pinn-arch},
together with the automatic-differentiation pipeline that
produces the residual losses defined below.

\begin{figure}[!htbp]
\centering
\begin{tikzpicture}[
  node distance=10mm and 12mm,
  every node/.style={font=\small},
  inp/.style={circle, draw, fill=blue!10, minimum size=7mm, inner sep=0pt},
  hid/.style={circle, draw, fill=gray!12, minimum size=4mm, inner sep=0pt},
  outn/.style={circle, draw, fill=orange!20, minimum size=7mm, inner sep=0pt},
  loss/.style={rectangle, draw, fill=green!10, rounded corners=2pt,
               minimum height=6mm, minimum width=18mm, inner sep=2pt,
               align=center, font=\footnotesize},
  arr/.style={->, >=stealth, thin, gray!70},
]
  % Inputs
  \node[inp] (x) at (0, 0) {$x$};
  \node[inp] (t) at (0, -1.2) {$t$};

  % Layer geometry: visually decreasing column heights to reflect 80, 40, 20, 10.
  % Layer 1: 8 nodes, Layer 2: 4 nodes, Layer 3: 2 nodes, Layer 4: 1 node.
  % Use vertical spacing 0.45 cm between nodes; centre each column at y=-0.6.
  \def\xL{2.0}
  \def\xLii{4.0}
  \def\xLiii{6.0}
  \def\xLiv{8.0}

  % Layer 1: 8 nodes, ys from +1.6 to -2.8 (step 0.55)
  \foreach \k/\y in {1/1.6, 2/1.05, 3/0.5, 4/-0.05, 5/-0.6, 6/-1.15, 7/-1.7, 8/-2.25}{
    \node[hid] (a\k) at (\xL, \y) {};
  }
  % Layer 2: 4 nodes, centred at y=-0.6, spacing 0.65
  \foreach \k/\y in {1/0.4, 2/-0.25, 3/-0.9, 4/-1.55}{
    \node[hid] (b\k) at (\xLii, \y) {};
  }
  % Layer 3: 2 nodes
  \foreach \k/\y in {1/-0.2, 2/-1.0}{
    \node[hid] (c\k) at (\xLiii, \y) {};
  }
  % Layer 4: 1 node
  \node[hid] (d1) at (\xLiv, -0.6) {};

  % Layer width labels
  \node[font=\scriptsize] at (\xL, 2.15) {$80$};
  \node[font=\scriptsize] at (\xLii, 0.95) {$40$};
  \node[font=\scriptsize] at (\xLiii, 0.35) {$20$};
  \node[font=\scriptsize] at (\xLiv, -0.05) {$10$};

  % Output
  \node[outn] (phi) at (10.0, -0.6) {$\Phi_\theta$};

  % Connections (sparse, only a sample to suggest dense connectivity)
  \foreach \src in {x, t}
    \foreach \dst in {a1, a3, a5, a7}
      \draw[arr] (\src) -- (\dst);
  \foreach \a in {1,3,5,7}
    \foreach \b in {1,2,3,4}
      \draw[arr, opacity=0.30] (a\a) -- (b\b);
  \foreach \a in {1,2,3,4}
    \foreach \b in {1,2}
      \draw[arr, opacity=0.30] (b\a) -- (c\b);
  \foreach \a in {1,2}
      \draw[arr, opacity=0.45] (c\a) -- (d1);
  \draw[arr] (d1) -- (phi);

  % Loss boxes on the right
  \node[loss, right=14mm of phi, yshift=14mm] (lpde)
    {gPINN: $\mathcal{L}_{\mathrm{r}},\,\mathcal{L}_{\partial_x r},\,\mathcal{L}_{\partial_t r}$};
  \node[loss, right=14mm of phi] (lic)
    {IC: $\mathcal{L}_{\Phi}^{\mathrm{IC}},\,\mathcal{L}_{\partial_t\Phi}^{\mathrm{IC}}$};
  \node[loss, right=14mm of phi, yshift=-14mm] (lbc)
    {BC: $\mathcal{L}_{\mathrm{BC}}^{\mathrm{left}},\,\mathcal{L}_{\mathrm{BC}}^{\mathrm{right}}$};
  \draw[arr, thick, black] (phi) -- (lpde) node[midway, above, font=\scriptsize, sloped] {AD};
  \draw[arr, thick, black] (phi) -- (lic);
  \draw[arr, thick, black] (phi) -- (lbc);

  % Sum to total loss
  \node[loss, fill=green!25, right=8mm of lic] (ltot)
    {$\mathcal{L}_{\mathrm{fwd}}$\\Eq.~\eqref{eq:loss-fwd}};
  \draw[arr, thick, black] (lpde) -| (ltot);
  \draw[arr, thick, black] (lic) -- (ltot);
  \draw[arr, thick, black] (lbc) -| (ltot);

  % Section labels (placed above the layer columns).
  \node[font=\scriptsize, gray!70!black] at (0.0, 2.7) {inputs};
  \node[font=\scriptsize, gray!70!black] at (5.0, 2.7) {$[80,40,20,10]$ tanh MLP};
  \node[font=\scriptsize, gray!70!black] at (10.0, 2.7) {output transform};
  \node[font=\scriptsize, gray!70!black] at (13.5, 2.7) {weighted loss terms};
\end{tikzpicture}
\caption{Forward PINN architecture used in this work. Inputs
$(x,t)$ pass through a fully-connected $[80, 40, 20, 10]$ $\tanh$
network with $4{,}521$ trainable parameters, whose scalar output
is wrapped in the tanh envelope
$\Phi_\theta(x,t) = A\tanh(\hat y(x,t))$ with $A = 1$. Spatial and
temporal derivatives required by the Zerilli PDE residual
$\mathcal{L}_{\mathrm{r}}$ of Eq.~\eqref{eq:loss-fwd} and its
gradient augmentations $\mathcal{L}_{\partial_x r},
\mathcal{L}_{\partial_t r}$ (gPINN, \citealt{Yu2022gpinn}) are
computed by automatic differentiation; the IC residuals
$\mathcal{L}_{\Phi}^{\mathrm{IC}}, \mathcal{L}_{\partial_t\Phi}^{\mathrm{IC}}$ and the left/right BC residuals
$\mathcal{L}_{\mathrm{BC}}^{\mathrm{left}}, \mathcal{L}_{\mathrm{BC}}^{\mathrm{right}}$ enter as additional weighted terms with
$\boldsymbol{\lambda} = (100,100,100,1,100,1,1)$
(Eq.~\eqref{eq:weights}). The decreasing layer widths are
reflected in the visually shrinking node columns.}
\label{fig:fwd-pinn-arch}
\end{figure}

Let $r_\theta(x,t) \equiv \partial_t^2\Phi_\theta -
\partial_x^2\Phi_\theta + V_\ell\,\Phi_\theta$ be the pointwise
residual of Eq.~\eqref{eq:zerilli}. The training loss is the
standard composite of PDE, initial-condition and boundary-condition
residuals, augmented with the gradient terms of the
gradient-enhanced PINN \citep{Yu2022gpinn},
\begin{equation}
  \mathcal{L}_{\mathrm{fwd}}
  = \lambda_{r}     \Lpde
  + \lambda_{r_x}   \mathcal{L}_{\partial_x r}
  + \lambda_{r_t}   \mathcal{L}_{\partial_t r}
  + \lambda_{\mathrm{IC}} \mathcal{L}_{\Phi}^{\mathrm{IC}}
  + \lambda_{\mathrm{IV}} \mathcal{L}_{\partial_t\Phi}^{\mathrm{IC}}
  + \lambda_{\mathrm{BL}} \mathcal{L}_{\mathrm{BC}}^{\mathrm{left}}
  + \lambda_{\mathrm{BR}} \mathcal{L}_{\mathrm{BC}}^{\mathrm{right}}.
  \label{eq:loss-fwd}
\end{equation}
The seven mean-squared component losses are
\begin{align}
  \Lpde &= \frac{1}{N_r}\sum_{(x,t)\in\mathcal{X}_r}
            r_\theta(x,t)^{2},
  & \mathcal{L}_{\partial_x r} &= \frac{1}{N_r}\sum_{(x,t)\in\mathcal{X}_r}
            \bigl(\partial_x r_\theta(x,t)\bigr)^{2},
  \nonumber\\
  \mathcal{L}_{\partial_t r} &= \frac{1}{N_r}\sum_{(x,t)\in\mathcal{X}_r}
            \bigl(\partial_t r_\theta(x,t)\bigr)^{2},
  & \mathcal{L}_{\Phi}^{\mathrm{IC}} &= \frac{1}{N_i}\sum_{x\in\mathcal{X}_i}
            \bigl(\Phi_\theta(x,0)-\Phi(x,0)\bigr)^{2},
  \nonumber\\
  \mathcal{L}_{\partial_t\Phi}^{\mathrm{IC}}
     &= \frac{1}{N_i}\sum_{x\in\mathcal{X}_i}
        \bigl(\partial_t\Phi_\theta(x,0)-\partial_t\Phi(x,0)\bigr)^{2},
  & \mathcal{L}_{\mathrm{BC}}^{\mathrm{left}}
     &= \frac{1}{N_b}\sum_{t\in\mathcal{X}_b^{L}}
        \bigl((\partial_t-\partial_x)\Phi_\theta(x_{\min},t)\bigr)^{2},
  \nonumber\\
  \mathcal{L}_{\mathrm{BC}}^{\mathrm{right}}
     &= \frac{1}{N_b}\sum_{t\in\mathcal{X}_b^{R}}
        \bigl((\partial_t+\partial_x)\Phi_\theta(x_{\max},t)\bigr)^{2},
  & &
  \label{eq:loss-components}
\end{align}
mirroring the formulation of \citet[Eqs.~(4)--(10)]{Patel2024pinn}.
The two gradient terms $\mathcal{L}_{\partial_x r},
\mathcal{L}_{\partial_t r}$ drive the residual to vanish
smoothly rather than oscillate about zero \citep{Yu2022gpinn}. The collocation set sizes are listed in
Table~\ref{tab:fwd-hyper}, with $N_b = 400$ boundary points
shared across both boundaries where \citet{Patel2024pinn} quote
$400$ \emph{per boundary} --- the only collocation difference
between the two baselines. The weight vector applied throughout this work is
\begin{equation}
  \boldsymbol{\lambda}
  = (\lambda_{r},\lambda_{r_x},\lambda_{r_t},\lambda_{\mathrm{IC}},
     \lambda_{\mathrm{IV}},\lambda_{\mathrm{BL}},\lambda_{\mathrm{BR}})
  = (100,\,100,\,100,\,1,\,100,\,1,\,1),
  \label{eq:weights}
\end{equation}
following \citet{Patel2024pinn}: $\lambda_{\mathrm{IV}} = 100$
suppresses the phase drift reported (and reproduced here) when
the velocity condition is under-weighted, and the unit
Sommerfeld weights reflect the causal disconnection at
Eq.~\eqref{eq:bc-arrival}.

Optimisation is Adam \citep{Kingma2015adam} followed by L-BFGS
\citep{Liu1989lbfgs} at double the budget of
\citet{Patel2024pinn} (Table~\ref{tab:train-config}), since the
loss still decreases through iteration $\sim\!25{,}000$;
training is full-batch in double precision, which we found
necessary to avoid floating-point-induced L-BFGS stalls.
Every $1{,}000$ Adam iterations a greedy residual-resampling
scheme \citep{Lu2021deepxde,Wu2023adaptive} scores $10^{5}$
uniform candidates by $|r_\theta|$ and rebuilds the collocation
set as the top $0.3\,N_r$ by residual plus $0.7\,N_r$ uniform
redraws; the uniform floor prevents collapse onto the wavefront
late in training.

% --------------------------------------------------------------
\subsection{Inverse PINN}
\label{sec:inv-pinn}

The inverse problem recovers five unknowns from noisy field
observations: the black-hole mass $M$, which enters the Zerilli
potential Eq.~\eqref{eq:V-zerilli}, and the template parameters
$(\omega,\tau,A_{\mathrm{ring}},\phi)$ of the late-time damped
sinusoid of Eq.~\eqref{eq:qnm-omega} at the observer. The
network of Sec.~\ref{sec:fwd-pinn} is trained jointly with these
five scalars --- appended to the optimiser's parameter list and
updated in the same gradient steps --- under the single loss of
Eq.~\eqref{eq:loss-inv} \citep{Lu2021deepxde,Cornell2022pinnqnm}.
Training data are noisy samples of
the true Zerilli waveform, generated by adding Gaussian noise to
the FD reference,
\begin{equation}
  \Phiobs(x_i, t_i) = \Phi_{\mathrm{FD}}(x_i, t_i) + \eta_i,
  \qquad
  \eta_i \sim \mathcal{N}(0,\sigma_{\mathrm{obs}}^2),
  \label{eq:noisy-obs}
\end{equation}
with
$\sigma_{\mathrm{obs}} = 0.01\times\max_{x,t}|\Phi_{\mathrm{FD}}(x,t)|$
and $\Nobs = 500$ points $(x_i, t_i)$ drawn uniformly from the full
two-dimensional domain $[-50,150]\,M\times[0,50]\,M$, distinct
from the one-dimensional template grid at $x = \xq$ introduced in
Eq.~\eqref{eq:loss-ring} below. The fixed observer position
$\xq = 10\,M$ is used both for the ringdown template constraint
and for all post-hoc QNM extraction.

The composite inverse loss has three blocks,
\begin{equation}
  \mathcal{L}_{\mathrm{inv}}
  = \mathcal{L}_{\mathrm{PINN}}(M)
  + \lambda_{\mathrm{data}}\,\Ldata
  + \lambda_{\mathrm{ring}}\,\Lring(\omega,\tau,A_{\mathrm{ring}},\phi),
  \label{eq:loss-inv}
\end{equation}
where $\mathcal{L}_{\mathrm{PINN}}(M)$ is the seven-term sum of
Eq.~\eqref{eq:loss-fwd} with weights as in Eq.~\eqref{eq:weights}, the
PDE residual now depending on the learnable $M$ through the Zerilli
potential. The data term is the mean squared mismatch between the
network output and the observations,
\begin{equation*}
  \Ldata = \frac{1}{\Nobs}\sum_{i=1}^{\Nobs}
  \bigl(\Phitheta(x_i,t_i) - \Phiobs(x_i,t_i)\bigr)^{2},
\end{equation*}
with $\lambda_{\mathrm{data}} = 10$, set once so that the data
and PDE terms start at comparable magnitude. The ringdown term
enforces a damped-sinusoid template at the observer position,
\begin{equation}
  \Lring = \frac{1}{\Nring}\sum_{j=1}^{\Nring}
  \Bigl(\Phitheta(\xq,t_j) - A_{\mathrm{ring}}\,e^{-t_j/\tau}\cos(\omega t_j + \phi)\Bigr)^{2},
  \label{eq:loss-ring}
\end{equation}
with the times $t_j$ on a uniform grid of $\Nring$ points on
$[\tring,\,50]\,M$; the residual back-propagates into both
$\theta$ and the template scalars on every step. Embedding the
template inside the loss is new --- prior PINN-based
perturbation studies, including \citet{Patel2024pinn}, fit it
only after training --- and gives the scalars their operational
definition; its hyperparameters $(\tring,
\lambda_{\mathrm{ring}}, \Nring)$ are ablated in
Sec.~\ref{sec:variants}.

The architecture and optimiser sequence match the forward PINN
at a reduced L-BFGS budget (Table~\ref{tab:train-config}).
The full architecture, including the five learnable scalars
appended to the network parameters, is shown in
Fig.~\ref{fig:inv-pinn-arch}.

\begin{figure}[!htbp]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
  node distance=10mm and 12mm,
  every node/.style={font=\small},
  inp/.style={circle, draw, fill=blue!10, minimum size=7mm, inner sep=0pt},
  hid/.style={circle, draw, fill=gray!12, minimum size=4mm, inner sep=0pt},
  outn/.style={circle, draw, fill=orange!20, minimum size=7mm, inner sep=0pt},
  var/.style={rectangle, draw, fill=yellow!25, rounded corners=2pt,
              minimum height=5mm, minimum width=9mm, inner sep=2pt,
              font=\scriptsize, align=center},
  loss/.style={rectangle, draw, fill=red!10, rounded corners=2pt,
               minimum height=6mm, minimum width=22mm, inner sep=2pt,
               align=center, font=\footnotesize},
  arr/.style={->, >=stealth, thin, gray!70},
]
  % Inputs
  \node[inp] (x) at (0, 0) {$x$};
  \node[inp] (t) at (0, -1.2) {$t$};

  \def\xL{2.0}
  \def\xLii{4.0}
  \def\xLiii{6.0}
  \def\xLiv{8.0}

  % Layer 1: 8 nodes
  \foreach \k/\y in {1/1.6, 2/1.05, 3/0.5, 4/-0.05, 5/-0.6, 6/-1.15, 7/-1.7, 8/-2.25}{
    \node[hid] (a\k) at (\xL, \y) {};
  }
  % Layer 2: 4 nodes
  \foreach \k/\y in {1/0.4, 2/-0.25, 3/-0.9, 4/-1.55}{
    \node[hid] (b\k) at (\xLii, \y) {};
  }
  % Layer 3: 2 nodes
  \foreach \k/\y in {1/-0.2, 2/-1.0}{
    \node[hid] (c\k) at (\xLiii, \y) {};
  }
  % Layer 4: 1 node
  \node[hid] (d1) at (\xLiv, -0.6) {};

  % Layer width labels
  \node[font=\scriptsize] at (\xL, 2.15) {$80$};
  \node[font=\scriptsize] at (\xLii, 0.95) {$40$};
  \node[font=\scriptsize] at (\xLiii, 0.35) {$20$};
  \node[font=\scriptsize] at (\xLiv, -0.05) {$10$};

  % Output
  \node[outn] (phi) at (10.0, -0.6) {$\Phi_\theta$};

  % Connections (sparse)
  \foreach \src in {x, t}
    \foreach \dst in {a1, a3, a5, a7}
      \draw[arr] (\src) -- (\dst);
  \foreach \a in {1,3,5,7}
    \foreach \b in {1,2,3,4}
      \draw[arr, opacity=0.30] (a\a) -- (b\b);
  \foreach \a in {1,2,3,4}
    \foreach \b in {1,2}
      \draw[arr, opacity=0.30] (b\a) -- (c\b);
  \foreach \a in {1,2}
      \draw[arr, opacity=0.45] (c\a) -- (d1);
  \draw[arr] (d1) -- (phi);

  % Loss boxes on the right.
  \node[loss, right=14mm of phi, yshift=22mm] (lpinn)
    {$\mathcal{L}_{\mathrm{PINN}}(M)$\\Eq.~\eqref{eq:loss-fwd}};
  \node[loss, right=14mm of phi] (ldata)
    {$\lambda_{\mathrm{data}}\,\Ldata$\\$\Phiobs$ at $\Nobs=500$ pts};
  \node[loss, right=14mm of phi, yshift=-22mm] (lring)
    {$\lambda_{\mathrm{ring}}\,\Lring$\\template at $\xq$, Eq.~\eqref{eq:loss-ring}};

  % Edges from network output to losses
  \draw[arr, thick, black] (phi) -- (lpinn) node[midway, above, font=\scriptsize, sloped] {AD};
  \draw[arr, thick, black] (phi) -- (ldata);
  \draw[arr, thick, black] (phi) -- (lring);

  % Trainable scalar M sits above L_PINN at the top right.
  \node[var, above=4mm of lpinn] (vM) {$M$};
  \node[font=\scriptsize, gray!70!black, above=0.5mm of vM]
    {trainable scalar};
  \draw[arr, thick, black!70] (vM) -- (lpinn);

  % Trainable template scalars sit below L_ring at the bottom right.
  % The four boxes are placed first; the label sits below them so it
  % cannot collide with the L_ring loss box above.
  \node[var, below=6mm of lring.south west, anchor=north west] (vom) {$\omega$};
  \node[var, right=1mm of vom]   (vtau)  {$\tau$};
  \node[var, right=1mm of vtau]  (vA)    {$A_{\mathrm{ring}}$};
  \node[var, right=1mm of vA]    (vphi0) {$\phi$};
  \node[font=\scriptsize, gray!70!black, below=0.5mm of vom.south west, anchor=north west]
    {trainable scalars};
  \draw[arr, thick, black!70] (vom.north)   -- (vom.north   |- lring.south);
  \draw[arr, thick, black!70] (vtau.north)  -- (vtau.north  |- lring.south);
  \draw[arr, thick, black!70] (vA.north)    -- (vA.north    |- lring.south);
  \draw[arr, thick, black!70] (vphi0.north) -- (vphi0.north |- lring.south);

  % Sum to total loss
  \node[loss, fill=red!25, right=8mm of ldata] (ltot)
    {$\mathcal{L}_{\mathrm{inv}}$\\Eq.~\eqref{eq:loss-inv}};
  \draw[arr, thick, black] (lpinn) -| (ltot);
  \draw[arr, thick, black] (ldata) -- (ltot);
  \draw[arr, thick, black] (lring) -| (ltot);

  % Section labels (placed above the network).
  \node[font=\scriptsize, gray!70!black] at (0.0, 2.7) {inputs};
  \node[font=\scriptsize, gray!70!black] at (5.0, 2.7) {$[80,40,20,10]$ tanh MLP};
  \node[font=\scriptsize, gray!70!black] at (10.0, 2.7) {output};
\end{tikzpicture}%
}
\caption{Inverse PINN architecture. The same $[80,40,20,10]$
tanh MLP as the forward PINN of Fig.~\ref{fig:fwd-pinn-arch}
produces $\Phi_\theta(x,t)$. Five additional scalars
$(M,\omega,\tau,A_{\mathrm{ring}},\phi)$ (yellow boxes) are
appended to the optimiser's parameter list alongside
the network weights $\theta$.
The mass $M$ enters the Zerilli potential of the PINN PDE
residual $\mathcal{L}_{\mathrm{PINN}}(M)$ at the top, the noisy
observations $\Phiobs$ enter the data residual $\Ldata$ in the
middle, and the four template parameters
$(\omega,\tau,A_{\mathrm{ring}},\phi)$ enter the ringdown
template residual $\Lring$ at the observer column $\xq=10\,M$ at
the bottom. The total loss $\mathcal{L}_{\mathrm{inv}}$ of
Eq.~\eqref{eq:loss-inv} is back-propagated through both the
network weights and the five learnable scalars in every gradient
step; the embedded template residual is the key structural
difference from the forward PINN.}
\label{fig:inv-pinn-arch}
\end{figure}

We initialise $M_{\mathrm{init}} = 0.8$,
$\omega_{\mathrm{init}} = 0.30$ and $\tau_{\mathrm{init}} = 8.0$
--- offsets of $20$--$29\%$ from the true values ($M = 1$ and
the Leaver references of Eq.~\eqref{eq:qnm-ref}) --- so the
inverse pipeline cannot succeed by sitting near its
initialisation.

The full training pipeline is summarised in
Fig.~\ref{fig:pipeline}.

\begin{figure}[t]
\centering
\begin{tikzpicture}[
  node distance=0.55cm and 0.7cm,
  block/.style={draw, rounded corners, minimum width=2.7cm,
                minimum height=0.9cm, align=center, font=\footnotesize,
                fill=blue!8},
  data/.style={draw, rounded corners, minimum width=2.7cm,
               minimum height=0.9cm, align=center, font=\footnotesize,
               fill=green!10},
  loss/.style={draw, rounded corners, minimum width=2.6cm,
               minimum height=0.7cm, align=center, font=\scriptsize,
               fill=red!8},
  outbox/.style={draw, rounded corners, minimum width=2.7cm,
                 minimum height=0.9cm, align=center, font=\footnotesize,
                 fill=orange!15},
  arr/.style={->,>=stealth,thick}
]
  % Data row
  \node[data] (fd) {FD reference\\$\Phi_{\mathrm{FD}}(x,t)$};
  \node[data, right=of fd] (noise)
       {Add noise\\$\eta\sim\mathcal{N}(0,\sigma_{\mathrm{obs}}^2)$};
  \node[data, right=of noise] (obs)
       {$\Phiobs$\\$\Nobs=500$};

  % Network
  \node[block, below=1.1cm of noise] (net)
       {PINN $\Phi_\theta(x,t)$\\$+$ learnables\\$M,\omega,\tau,A_{\mathrm{ring}},\phi$};

  % Losses
  \node[loss, below left=1.0cm and 0.0cm of net] (lpinn)
       {$\mathcal{L}_{\mathrm{PINN}}(M)$\\
        $\boldsymbol{\lambda}=(100,100,100,1,100,1,1)$};
  \node[loss, below=1.0cm of net] (ldata)
       {$\lambda_{\mathrm{data}}\,\Ldata$\\$\lambda_{\mathrm{data}}=10$};
  \node[loss, below right=1.0cm and 0.0cm of net] (lring)
       {$\lambda_{\mathrm{ring}}\,\Lring$\\(template at $\xq=10M$)};

  % Optimiser
  \node[block, below=1.0cm of ldata] (opt)
       {Adam ($10^{4}$) $\to$ L-BFGS ($1.5{\times}10^{4}$)};

  % Post-hoc
  \node[outbox, below=of opt] (qnm)
       {Post-hoc QNM extraction\\(M1, M2, M3, M4, M5)};

  % Edges
  \draw[arr] (fd) -- (noise);
  \draw[arr] (noise) -- (obs);
  \draw[arr] (obs) |- (net.east);
  \draw[arr] (net) -- (lpinn);
  \draw[arr] (net) -- (ldata);
  \draw[arr] (net) -- (lring);
  \draw[arr] (lpinn) |- (opt);
  \draw[arr] (ldata) -- (opt);
  \draw[arr] (lring) |- (opt);
  \draw[arr] (opt) -- node[right,font=\scriptsize]{trained $\Phi_\theta(\xq,t)$} (qnm);
\end{tikzpicture}
\caption{Inverse PINN training and extraction pipeline. The FD
reference waveform is corrupted by $1\%$ Gaussian noise to produce
$\Nobs = 500$ observations. The PINN $\Phi_\theta$ is trained jointly
with the learnable mass $M$ and template parameters
$(\omega,\tau,A_{\mathrm{ring}},\phi)$ against the composite loss of
Eq.~\eqref{eq:loss-inv}. After training, the time series
$\Phi_\theta(\xq, t)$ at the observer is passed to the five post-hoc
QNM extraction methods of Sec.~\ref{sec:qnm-methods}.}
\label{fig:pipeline}
\end{figure}

\subsubsection{Variants of the ringdown-template loss}
\label{sec:variants}

The hyperparameters $\tring$, $\lambda_{\mathrm{ring}}$ and $\Nring$
control the start of the ringdown window, the strength
of the template constraint relative to the PDE terms, and the
number of template evaluation points. The six configurations of
Table~\ref{tab:variants} form a one-factor-at-a-time ablation
(A, B, C), a combined run (D), and a structural extension (E)
that adds the first overtone to the template as a second damped
sinusoid with its own learnable $(\omega_1,\tau_1,A_1,\phi_1)$,
initialised $19$--$29\%$ from the Leaver values in the same
offset philosophy as the fundamental. The template-mode
subscripts $\omega_n,\tau_n$ are distinct from the Leaver
overtone labels of Eq.~\eqref{eq:qnm-ref} and from the two-mode
NLS post-hoc fit of Eq.~\eqref{eq:two-mode} below; the three
notations agree numerically only at the truth.

\begin{table}[t]
\centering
\caption{Inverse PINN variants. All variants share the architecture
of Sec.~\ref{sec:fwd-pinn} (Eq.~\eqref{eq:weights} weights), the
optimiser schedule of Sec.~\ref{sec:inv-pinn}, the noisy data of
Eq.~\eqref{eq:noisy-obs} with $\Nobs = 500$ and
$\sigma_{\mathrm{obs}} = 0.01\,\Phi_{\max}$, and the observer
position $\xq = 10\,M$. Variant E adds an overtone to the template
with learnable $(\omega_1,\tau_1,A_1,\phi_1)$, initialised at
$\omega_1 = 0.28$, $\tau_1 = 4.7$, $A_1 = 0.25$, $\phi_1 = 0$.
``Single mode'' refers to the template of
Eq.~\eqref{eq:loss-ring}; ``two mode'' replaces the template by a sum
of two damped sinusoids. Bold values mark the hyperparameter changed
relative to the baseline.}
\label{tab:variants}
\small
\begin{tabular}{@{}lcccl@{}}
\toprule
Tag & $\tring/M$ & $\lambda_{\mathrm{ring}}$ & $\Nring$ & Template \\
\midrule
baseline   & $10$ & $1$   & $200$  & single mode \\
A          & $\mathbf{18}$ & $1$   & $200$  & single mode \\
B          & $10$ & $\mathbf{100}$ & $200$  & single mode \\
C          & $10$ & $1$   & $\mathbf{1000}$ & single mode \\
\textbf{D} & $\mathbf{18}$ & $\mathbf{100}$ & $\mathbf{1000}$ & single mode \\
E          & $18$ & $100$ & $1000$ & two modes \\
\bottomrule
\end{tabular}
\end{table}

The value $\tring = 18\,M$ (variants A, D, E) sits at the upper
edge of the forward M5 plateau: overtone contamination is
negligible there ($e^{-18/\tau_1} \approx 7\times 10^{-3}$)
while the fundamental retains
$e^{-18/\tau_0} \approx 0.20$ of its initial amplitude.
$\lambda_{\mathrm{ring}} = 100$ (variants B, D, E) raises the
template to the weight of the PDE residual in
Eq.~\eqref{eq:weights}; at the default
$\lambda_{\mathrm{ring}} = 1$ it is two orders of magnitude
weaker and the QNM scalars drift.

The ringdown loss Eq.~\eqref{eq:loss-ring} contains an
$A_{\mathrm{ring}}$--$\tau$ degeneracy: expanding the envelope,
$A_{\mathrm{ring}} e^{-t/\tau}
= A_{\mathrm{ring}}\bigl(1 - t/\tau
+ \mathcal{O}(t^2/\tau^2)\bigr)$, the residual depends to first
order only on $A_{\mathrm{ring}}$ and $A_{\mathrm{ring}}/\tau$,
and the exponentially suppressed tail is too weak to lift the
degeneracy at second order. L-BFGS drifts along this direction,
so $\tau_{\mathrm{learned}}$ can differ substantially from the
decay rate of the converged field: M2 on the training window
$[\tring,\,50]\,M$ recovers the biased $\tau_{\mathrm{learned}}$
while M2 on $[10,\,50]\,M$ recovers a value close to Leaver,
and the convention of Sec.~\ref{sec:reporting} therefore quotes
the wide window.

% --------------------------------------------------------------
\subsection{Fourier neural operator surrogate}
\label{sec:fno-method}

Each PINN above solves a \emph{single} Zerilli instance,
parametrised by one mass $M$ and one Gaussian pulse
$(x_{0},\sigma)$; a new pulse or mass requires a fresh
$\mathcal{O}(10^{4})$-iteration run. The Fourier neural operator
(FNO) \citep{Li2021fno,Kovachki2023neuralop} instead trains once
on $1{,}000$ $(M,x_{0},\sigma)$ draws and predicts $\Phi(x,t)$
for an unseen draw with one forward pass, producing the same
observer series on which the extractors and reporting
convention of Secs.~\ref{sec:qnm-methods}--\ref{sec:reporting}
operate unchanged. The architecture and
loss pipeline are summarised in Fig.~\ref{fig:fno-arch}, and
the configuration of both surrogates in
Table~\ref{tab:surrogate-config}.

\begin{table}[!htbp]
\centering
\caption{Configuration of the two surrogates: the end-to-end
FNO of Sec.~\ref{sec:fno-method} and the hybrid residual
operator of Sec.~\ref{sec:hyb-method}. Optimiser settings are
listed in Table~\ref{tab:train-config}; the FNO loss weights
are the deployed
$(\lambda_{f},\lambda_{H^{1}},\lambda_{\mathrm{ring}},
\lambda_{\mathrm{obs}},\lambda_{\mathrm{tw}},
\lambda_{\mathrm{pde}})$ of Eq.~\eqref{eq:fno-loss}.}
\label{tab:surrogate-config}
\small
\setlength{\tabcolsep}{5pt}
\begin{tabular}{@{}lcc@{}}
\toprule
 & FNO & Hybrid FNO \\
\midrule
Domain $x/M \times t/M$ & $[-50,150]\times[0,100]$ & $[-50,150]\times[0,50]$ \\
Grid $N_{x}\times N_{t}$ (fine) & $401\times 401$ & $1001\times 501$ \\
Coarse grids & --- & $k{=}4$ prior; $k{=}2$ label only \\
Input channels & $4$ & $5$ \\
Hidden channels $h$ & $96$ & $32$ \\
Fourier modes $(K_{t},K_{x})$ & $(64,64)$ & $(16,32)$ \\
Domain padding & $0.10$ & $0.10$ \\
Parameters & $7.8\times 10^{7}$ & $1.13\times 10^{6}$ \\
$M$ & $\mathrm{Unif}[0.8,1.2]$ & $\mathrm{Unif}[0.8,1.2]$ \\
$x_{0}/M$ & $\mathrm{Unif}[-10,30]$ & $\mathrm{Unif}[2,6]$ \\
$\sigma/M$ & $\mathrm{Unif}[3,8]$ & $\mathrm{Unif}[3,7]$ \\
Split (seed $1234$) & $1000/100/100$ & $1000/100/100$ \\
Loss & Eq.~\eqref{eq:fno-loss}, $(1,0.20,10,5,1,0.05)$ & Eq.~\eqref{eq:hyb-loss} \\
Observer $\xq$ & $2\,M$ & $2\,M$ \\
Canonical BH $(M,x_{0},\sigma)$ & $(1,4,5)$ & $(1,4,5)$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[!htbp]
\centering
\resizebox{0.68\textwidth}{!}{%
\begin{tikzpicture}[
  every node/.style={font=\small},
  inp/.style={rectangle, draw, fill=blue!10, rounded corners=2pt,
              minimum height=7mm, minimum width=88mm, inner sep=3pt,
              align=center, font=\small},
  stack/.style={rectangle, draw=none, fill=none,
                inner sep=2pt, align=center, font=\small},
  stackbg/.style={rectangle, draw, fill=blue!5, rounded corners=3pt,
                  inner sep=4pt},
  sconv/.style={rectangle, draw, fill=gray!16, rounded corners=2pt,
                minimum height=9mm, minimum width=96mm, inner sep=3pt,
                align=center, font=\small},
  sconvhdr/.style={rectangle, draw=none, fill=none,
                   inner sep=2pt, align=center},
  sconvbg/.style={rectangle, draw, fill=gray!18, rounded corners=3pt,
                  inner sep=5pt},
  outn/.style={rectangle, draw, fill=orange!22, rounded corners=2pt,
               minimum height=8mm, minimum width=68mm, inner sep=3pt,
               align=center, font=\small},
  loss/.style={rectangle, draw, fill=green!12, rounded corners=2pt,
               minimum height=6mm, minimum width=88mm, inner sep=2pt,
               align=center, font=\footnotesize},
  lossbg/.style={rectangle, draw, fill=green!5, rounded corners=3pt,
                 inner sep=4pt},
  ltot/.style={rectangle, draw, fill=green!28, rounded corners=2pt,
               minimum height=9mm, minimum width=68mm, inner sep=3pt,
               align=center, font=\small},
  arr/.style={->, >=stealth, thick, black!75},
  arrm/.style={->, >=stealth, thin, gray!75},
  arrt/.style={->, >=stealth, thick, gray!55!black, dashed},
]
  % ============ four-channel input stack ============
  \node[stack] (stackhdr) {%
    \textbf{four-channel input on fine grid}\;
    $X\in\mathbb{R}^{4\times N_{t}\times N_{x}}$};
  \node[inp, below=2mm of stackhdr]
        (c0) {ch~0: $\Phi(x,0)$ broadcast in $t$};
  \node[inp, below=1mm of c0]
        (c1) {ch~1: $\partial_{t}\Phi(x,0)$ broadcast in $t$};
  \node[inp, below=1mm of c1]
        (c2) {ch~2: $V_{\ell}(x;M)$ broadcast in $t$};
  \node[inp, below=1mm of c2]
        (c3) {ch~3: $M$ broadcast over $(x,t)$};
  \begin{scope}[on background layer]
    \node[stackbg, fit=(stackhdr)(c0)(c3)] (stackbg) {};
  \end{scope}

  % ============ FNO operator ============
  \node[sconvhdr, below=8mm of stackbg.south] (sconvhdrn) {%
    \begin{minipage}{96mm}
      \centering
      \textbf{neural solution operator $\mathcal{G}_{\theta}$ â€” four-layer 2-D FNO}\\[1mm]
      hidden channels $h=96$,\;
      Fourier modes $(K_{t},K_{x})=(64,64)$
    \end{minipage}};
  \node[sconv, below=3mm of sconvhdrn]
        (lift)  {lift $1{\times}1$ conv: $4{\to}96$ channels};
  \node[sconv, below=2mm of lift]
        (sb)    {$L=4 \times$ spectral block\\
                 $v^{(\ell+1)}=\mathrm{GELU}\bigl(
                  \mathcal{F}^{-1}[R^{(\ell)}{\odot}\mathcal{F}v^{(\ell)}]
                  + W^{(\ell)}v^{(\ell)}\bigr)$};
  \node[sconv, below=2mm of sb]
        (proj)  {projection $1{\times}1$ conv: $96{\to}1$ channel};
  \draw[arr] (lift) -- (sb);
  \draw[arr] (sb) -- (proj);
  \begin{scope}[on background layer]
    \node[sconvbg, fit=(sconvhdrn)(lift)(proj)] (fnoall) {};
  \end{scope}

  \draw[arr] (stackbg.south) -- (fnoall.north);

  % ============ output field ============
  \node[outn, below=8mm of fnoall.south, fill=orange!45]
        (phi) {$\Phi_{\theta}(x,t)$ â€” predicted field on $(N_{t}{\times}N_{x})$ fine grid};
  \draw[arr] (fnoall.south) -- (phi.north);

    % ============ six loss terms (training only) ============
    \node[stack, below=8mm of phi] (losshdr) {%
      \textbf{six weighted loss terms}\\[-0.5mm]
      training only};
    \node[loss, below=2mm of losshdr] (lf)
      {$\mathcal{L}_{\mathrm{field}}$ â€” relative $L^{2}$ on $\Phi$};
    \node[loss, below=1mm of lf] (lh1)
      {$\mathcal{L}_{H^{1}}$ â€” gradients $(\partial_{t}\Phi,\partial_{x}\Phi)$};
    \node[loss, below=1mm of lh1] (lring)
      {$\mathcal{L}_{\mathrm{ring}}$ â€” ringdown window $t\ge\tring$};
    \node[loss, below=1mm of lring] (lobs)
      {$\mathcal{L}_{\mathrm{obs}}$ â€” observer column $x=\xq$};
    \node[loss, below=1mm of lobs] (ltw)
      {$\mathcal{L}_{\mathrm{tw}}$ â€” time-window ramp $w(t)$};
    \node[loss, below=1mm of ltw] (lpde)
      {$\mathcal{L}_{\mathrm{pde}}$ â€” FD residual of the wave eq.};
    \begin{scope}[on background layer]
      \node[lossbg, fit=(losshdr)(lf)(lpde)] (lossall) {};
    \end{scope}

    \draw[arrt] (phi.south) -- (lossall.north);

    % aggregator below the loss stack
    \node[ltot, below=8mm of lossall.south]
        (lt) {$\mathcal{L}_{\mathrm{FNO}}
              =\sum_{k} \lambda_{k}\,\mathcal{L}_{k}$
              \quad (Eq.~\eqref{eq:fno-loss})};
    \draw[arrt] (lossall.south) -- (lt.north);
\end{tikzpicture}%
}
\caption{FNO surrogate architecture used in this work. The
four-channel input stack
$(\Phi(x,0), \partial_{t}\Phi(x,0), V_{\ell}(x;M), M)$ is lifted
to $96$ channels by a $1{\times}1$ convolution and passed through
four spectral-convolution blocks, each retaining
$64{\times}64$ Fourier modes along $(x,t)$ and combined with a
$1{\times}1$ skip convolution and a GELU nonlinearity, before a
$1{\times}1$ projection back to the single output channel
$\Phi_{\theta}(x,t)$. The output field feeds six weighted loss
terms summed into $\mathcal{L}_{\mathrm{FNO}}$
(Eq.~\eqref{eq:fno-loss}), of which $\mathcal{L}_{\mathrm{obs}}$
and $\mathcal{L}_{\mathrm{tw}}$ are the QNM-targeted terms
introduced in the final training configuration (see the
ablation in Appendix~\ref{app:supp-fno}). The total parameter count is
$\sim\!7.8\times 10^{7}$.}
\label{fig:fno-arch}
\end{figure}

\paragraph{Operator and discretisation.} We learn the parametric
solution operator
\begin{equation}
  \mathcal{G}_{\theta}\colon\;
  \bigl(\Phi(\cdot,0),\,\partial_{t}\Phi(\cdot,0),\,
        V_{\ell}(\cdot\,;M),\,M\bigr)
  \;\longmapsto\;
  \Phi(x,t),
  \label{eq:fno-operator}
\end{equation}
discretised on a uniform $401\times 401$ grid
($\Delta x = 0.5\,M$, $\Delta t = 0.25\,M$) covering
$x \in [-50,150]\,M$, $t \in [0,100]\,M$. The four input
channels --- $\Phi(x,0)$, the outgoing velocity
$\partial_{t}\Phi(x,0) = -\partial_{x}\Phi(x,0)$,
$V_{\ell}(x;M)$ of Eq.~\eqref{eq:V-zerilli} and the broadcast
mass $M$ --- are stacked as in Fig.~\ref{fig:fno-arch}; the
output is the single-channel field on the same grid, with
target fields from the RK4 solver of Appendix~\ref{app:fd} at
the same resolution.

\paragraph{Architecture.} A four-layer 2-D FNO with the
configuration of Table~\ref{tab:surrogate-config} (GELU
activations, grid positional embeddings). The FNO's
discretisation invariance
\citep{Li2021fno,Kovachki2023neuralop} is exercised in
Appendix~\ref{app:supp-fno} on grids from $201^{2}$ to $801^{2}$
over the same physical box; we prefer the FNO over DeepONet
\citep{Lu2021deeponet} because its spectral parameterisation
exploits our regular-grid inputs directly and carries this
invariance.

\paragraph{Loss.} The training loss combines six terms,
\begin{equation}
  \mathcal{L}_{\mathrm{FNO}}
  = \lambda_{f}\,\mathcal{L}_{\mathrm{field}}
  + \lambda_{H^{1}}\,\mathcal{L}_{H^{1}}
  + \lambda_{\mathrm{ring}}\,\mathcal{L}_{\mathrm{ring}}
  + \lambda_{\mathrm{obs}}\,\mathcal{L}_{\mathrm{obs}}
  + \lambda_{\mathrm{tw}}\,\mathcal{L}_{\mathrm{tw}}
  + \lambda_{\mathrm{pde}}\,\mathcal{L}_{\mathrm{pde}},
  \label{eq:fno-loss}
\end{equation}
with $\mathcal{L}_{\mathrm{field}}$ the relative $L^{2}$ field
error, $\mathcal{L}_{H^{1}}$ its $H^{1}$ analogue on
$(\partial_{t}\Phi,\partial_{x}\Phi)$,
$\mathcal{L}_{\mathrm{ring}}$ the field $L^{2}$ on
$t \ge \tring = 18\,M$, $\mathcal{L}_{\mathrm{obs}}$ the 1-D
$L^{2}$ on the observer slice $x = \xq$ that the QNM extractors
read, $\mathcal{L}_{\mathrm{tw}}$ the field $L^{2}$ under the
ramp $w(t) = 1 + \beta\,\mathrm{ramp}(t;\,10\,M,\,15\,M)$ with
$\beta = 50$, and $\mathcal{L}_{\mathrm{pde}}$ the central-FD
Zerilli residual on the output grid. The deployed weights are
$(\lambda_{f},\lambda_{H^{1}},\lambda_{\mathrm{ring}},
  \lambda_{\mathrm{obs}},\lambda_{\mathrm{tw}},
  \lambda_{\mathrm{pde}})
  = (1,\,0.20,\,10,\,5,\,1,\,0.05)$;
prior configurations differ by one or two terms, and the
ablation of Appendix~\ref{app:supp-fno}
(Table~\ref{tab:fno-versions}) isolates
$\mathcal{L}_{\mathrm{obs}}$ and $\mathcal{L}_{\mathrm{tw}}$ as
the QNM-critical additions.

\paragraph{Optimiser.} Training uses Adam only
(Table~\ref{tab:train-config}); the schedule is standard for
FNOs \citep{Li2021fno,Kovachki2023neuralop}, and L-BFGS on
$\sim\!7.8\times 10^{7}$ parameters is prohibitive on a single
A100.

\paragraph{Observer location.} All FNO numbers are reported at
$\xq = 2\,M$ rather than the PINNs' $10\,M$: within the
$[10,100]\,M$ extraction window the ringdown at $10\,M$ falls
below the field-error floor of the FD reference itself, so M4
returns no plateau for either source, while at $2\,M$ it
validates on both (observer sweep in
Appendix~\ref{app:supp-xq}). Since $(\omega,\tau)$ are
observer-independent, the choice affects only the conditioning
of the fit.

% --------------------------------------------------------------
\subsection{Hybrid coarse-FD + neural-residual surrogate}
\label{sec:hyb-method}

Where the FNO of Sec.~\ref{sec:fno-method} commits the network
to learning the entire wave-propagation operator, the hybrid
keeps a cheap classical solver for the propagation and asks the
network only for what that solver cannot resolve: it runs a
single coarse-grid FD solve, upsamples it to the fine grid, and
trains a small 2-D FNO to predict the pointwise discretisation
residual estimated by a label-free Richardson extrapolation of
two coarse solves. The network is never shown the fine-grid
field in its loss, and a parameter-free gradient gate confines
the correction to the under-resolved wavefronts, handing the
smooth interior and causal exterior back to the prior. The
extractors and reporting convention of
Secs.~\ref{sec:qnm-methods}--\ref{sec:reporting} apply
unchanged; the pipeline is summarised in
Fig.~\ref{fig:hyb-arch}.

\begin{figure}[!htbp]
\centering
\resizebox{0.78\textwidth}{!}{%
\begin{tikzpicture}[
  every node/.style={font=\small},
  ic/.style={rectangle, draw, fill=yellow!18, rounded corners=2pt,
             minimum height=8mm, minimum width=42mm, inner sep=3pt,
             align=center},
  fd/.style={rectangle, draw, fill=red!12, rounded corners=2pt,
             minimum height=11mm, minimum width=58mm, inner sep=3pt,
             align=center},
  up/.style={rectangle, draw, fill=red!6, rounded corners=2pt,
             minimum height=10mm, minimum width=58mm, inner sep=3pt,
             align=center},
  inp/.style={rectangle, draw, fill=blue!10, rounded corners=2pt,
              minimum height=7mm, minimum width=88mm, inner sep=3pt,
              align=center, font=\small},
  stack/.style={rectangle, draw=none, fill=none,
                inner sep=2pt, align=center, font=\small},
  stackbg/.style={rectangle, draw, fill=blue!5, rounded corners=3pt,
                  inner sep=4pt},
  fnobox/.style={rectangle, draw, fill=gray!12, rounded corners=2pt,
                 minimum height=14mm, minimum width=58mm, inner sep=3pt,
                 align=center},
  fnoblock/.style={rectangle, draw=none, fill=none,
                   inner sep=2pt, align=center},
  fnobg/.style={rectangle, draw, fill=gray!18, rounded corners=3pt,
                inner sep=5pt},
  outn/.style={rectangle, draw, fill=orange!25, rounded corners=2pt,
               minimum height=10mm, minimum width=58mm, inner sep=3pt,
               align=center},
  sum/.style={circle, draw, fill=green!20, inner sep=0pt,
              minimum size=9mm, font=\large},
  loss/.style={rectangle, draw, fill=green!12, rounded corners=2pt,
               minimum height=10mm, minimum width=68mm, inner sep=3pt,
               align=center},
  arr/.style={->, >=stealth, thick, black},
  arrs/.style={->, >=stealth, thick, blue!55!black, dashed},
  arrt/.style={->, >=stealth, thick, gray!55!black, dashed},
  sec/.style={font=\footnotesize\itshape, gray!55!black, anchor=west},
]
  % ============ classical solver path ============
  \node[ic]   (icbox) {initial data $(M,\,x_{0},\,\sigma)$};
  \node[fd, below=4mm of icbox]
        (cfd)   {coarse RK4 method-of-lines, 2nd-order stencil\\
                 $(\Delta x,\Delta t)_{\mathrm{c}}=(0.8,0.4)\,M$ \\
                 grid $251\times 126$ ($k=4$)};
  \node[up, below=4mm of cfd]
        (upbox) {quintic-spline upsample $\mathcal{U}$ in $(t,x)$\\
                 (Eq.~\eqref{eq:hyb-upsample}, fine grid $1001\times 501$)};

  \draw[arr] (icbox) -- (cfd);
  \draw[arr] (cfd) -- (upbox);

  % ============ five-channel input stack ============
  \node[stack, below=6mm of upbox] (stackhdr) {%
    \textbf{five-channel input on fine grid}\;
    $X\in\mathbb{R}^{5\times N_{t}\times N_{x}}$};
  \node[inp, below=2mm of stackhdr]
        (c0) {ch~0: $\mathcal{U}\Phi^{\mathrm{c}}(x,t)$ â€” quintic-upsampled $k{=}4$ prior};
  \node[inp, below=1mm of c0]
        (c1) {ch~1: $\Phi^{\mathrm{c}}(x,0)$ broadcast in $t$};
  \node[inp, below=1mm of c1]
        (c2) {ch~2: $V_{\ell}(x;M)$ broadcast in $t$};
  \node[inp, below=1mm of c2]
        (c3) {ch~3: $M$ broadcast over $(x,t)$};
  \node[inp, below=1mm of c3]
        (c4) {ch~4: $\Pi^{\mathrm{c}}_{0}(x)$ broadcast in $t$
              (Eq.~\eqref{eq:hyb-pi0})};
  \begin{scope}[on background layer]
    \node[stackbg, fit=(stackhdr)(c0)(c4)] (stackbg) {};
  \end{scope}

  \draw[arr] (upbox) -- (stackbg.north);

  % ============ FNO residual operator ============
  \node[fnoblock, below=8mm of stackbg.south] (fnohdr) {%
    \begin{minipage}{96mm}
      \centering
      \textbf{neural residual operator $\mathcal{H}_{\theta}$ â€” four-layer 2-D FNO}\\[1mm]
      hidden channels $h=32$,\;
      Fourier modes $(K_{t},K_{x})=(16,32)$,\;
      domain padding $0.10$
    \end{minipage}};

  \node[fnobox, below=3mm of fnohdr]
        (lift)  {lift $1{\times}1$ conv: $5{\to}32$ channels};
  \node[fnobox, below=2mm of lift]
        (sb)    {$L=4 \times$ spectral block\\
                 $v^{(\ell+1)}=\mathrm{GELU}\bigl(
                  \mathcal{F}^{-1}[R^{(\ell)}{\odot}\mathcal{F}v^{(\ell)}]
                  + W^{(\ell)}v^{(\ell)}\bigr)$};
  \node[fnobox, below=2mm of sb]
        (proj)  {projection $1{\times}1$ conv: $32{\to}1$ channel};

  \draw[arr] (lift) -- (sb);
  \draw[arr] (sb) -- (proj);
  \begin{scope}[on background layer]
    \node[fnobg, fit=(fnohdr)(lift)(proj)] (fnoall) {};
  \end{scope}

  \draw[arr] (stackbg.south) -- (fnoall.north);

  % ============ output residual + reconstruction ============
  \node[outn, below=8mm of fnoall.south]
        (delta) {$g\,\delta\Phi_{\theta}(x,t)$ â€” gated residual\\
                 $g=|\nabla\mathcal{U}\Phi^{\mathrm{c}}|/
                 (|\nabla\mathcal{U}\Phi^{\mathrm{c}}|+s),\;s{=}10^{-3}$\\
                 (Eq.~\eqref{eq:hyb-gate})};
  \draw[arr] (fnoall.south) -- (delta.north);

  % sum node and Phi_hybrid
  \node[sum, below=8mm of delta] (sumn) {$+$};
  \node[outn, fill=orange!45, below=4mm of sumn]
        (phih)  {$\Phi^{\mathrm{hyb}}(x,t)
                 =\mathcal{U}\Phi^{\mathrm{c}}+g\,\delta\Phi_{\theta}$\\
                 (Eq.~\eqref{eq:hyb-reconstruct})};
  \draw[arr] (delta) -- (sumn);
  \draw[arr] (sumn) -- (phih);

  % skip connection from upsample (long curve down the left)
  \draw[arrs] (upbox.west) -- ++(-3.6,0) |-
              node[pos=0.10, anchor=south, rotate=90, font=\scriptsize, blue!55!black, yshift=2pt]
                   {skip: $\mathcal{U}\Phi^{\mathrm{c}}$ (physics prior)}
              (sumn.west);

  % training-only target + loss (right side, dashed)
  \node[outn, fill=red!18, right=22mm of delta]
        (target){label-free Richardson target\\
                 $\delta\Phi_{\mathrm{R}}=\Phi^{\mathrm{R}}-\mathcal{U}\Phi^{\mathrm{c}}$\\
                 (Eqs.~\eqref{eq:hyb-richardson},\eqref{eq:hyb-residual})};
  \node[loss, below=4mm of target]
        (lhyb)  {$\mathcal{L}_{\mathrm{hyb}}
                 =\mathrm{MSE}\bigl(g\,\delta\Phi_{\theta},\,\delta\Phi_{\mathrm{R}}\bigr)$\\
                 (Eq.~\eqref{eq:hyb-loss})};
  \draw[arrt] (delta.east) --
              node[pos=0.35, above, font=\scriptsize, gray!55!black] {training only}
              (target.west);
  \draw[arr]  (target) -- (lhyb);

  % gradient backflow from loss to FNO (training only)
  \draw[arrt, gray!65!black] (lhyb.east) -- ++(0.5,0) |-
              node[pos=0.25, anchor=south, rotate=90, font=\scriptsize, gray!55!black, yshift=2pt]
                   {gradient $\nabla_{\theta}\mathcal{L}_{\mathrm{hyb}}$}
              (fnoall.east);
\end{tikzpicture}
}
\caption{Hybrid surrogate pipeline of Sec.~\ref{sec:hyb-method},
shown as a top-to-bottom flow. The classical RK4
method-of-lines solver of Appendix~\ref{app:fd} produces the
coarse field $\Phi^{\mathrm{c}}$ on
$(\Delta x,\Delta t)_{\mathrm{c}} = (0.8,0.4)\,M$ ($k=4$), which
is quintic-spline upsampled in $(t,x)$ onto the fine grid by the
operator $\mathcal{U}$ of Eq.~\eqref{eq:hyb-upsample}. The
five-channel input stack combines this prior
$\mathcal{U}\Phi^{\mathrm{c}}$ with the initial displacement
$\Phi^{\mathrm{c}}(x,0)$, initial velocity
$\Pi^{\mathrm{c}}_{0}(x)$ (Eq.~\eqref{eq:hyb-pi0}), Zerilli
potential $V_{\ell}(x;M)$ and broadcast mass $M$. A four-layer
2-D FNO $\mathcal{H}_{\theta}$ with hidden channel count
$h = 32$ and retained Fourier modes $(K_{t},K_{x}) = (16,32)$
predicts an additive correction, which is multiplied by the
parameter-free gradient gate
$g=|\nabla\mathcal{U}\Phi^{\mathrm{c}}|/
(|\nabla\mathcal{U}\Phi^{\mathrm{c}}|+s)$
(Eq.~\eqref{eq:hyb-gate}, $s = 10^{-3}$, a deterministic
function of the prior) before being added back; the hybrid
prediction Eq.~\eqref{eq:hyb-reconstruct} adds the
quintic-upsampled coarse field as a hard skip (blue dashed),
and the gate confines the correction to the under-resolved
wavefronts. The dashed grey path on the right depicts the
training-only, label-free Richardson target
$\delta\Phi_{\mathrm{R}} = \Phi^{\mathrm{R}} -
\mathcal{U}\Phi^{\mathrm{c}}$
(Eqs.~\eqref{eq:hyb-richardson},\eqref{eq:hyb-residual}), built
from $k = 4$ and $k = 2$ coarse solves alone, and the MSE loss
Eq.~\eqref{eq:hyb-loss} against which $\mathcal{H}_{\theta}$ is
fitted; at inference time the right-hand path is detached and
only the single $k = 4$ coarse solve and the left-hand
reconstruction path are evaluated. Total trainable parameters:
$1.13\times 10^{6}$.}
\label{fig:hyb-arch}
\end{figure}

\paragraph{Discretisation and grids.} The fine and coarse
grids (Table~\ref{tab:surrogate-config}) share the box and the
RK4 method-of-lines scheme of Appendix~\ref{app:fd} with the
same second-order centred stencil. Coarsening by $k$ cuts FD
work by $k^{3} = 64$, so the classical component of one
deployed prediction costs $\tfrac{1}{64}$ of the fine solve,
with the FNO forward pass small by comparison
(Sec.~\ref{sec:res-hyb}).

\paragraph{Label-free Richardson target.} At $k = 4$ the
second-order stencil leaves a large
$\mathcal{O}(\Delta x_{\mathrm{c}}^{2})$ dispersion error: the
upsampled prior misses the fine reference by $4.93\%$ relative
$L^{2}$ on the canonical draw ($6.11\%$ in the population mean;
Sec.~\ref{sec:res-hyb}). We therefore run the same problem at
$k = 4$ and $k = 2$, upsample both to the fine grid with the
operator $\mathcal{U}$ of Eq.~\eqref{eq:hyb-upsample}, and form
\begin{equation}
  \Phi^{\mathrm{R}}
  \;=\; \frac{4\,\mathcal{U}\Phi^{\mathrm{c}}_{2}
              - \mathcal{U}\Phi^{\mathrm{c}}}{3},
  \label{eq:hyb-richardson}
\end{equation}
the standard $p = 2$ Richardson combination: the
$\mathcal{O}(\Delta x_{\mathrm{c}}^{2})$ terms cancel and
$\Phi^{\mathrm{R}} = \Phi + \mathcal{O}(\Delta x_{\mathrm{c}}^{4})$
up to the quintic-interpolation error of the upsample. The
network is trained to predict
the Richardson-estimated residual
\begin{equation}
  \delta\Phi_{\mathrm{R}}(x, t)
  \;=\; \Phi^{\mathrm{R}}(x, t) - \mathcal{U}\Phi^{\mathrm{c}}(x, t),
  \label{eq:hyb-residual}
\end{equation}
which agrees with the true error
$\Phi^{\mathrm{f}} - \mathcal{U}\Phi^{\mathrm{c}}$ to
$\mathcal{O}(\Delta x_{\mathrm{c}}^{4})$. The entire supervision
signal is thus built from coarse solves --- the fine solve is
retained only to \emph{evaluate} the surrogate, and at inference
only the single $k = 4$ solve is required. The target is largest
on the under-resolved wavefronts, where the stencil dephases the
high-wavenumber content of the prompt burst, and near zero
elsewhere: a bounded repair task rather than full-field
synthesis, whose spatial localisation the gate of
Eq.~\eqref{eq:hyb-gate} exploits.

\paragraph{Upsample operator.} The operator $\mathcal{U}$ used
in Eqs.~\eqref{eq:hyb-richardson}--\eqref{eq:hyb-residual} and as
the channel-0 prior is a separable degree-five (quintic)
tensor-product spline on the rectangular grid:
\begin{equation}
  (\mathcal{U}\Phi^{\mathrm{c}})(x, t)
  \;=\; \sum_{i, j} c_{ij}\,
        B_{5}\!\Bigl(\tfrac{t - t_{i}}{\Delta t_{\mathrm{c}}}\Bigr)\,
        B_{5}\!\Bigl(\tfrac{x - x_{j}}{\Delta x_{\mathrm{c}}}\Bigr),
  \label{eq:hyb-upsample}
\end{equation}
with $B_{5}$ the quintic B-spline basis and $\{c_{ij}\}$ fixed
by collocation at the coarse nodes. The quintic order keeps the
$\mathcal{O}(\Delta x_{\mathrm{c}}^{6})$ interpolation error
below the $\mathcal{O}(\Delta x_{\mathrm{c}}^{4})$ Richardson
signal; the same $\mathcal{U}$ is applied to both coarse
solves.

\paragraph{Input stack.} The five input channels are listed in
Fig.~\ref{fig:hyb-arch}: the upsampled prior
$\mathcal{U}\Phi^{\mathrm{c}}$ (the dynamical content the
network is asked to repair), the initial datum
$\Phi^{\mathrm{c}}(x, 0)$, the potential $V_{\ell}(x; M)$ of
Eq.~\eqref{eq:V-zerilli} (the mass dependence in the form the
spectral $x$-convolution exploits), the broadcast scalar $M$
(redundant with it but empirically helpful at the prompt-burst
edge), and the initial velocity
\begin{equation}
  \Pi^{\mathrm{c}}_{0}(x)
  \;=\; \frac{\Phi^{\mathrm{c}}(x, \Delta t_{\mathrm{c}}) -
              \Phi^{\mathrm{c}}(x, 0)}{\Delta t_{\mathrm{c}}},
  \label{eq:hyb-pi0}
\end{equation}
forward-Euler differenced on the coarse grid and linearly
resampled onto the fine $x$-grid; its first-order truncation is
acceptable for an auxiliary channel.

\paragraph{Residual operator, gradient gate, and reconstruction.}
The neural residual operator
\begin{equation}
  \mathcal{H}_{\theta}\colon\;
  X = \bigl[\mathcal{U}\Phi^{\mathrm{c}},\,\Phi^{\mathrm{c}}(\cdot, 0),\,
            V_{\ell}(\cdot; M),\,M,\,\Pi^{\mathrm{c}}_{0}\bigr]
  \;\longmapsto\;
  \delta\Phi_{\theta}(x, t)
  \label{eq:hyb-operator}
\end{equation}
is modulated by a parameter-free gradient gate before it is
added to the prior. The gate
\begin{equation}
  g(x, t)
  \;=\; \frac{|\nabla\mathcal{U}\Phi^{\mathrm{c}}(x, t)|}
             {|\nabla\mathcal{U}\Phi^{\mathrm{c}}(x, t)| + s},
  \qquad
  |\nabla\mathcal{U}\Phi^{\mathrm{c}}|
  = \sqrt{(\partial_{t}\mathcal{U}\Phi^{\mathrm{c}})^{2}
          + (\partial_{x}\mathcal{U}\Phi^{\mathrm{c}})^{2}},
  \label{eq:hyb-gate}
\end{equation}
is a deterministic function of the prior alone, with a single
scale $s = 10^{-3}$ whose label-free selection from the prior
gradient distribution is derived in App.~\ref{app:gate-scale}:
$g \to 1$ on the high-gradient wavefronts where the prior
carries its dispersion error, and $g \to 0$ in the smooth
interior and causal exterior. The hybrid prediction is the
gated additive skip
\begin{equation}
  \Phi^{\mathrm{hyb}}(x, t)
  \;=\; \mathcal{U}\Phi^{\mathrm{c}}(x, t)
         + g(x, t)\,\mathcal{H}_{\theta}(X)(x, t),
  \label{eq:hyb-reconstruct}
\end{equation}
which confines the correction to the under-resolved regions by
construction and reduces to the quintic-upsampled coarse solve
(the ``coarse-up'' baseline of Sec.~\ref{sec:res-hyb}) wherever
$g = 0$ or $\mathcal{H}_{\theta} \equiv 0$; the network sits in
series with the classical solver and inherits its stability
behaviour as a hard prior.

\paragraph{FNO architecture.} The residual operator
$\mathcal{H}_{\theta}$ is a 2-D Fourier neural operator
\citep{Li2021fno,Kovachki2023neuralop} on the fine $(t, x)$
grid: a $1\times 1$ lift to $h = 32$ hidden channels, $L = 4$
Fourier layers of the form
\begin{equation}
  v^{(\ell+1)}(x, t)
  \;=\; \mathrm{GELU}\!\Bigl(
         \mathcal{F}^{-1}\bigl[R^{(\ell)} \odot
         \mathcal{F}\,v^{(\ell)}\bigr](x, t)
         \;+\; W^{(\ell)}\,v^{(\ell)}(x, t)
       \Bigr),
  \label{eq:hyb-fno-layer}
\end{equation}
and a $1\times 1$ projection back to one channel, where
$\mathcal{F}$ is the 2-D DFT along $(t, x)$, $R^{(\ell)}$ the
complex spectral-weight tensor truncated to
$(K_{t}, K_{x}) = (16, 32)$ retained modes (the grid's natural
$2{:}1$ mode ratio, resolving
$k_{x} \lesssim 1.0\,M^{-1}$), $\odot$ mode-by-mode matrix
multiplication across channels, and $W^{(\ell)}$ a pointwise
residual path. Each axis is pre-padded by $p = 0.10$ against
periodic wraparound, and positional $(x, t)$ encodings break
translation equivariance to distinguish the source region from
the wave zones. The total parameter count is $1.13\times 10^{6}$ under the
\texttt{neuraloperator} factorised parametrisation
\citep{Kovachki2023neuralop} --- almost two orders of magnitude
below the end-to-end FNO (Table~\ref{tab:surrogate-config}):
the residual target has narrower spectral
support than the full field, since the wave-propagation
operator already lives in the upsampled prior.

\paragraph{Loss.} The training loss is the gridwise mean
squared error between the gated network output and the
label-free Richardson residual,
\begin{equation}
  \mathcal{L}_{\mathrm{hyb}}(\theta)
  \;=\; \frac{1}{B}\sum_{b=1}^{B}
         \frac{1}{N_{t} N_{x}}
         \sum_{i=1}^{N_{t}}\sum_{j=1}^{N_{x}}
         \bigl[
           g^{(b)}_{ij}\,\mathcal{H}_{\theta}(X^{(b)})_{ij}
           \,-\, \delta\Phi^{(b)}_{\mathrm{R},\,ij}
         \bigr]^{2},
  \label{eq:hyb-loss}
\end{equation}
averaged over the batch of size $B$. No auxiliary terms are
needed: the target is small, smooth and localised and the gate
has already confined the correction to the wavefronts, so a
uniform MSE on the gated output suffices
(Sec.~\ref{sec:res-hyb}).

\paragraph{Optimiser.} A single Adam phase
(Table~\ref{tab:train-config}) reaches the validation floor
within $100$ epochs on this small network and smooth, localised
target; a trial L-BFGS phase gave no measurable gain. The
per-epoch validation history is shown in
Sec.~\ref{sec:res-hyb}.

\paragraph{Training distribution.} The Sobol distribution,
split and canonical BH are listed in
Table~\ref{tab:surrogate-config}; the fine, $k = 4$ and $k = 2$
solves are precomputed and cached per triple. The
$(x_{0}, \sigma)$ box is contracted relative to the FNO's to
keep the prompt burst inside the shorter $t \in [0, 50]\,M$
hybrid window --- the only intentional difference between the
two surrogates' distributions.

\paragraph{Observer location.} Hybrid numbers are reported at
$\xq = 2\,M$, matching the FNO of Sec.~\ref{sec:fno-method} for
direct comparability; since the coarse-up baseline already sits
at the extractor floor there (Sec.~\ref{sec:res-hyb}),
the hybrid's value is reported through the global field-error
reduction rather than the median QNM.

% --------------------------------------------------------------
\subsection{QNM extraction methods}
\label{sec:qnm-methods}

Once any of the five models has produced a field, the QNM
parameters $(\omega,\tau)$ are extracted post-hoc from the time
series $\Phi(\xq,t)$ at the model's observer position
($\xq = 10\,M$ for the PINNs, $\xq = 2\,M$ for the surrogates).
The scan grids below are quoted for the PINN window
$[10,50]\,M$; on the surrogate windows the same procedures apply
with the block-width rule $w = \lfloor N/2\rfloor$ unchanged. We
apply five methods: M1--M3 mirror standard numerical-relativity
practice; M4--M5 are the plateau-based estimators that
constitute our methodological contribution, relying on the
operational definition below (formalised and motivated in
Sec.~\ref{sec:plateau}).

\begin{definition}[fit-window plateau]
\label{def:plateau}
A fit window $[t_0,\,\tend]$ lies inside the QNM regime if and only
if shifting either edge by a small amount produces no significant
change in the recovered $(\omega,\tau)$. Operationally, in M4 and
M5 the plateau is the contiguous block of fixed size
$w = \lfloor\tfrac12 N\rfloor$ along each scan axis that minimises
the combined relative population standard deviation
$\mathrm{std}(\omega)/|\bar\omega|+\mathrm{std}(\tau)/|\bar\tau|$ of
the per-window fits inside the block (population standard deviation,
divisor $N$).
\end{definition}

Method 1 (M1), the procedure adopted by \citet{Patel2024pinn},
estimates $\omega$ from the largest peak of a zero-padded,
Hann-windowed FFT of $\Phi(\xq,t)$ over the fit window
(parabolic interpolation around the peak bin), and $\tau$ from
an ordinary least-squares fit of Eq.~\eqref{eq:logline} to the
local maxima $\log|\Phi(\xq, t_k)|$ versus $t_k$.

Method 2 (M2) fits a single damped sinusoid to $\Phi(\xq,t)$ on
the chosen window,
\[
  \Phi_{\mathrm{model}}(t) = A\,e^{-t/\tau}\cos(\omega t + \phi),
\]
by bound-constrained Levenberg--Marquardt; the four-parameter
fit is the most sensitive of the five methods to overtone
contamination at the early window edge.

Method 3 (M3) is ESPRIT subspace decomposition
\citep{Roy1989esprit}, applied to the Hilbert-analytic signal.
It is ill-conditioned on the PINN waveforms at $\xq = 10\,M$
(model order $K = 6$; Appendix~\ref{app:esprit}) and is not used
for primary values; on the surrogate fields at $\xq = 2\,M$ it
is well-conditioned and is quoted in the corresponding tables.

Method 4 (M4) is two-mode NLS with a start-time plateau scan,
following the Schwarzschild $\ell=m=2$ overtone-aware
prescription of \citet{Giesler2019overtones}: the overtone term
absorbs residual $n=1$ power that would otherwise bias a
single-mode template at early times. The two-mode template,
\begin{equation}
  \Phi_{\mathrm{model}}(t) =
  A_0\,e^{-t/\tau_0}\cos(\omega_0 t + \phi_0)
  + A_1\,e^{-t/\tau_1}\cos(\omega_1 t + \phi_1),
  \label{eq:two-mode}
\end{equation}
is fitted by nonlinear least squares over $[t_0,\,\tend]$, with
the longer-$\tau$ mode identified as the fundamental;
Algorithm~\ref{alg:m4} scans $t_0$ and reports the plateau mean
and population standard deviation as the M4 estimate and its
fit-window systematic uncertainty, with the block width fixed by
the convention $w = \lfloor\tfrac12 N_{t_0}\rfloor$.

\begin{algorithm}[t]
\caption{M4 --- 1-D plateau scan for $(\omega_0,\tau_0)$.}
\label{alg:m4}
\begin{algorithmic}[1]
\STATE \textbf{Input:} time series $\Phi(\xq,t)$,
       grid $\{t_0^{(k)}\}_{k=1}^{16} \subset [10,25]\,M$,
       fixed $\tend = 50\,M$, plateau width $w = 8$.
\FOR{$k = 1,\dots,16$}
  \STATE Fit Eq.~\eqref{eq:two-mode} to $\Phi(\xq,t)$ on $[t_0^{(k)},\tend]$
         by NLS.
  \STATE Identify the longer-$\tau$ component as the fundamental,
         giving $(\omega_0^{(k)},\tau_0^{(k)})$.
\ENDFOR
\STATE Find the contiguous index range $k^* : k^*{+}w{-}1$ minimising
       the combined relative standard deviation
       $\mathrm{std}(\omega_0)/\bar\omega_0 + \mathrm{std}(\tau_0)/\bar\tau_0$.
\STATE \textbf{Return} plateau mean
       $(\bar\omega_0,\bar\tau_0)$ and population standard deviation
       $(\sigma_{\omega_0},\sigma_{\tau_0})$ over the plateau block.
\end{algorithmic}
\end{algorithm}

Method 5 (M5) generalises M4 by sweeping both window edges on a
$10\times 6$ rectangular grid ($t_0 \in [10,\,25]\,M$,
$\tend \in [30,\,50]\,M$), with the plateau the contiguous
$5\times 3$ rectangle of smallest combined relative scatter
under the same $\lfloor\tfrac12 N\rfloor$ rule applied per axis,
reported as for M4. To our knowledge M5 is the first
systematic two-dimensional plateau analysis applied to PINN-derived
QNM extraction. Both scans are sketched in Fig.~\ref{fig:windows}.

\begin{figure}[t]
\centering
%-------------------------------------------------------------
% (a) M4 1-D start-time scan
%-------------------------------------------------------------
\begin{subfigure}[t]{\textwidth}
\centering
\begin{tikzpicture}[>=stealth, x=0.20cm, y=0.45cm,
                    every node/.style={font=\scriptsize}]
  % --- decorative ringdown waveform (top band) ---
  \begin{scope}[yshift=2.6cm]
    \draw[domain=0:50, samples=240, smooth, thick, blue!55!black]
      plot (\x, {0.9*exp(-0.09*\x)*cos(72*\x)});
    \node[blue!55!black, anchor=west] at (51,0)
         {$\Phi(\xq,t)$};
    % light dashed connector to indicate same time axis
    \foreach \x in {10,18,25,50}
      \draw[gray!40, dashed, very thin] (\x,-1.0) -- (\x,-2.5);
  \end{scope}

  % --- M4 windows: vary t_0 in [10,25], fixed t_end = 50 ---
  % plateau block (t_0 = 10..16) drawn solid
  \foreach \t/\y in {10/0, 12/0.55, 14/1.10, 16/1.65}
    \draw[orange!85!black, very thick] (\t,\y) -- (50,\y);
  % post-plateau windows drawn dotted
  \foreach \t/\y in {18/2.20, 22/2.75}
    \draw[orange!85!black, very thick, dotted] (\t,\y) -- (50,\y);
  \node[orange!85!black, anchor=west] at (51,1.4)
       {M4: scan $t_0$};

  % time axis
  \draw[->, thick] (-1,-0.6) -- (54,-0.6) node[right]{$t/M$};
  \foreach \x in {0,10,18,25,50}
    \draw (\x,-0.5) -- (\x,-0.7) node[below]{$\x$};
\end{tikzpicture}
\caption{M4 schematic: 1-D scan of the start time $t_0$ (six
representative windows of the sixteen-window, step-$1\,M$ scan
shown) with the end time fixed at $t=50\,M$.
Solid bars belong to the contiguous plateau block; dotted bars sit
outside it. The decorative curve at the top depicts the ringdown
waveform $\Phi(\xq,t)$ on the same time axis.}
\label{fig:m4}
\end{subfigure}

\vspace{1.0em}

%-------------------------------------------------------------
% (b) M5 2-D plateau in (t_0, t_end) plane
%-------------------------------------------------------------
\begin{subfigure}[t]{\textwidth}
\centering
\begin{tikzpicture}[>=stealth, x=0.55cm, y=0.55cm,
                    every node/.style={font=\scriptsize}]
  % axes
  \draw[->, thick] (-0.2,0) -- (10.5,0) node[right]{$t_0/M$};
  \draw[->, thick] (0,-0.2) -- (0,7.5) node[above]{$\tend/M$};
  \foreach \x/\lab in {0/10, 3/16.7, 6/22, 9/25}
    \draw (\x,0.08) -- (\x,-0.08) node[below]{$\lab$};
  \foreach \y/\lab in {0/30, 2.5/35, 5/42, 7/50}
    \draw (0.08,\y) -- (-0.08,\y) node[left]{$\lab$};

  % grid of (t_0,t_end) candidate windows (10x6 dots)
  \foreach \i in {0,...,9}
    \foreach \j in {0,...,5}
      \fill[gray!50] ({\i*0.95+0.1},{\j*1.25+0.2}) circle (1.6pt);

  % plateau rectangle: t_0 in [10,16.7], t_end in [42,50]
  \draw[red!80!black, very thick, fill=red!10, fill opacity=0.5]
        (-0.05,4.7) rectangle (3.1,7.1);
  \node[red!80!black, anchor=south west, font=\small] at (3.2,5.6)
       {plateau};

  % marker for inverse-variant choice t_0=18 (placed below the x-axis
  % to avoid colliding with the t_end/M axis label at the top)
  \draw[->, thick, blue!70!black] (4.85,-1.05) -- (4.85,-0.15);
  \node[blue!70!black, anchor=north, font=\small] at (4.85,-1.1)
       {$t_0{=}18\,M$ (variants A, D, E)};
\end{tikzpicture}
\caption{M5 schematic: 2-D scan in the $(t_0,\tend)$ plane. Grey
dots are candidate fit windows on a $10\times 6$ grid with
$t_0\in[10,25]\,M$, $\tend\in[30,50]\,M$. The shaded red rectangle
(plateau) is the contiguous $5\times 3$ block of lowest combined
relative scatter measured on the forward waveform,
$t_0\in[10,16.7]\,M$ and $\tend\in[42,50]\,M$. The arrow marks the
value $t_0=18\,M$ adopted for inverse variants A, D, E, which sits
just above the plateau's upper $t_0$ edge.}
\label{fig:m5}
\end{subfigure}
\caption{Fit-window scan schematics for the two plateau-based QNM
extraction methods. (a) M4 sweeps the start time only; (b) M5
sweeps both edges, with the plateau identified as a contiguous
rectangle in the $(t_0,\tend)$ plane.}
\label{fig:windows}
\end{figure}

% --------------------------------------------------------------
\subsection{Plateau-based fit-window selection}
\label{sec:plateau}

The damped-sinusoid template
$A\,e^{-t/\tau}\cos(\omega t + \phi)$ is the correct model for
$\Phi(\xq,t)$ only inside the QNM regime --- after the prompt
response has passed and before the power-law tail becomes
comparable to the exponential. Fitting it outside this regime
is \emph{model misspecification}, not a noise problem: the
resulting biases do not vanish with more data, so the fit
window is a hyperparameter of the extraction and a robust
$(\omega,\tau)$ measurement requires a reproducible criterion
for it. Definition~\ref{def:plateau} captures the criterion
adopted in M4 and M5; inside the plateau the recovered
$(\omega,\tau)$ are stationary in the window edges, and their
variation across it estimates the fit-window systematic
uncertainty.

For the Schwarzschild $\ell = 2$ fundamental at our parameters the
four relevant timescales are summarised by
\begin{equation}
  t_{\mathrm{burst}} \sim 5\,M, \qquad
  \tau_1 = 3.651\,M, \qquad
  \tau_0 = 11.241\,M, \qquad
  t_{\mathrm{tail}} \gg 50\,M,
  \label{eq:timescales}
\end{equation}
where $t_{\mathrm{burst}}$ is the prompt-burst duration set by
the initial condition Eq.~\eqref{eq:ic} and $t_{\mathrm{tail}}$
is the onset of the power-law Price tail
$\propto t^{-(2\ell+3)}$ \citep{Berti2009review}. For our setup
the QNM dominates $\Phi(\xq,t)$ over the entire simulation
interval (confirmed by direct inspection of the FD reference),
so any right edge inside $[0,50]\,M$ lies within the QNM regime
--- borne out by the M5 plateaus extending in $\tend$ to the
domain end (Sec.~\ref{sec:results}).
This differs from the single-window practice of
\citet{Patel2024pinn}, who specify no numerical $t_0$ or $\tend$
and report no window sensitivity. The closest precedents are the
start-time stability scans of \citet{Buonanno2007ringdown} and
the GW150914 ringdown analysis \citep{LIGO2016ringdown}, both
sweeping $t_0$ at fixed $\tend$, and the Bayesian start-time
marginalisation of \citet{Isi2021priors}. The present approach
extends these by sweeping both edges simultaneously (M5) and by
quoting the plateau spread as a fit-window systematic
uncertainty, in lieu of the nonlinear-fit covariance which our
M2 implementation discards (Sec.~\ref{sec:reporting}).
% --------------------------------------------------------------
\subsection{Reporting convention}
\label{sec:reporting}

We quote one $(\omega,\tau)$ per experiment. $\omega$ is always
the M4 plateau mean, and the $\sigma_\omega$ quoted alongside it
is the M4 plateau standard deviation (Algorithm~\ref{alg:m4},
line~7), an estimate of the fit-window systematic uncertainty in
the sense of Def.~\ref{def:plateau}. $\tau$ is likewise taken
from M4, except for the inverse PINN: there the training embeds
the ringdown template of Eq.~\eqref{eq:loss-ring}, whose
$A_{\mathrm{ring}}$--$\tau$ degeneracy (Sec.~\ref{sec:inv-pinn})
biases the template-window fit, and the primary $\tau$ is
instead M2 on the wide window $[10,\,50]\,M$, never the learned
scalar $\tau_{\mathrm{learned}}$. M2 over a single
fixed window does not produce an uncertainty, so the dispersion
in $\tau$ across variants is reported through the per-window
per-method tables in the repository documentation rather than as
a single $\sigma_\tau$ on the primary value.

The lower edge $t = 10\,M$, used unchanged across all
extraction variants, sits just before the first ringdown peak
of the FD reference ($t \approx 10.8\,M$) and matches the
baseline template window $\tring$ of Eq.~\eqref{eq:loss-ring}.

Full per-window per-method results are tabulated in the
repository documentation, with the corresponding plateau plots
and 2-D heatmaps in Appendix~\ref{app:plots}; the summary table
in Sec.~\ref{sec:results} collects only the values selected by
this convention.

Quoting different estimators for $\omega$ and $\tau$ follows
established numerical-relativity practice
\citep{Berti2007ringdown,Patel2024pinn}: $\omega$ is best
estimated from phase information aggregated across many cycles
--- the M4 plateau is the lowest-variance such estimator here,
with $\sigma_\omega \sim 10^{-5}$ for the converged variants ---
and $\tau$ from amplitude information over the longest available
decay range, motivating the wide M2 window in the inverse case.
